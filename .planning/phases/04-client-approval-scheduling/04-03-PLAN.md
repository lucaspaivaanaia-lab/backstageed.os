---
phase: 04-client-approval-scheduling
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/pm/board/page.tsx
  - app/pm/board/actions.ts
  - app/pm/board/board-panel.tsx
autonomous: true
requirements: [APR-04, SCH-01, SCH-02]

must_haves:
  truths:
    - "PM sees the Client's adjustment comment directly on the card detail dialog once the card is back in producao — not a separate document"
    - "PM can register a publish date/time on a card only once it has reached agendamento stage — the field is hidden, not just disabled, for every earlier stage"
    - "A card in agendamento stage with a publish date set displays 'Pronto para publicar' on both the board card and its detail dialog"
    - "The Client's own actions (approveCard/requestAdjustment, plan 04-02) never touch publish_at — only PM/Admin's existing updateCardDetails can set it"
  artifacts:
    - path: "app/pm/board/page.tsx"
      provides: "cards select extended with client_adjustment_comment/publish_at, BoardCard type extended"
      contains: "publish_at"
    - path: "app/pm/board/actions.ts"
      provides: "updateCardDetails writes publish_at as one more PM/Admin-writable column"
      contains: "publish_at: parsed.data.publishAt"
    - path: "app/pm/board/board-panel.tsx"
      provides: "Data de publicação field (gated to agendamento), read-only Comentário do cliente block, Pronto para publicar badge on both the dialog and the board card"
      contains: "isReadyToPublish"
  key_links:
    - from: "app/pm/board/board-panel.tsx"
      to: "lib/cards/publish-status.ts"
      via: "isReadyToPublish import, used identically to the Client's own history-tab badge (04-02)"
      pattern: "isReadyToPublish"
    - from: "app/pm/board/actions.ts"
      to: "lib/validation/cards.ts"
      via: "updateCardDetailsSchema.publishAt (04-01)"
      pattern: "publishAt"
---

<objective>
Deliver the PM-facing half of Phase 4: surface the Client's adjustment comment on the existing card detail dialog (APR-04), and let the PM register a publish date once a card reaches `agendamento` (SCH-01), with a "Pronto para publicar" badge computed from it (SCH-02) — two small, additive changes to the ALREADY-EXISTING PM board, zero new Server Action, zero new screen.

Purpose: closes the loop APR-01..03/KAN-04 (plan 04-02) opened — the PM needs to actually see what the Client said, and needs a way to tell the system "this is going out on this date."
Output: `app/pm/board/page.tsx`, `app/pm/board/actions.ts`, `app/pm/board/board-panel.tsx` — all three extended in place, no new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-client-approval-scheduling/04-CONTEXT.md
@.planning/phases/04-client-approval-scheduling/04-RESEARCH.md
@.planning/phases/04-client-approval-scheduling/04-PATTERNS.md
@.planning/phases/04-client-approval-scheduling/04-UI-SPEC.md
</context>

<interfaces>
<!-- Exact current-state code the executor needs. -->

From `lib/cards/publish-status.ts` (created by 04-01, Wave 1):
```
export function isReadyToPublish(card: { stage: CardStage | null; publish_at: string | null }): boolean;
```

From `lib/validation/cards.ts`'s `updateCardDetailsSchema` (extended by 04-01) — now carries a `publishAt` field sibling to `dueDate`, identical refine/nullable shape.

From `app/pm/board/board-panel.tsx` lines 218-223 (`toDatetimeLocalValue`, existing helper to reuse verbatim for `publish_at` too — do not write a second date-formatting helper).

From `app/pm/board/board-panel.tsx` lines 1296-1318 (the exact "Prazo" `Input type="datetime-local"` block — the structural template for the new "Data de publicação" block).

From `app/pm/board/board-panel.tsx` lines 1146-1165 (`handleSaveDetails` — the exact payload object `updateCardDetails` is called with; `publishAt` joins this object as one more sibling key).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire publish_at + client_adjustment_comment through the read/write path</name>
  <files>app/pm/board/page.tsx, app/pm/board/actions.ts</files>
  <read_first>
    app/pm/board/page.tsx (full file, already read during planning — `BoardCard` type at lines 42-57, the `cards` select at lines 112-120)
    app/pm/board/actions.ts lines 496-555 (`updateCardDetails`'s full current body — the exact `.update({...})` payload object this task extends by one key)
    lib/validation/cards.ts (04-01's extended `updateCardDetailsSchema` — confirms `publishAt` is already a valid input field)
  </read_first>
  <action>
    In `app/pm/board/page.tsx`: add `client_adjustment_comment: string | null;` and `publish_at: string | null;` to the exported `BoardCard` type (as siblings to the existing `due_date: string | null;` field). Extend the `cards` select string (line ~116) to include `client_adjustment_comment, publish_at` alongside the existing column list — the query already reads every other card column this way, this is a pure additive change, no new query, no new `.eq()`.

    In `app/pm/board/actions.ts`'s `updateCardDetails` (lines 496-555): add `publish_at: parsed.data.publishAt,` as a sibling key to the existing `due_date: parsed.data.dueDate,` line inside the `.update({...})` call. No other change to this function — `assertPmOrAdminCaller` (already gating this function since `260811-oe0`) continues to be the sole authorization boundary; `publish_at` is simply one more PM/Admin-writable column, exactly like `due_date`.
  </action>
  <acceptance_criteria>
    - `BoardCard` type in `app/pm/board/page.tsx` contains both `client_adjustment_comment` and `publish_at` as `string | null` fields
    - The `cards` select string in `app/pm/board/page.tsx` contains the literal substring `client_adjustment_comment, publish_at` (or both column names present, order-independent)
    - `updateCardDetails`'s `.update({...})` payload in `app/pm/board/actions.ts` contains `publish_at: parsed.data.publishAt`
    - No new function, no new authorization check, no new Server Action is added by this task — `git diff --stat` shows only these 2 files with small, additive diffs
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>publish_at and client_adjustment_comment flow from the DB through page.tsx's loader into BoardCard, and updateCardDetails can write publish_at.</done>
</task>

<task type="auto">
  <name>Task 2: board-panel.tsx — Data de publicação field, Comentário do cliente block, Pronto para publicar badge</name>
  <files>app/pm/board/board-panel.tsx</files>
  <read_first>
    app/pm/board/board-panel.tsx lines 999-1330 (`CardDetailDialogBody` — full existing structure: `draftDueDate` state at line 1030, `hasDetailChanges` at lines 1079-1085, `handleSaveDetails` at lines 1146-1165, the "Prazo" field block at lines 1296-1318, the top `StatusBadge` at lines 1172-1175)
    app/pm/board/board-panel.tsx lines 1404-1487 (`BoardCardItem` — the badge composition at lines 1455-1471 this task extends)
    lib/cards/publish-status.ts (04-01's `isReadyToPublish`, exact signature)
    .planning/phases/04-client-approval-scheduling/04-UI-SPEC.md ("PM-Side Additions" section — the full copy/placement/gating spec for both additions; Copywriting Contract table for exact strings)
  </read_first>
  <action>
    Add `import { isReadyToPublish } from "@/lib/cards/publish-status";` to the top of the file.

    In `CardDetailDialogBody`: add a sibling state `const [draftPublishAt, setDraftPublishAt] = useState(toDatetimeLocalValue(card.publish_at));` next to the existing `draftDueDate` line. Add `draftPublishAt !== toDatetimeLocalValue(card.publish_at) ||` as one more clause in `hasDetailChanges`. Add `publishAt: draftPublishAt ? new Date(draftPublishAt).toISOString() : null,` as a sibling key inside `handleSaveDetails`'s `updateCardDetails({...})` call object.

    Immediately after the existing "Prazo" `<div className="flex flex-col gap-2">...</div>` block (lines ~1296-1318), add a new block, structurally identical (same `Input type="datetime-local"` + conditional "Limpar" `Button`), but: `<SectionTitle>Data de publicação</SectionTitle>`, bound to `draftPublishAt`/`setDraftPublishAt`, and the ENTIRE block wrapped in `{card.stage === "agendamento" ? (...) : null}` — hidden (not disabled) for every earlier stage, per UI-SPEC's resolved gating condition.

    Directly below the existing top `<StatusBadge tone="neutral">{card.stage ? STAGE_LABELS[card.stage] : "—"}</StatusBadge>` block (~line 1173-1175), add: `{isReadyToPublish(card) ? <StatusBadge tone="success">Pronto para publicar</StatusBadge> : null}`.

    Also directly below that same top badge block, add the read-only adjustment-comment display, gated `{card.stage === "producao" && card.client_adjustment_comment ? (...) : null}`: a `flex flex-col gap-2 rounded-md border p-3` box containing `<span className="text-body font-medium">Comentário do cliente</span>` and `<span className="text-body whitespace-pre-wrap">{card.client_adjustment_comment}</span>` — no edit affordance, this field is Client-write-only.

    In `BoardCardItem`'s badge composition (~lines 1455-1471, inside the existing `<div className="flex flex-col items-end gap-1">`): when `isReadyToPublish(card)` is true, render `<StatusBadge tone="success">Pronto para publicar</StatusBadge>` as an ADDITIONAL badge alongside the existing channel `StatusBadge` (do not remove/replace the channel badge — both can coexist in the `flex flex-col items-end gap-1` stack, same composition pattern already used for the checklist-progress badge on this same stack).
  </action>
  <acceptance_criteria>
    - `draftPublishAt` is seeded via `toDatetimeLocalValue(card.publish_at)` — the SAME existing helper function, no second date-parsing helper is introduced
    - The new "Data de publicação" block is wrapped in a condition that is `false` for every stage except `"agendamento"` (grep confirms `card.stage === "agendamento"` guards this specific block)
    - `handleSaveDetails`'s `updateCardDetails({...})` call object contains a `publishAt` key
    - The "Comentário do cliente" block's condition includes both `card.stage === "producao"` AND a truthiness check on `client_adjustment_comment`
    - `BoardCardItem` renders the channel `StatusBadge` AND, conditionally, the "Pronto para publicar" badge — never one replacing the other in source (both JSX nodes present)
    - `isReadyToPublish` is imported from `@/lib/cards/publish-status`, never reimplemented inline in this file
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npm run build</automated>
  </verify>
  <done>The PM's existing card detail dialog gains a stage-gated publish-date field wired into the existing save flow, a read-only Client-comment block, and a computed "Pronto para publicar" badge shown both in the dialog and on the board card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PM/Admin browser → `updateCardDetails` | Unchanged from before this plan — `assertPmOrAdminCaller` remains the sole authorization boundary; this plan adds one more writable column to an already-gated function, introducing no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01c | Elevation of Privilege | `publish_at` becoming reachable by a non-PM/Admin caller through `updateCardDetails` | mitigate — confirmed no new exposure | `updateCardDetails` is unconditionally gated by `assertPmOrAdminCaller` (unchanged by this plan, verified during 04-01's planning by direct source read) BEFORE the `.update({...})` call this task extends — `publish_at` is reachable through the exact same authorization path as `due_date`/`channel`/`description`, nothing new |
| T-04-07 | Information Disclosure | `client_adjustment_comment` rendering to a role other than PM/Admin | accept — not applicable | This field is only ever selected/rendered inside `app/pm/board/*` (PM/Admin-only routes, gated by `middleware.ts`'s `roleRoot` map, unchanged); the Client's OWN read of this column (if any) is governed entirely by plan 04-02's RLS branch and UI, not by this plan |
| T-04-08 | Tampering | The Client's `approveCard`/`requestAdjustment` (plan 04-02) somehow reaching `publish_at` | mitigate — verified structurally impossible | 04-01's `buildClientApprovePayload`/`buildClientAdjustPayload` have `publish_at` in their own `FORBIDDEN_KEYS` negative test set; this plan's `updateCardDetails` extension is a COMPLETELY SEPARATE code path the Client's actions never call (confirmed in 04-02's Task 1 acceptance criteria: `updateCardDetails`/`advanceStage`/`moveCard` never appear in `app/client/actions.ts`) |
| T-04-SC | Tampering (supply chain) | npm/pip/cargo installs | accept — not applicable | Zero new packages this phase |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npm run build` succeeds
- `git diff --stat` against `main` shows exactly 3 files touched, all additive (no deletions of existing fields/blocks)
</verification>

<success_criteria>
- PM sees the Client's adjustment comment on the card detail dialog whenever the card is back in `producao` with a non-null comment
- PM can set/clear a publish date only once a card is in `agendamento`
- "Pronto para publicar" renders identically (same predicate, same copy, same tone) on both the PM board and — once plan 04-02 lands — the Client's own history tab
</success_criteria>

<output>
Create `.planning/phases/04-client-approval-scheduling/04-03-SUMMARY.md` when done
</output>
