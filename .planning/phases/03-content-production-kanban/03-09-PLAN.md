---
phase: 03-content-production-kanban
plan: 09
type: execute
wave: 6
depends_on: ["03-07", "03-08"]
files_modified:
  - app/pm/board/page.tsx
  - app/pm/board/board-panel.tsx
autonomous: false
requirements: [KAN-01, KAN-03]

must_haves:
  truths:
    - "Every one of the five stage columns has its own + trigger, and a card created from it appears in that column — not in Briefing (D-14)"
    - "A card created directly in revisão interna already has its checklist listed and its Avançar gate active the first time the PM opens it (D-15)"
    - "The top-level Criar card button still creates in Briefing, unchanged (D-14 keeps both entry points)"
    - "A card can be given a multi-line plain-text description at creation or later, and it renders only inside the card detail dialog — never on the board face (D-16, D-17, D-18)"
    - "A card can be given one optional assignee, and the picker only ever offers PMs already assigned to that client (D-19)"
    - "Selecting no assignee is a valid, persisted state — a card can be unassigned and later picked up"
    - "A client with no assigned PMs beyond the viewer still renders a usable form instead of an empty broken Select"
  artifacts:
    - path: "app/pm/board/page.tsx"
      provides: "card query extended with description/assignee_id, plus the per-client PM roster and resolved assignee display names"
      contains: "listClientPmRoster"
    - path: "app/pm/board/board-panel.tsx"
      provides: "per-column create triggers, the extended create Dialog (stage/description/assignee), and the Descrição + Responsável sections of the card detail Dialog"
      min_lines: 300
  key_links:
    - from: "app/pm/board/page.tsx"
      to: "lib/actions/clients.ts"
      via: "listClientPmRoster(activeClientId) feeding the assignee picker options"
      pattern: "listClientPmRoster"
    - from: "app/pm/board/board-panel.tsx"
      to: "app/pm/board/actions.ts"
      via: "createCard called with an explicit stage from each column trigger, and updateCardDetails from the detail Dialog"
      pattern: "updateCardDetails"
    - from: "app/pm/board/board-panel.tsx"
      to: "lib/cards/stages.ts"
      via: "each column's create trigger labels itself with STAGE_LABELS[stage]"
      pattern: "STAGE_LABELS"
---

<objective>
Finish the 2026-07-31 re-scope on the UI side: a "+" on every column so a card can be born wherever the work actually starts (D-14/D-15), and two new card fields — an optional multi-line plain-text description shown only in the card detail dialog (D-16/D-17/D-18) and an optional single assignee restricted to that client's own PMs (D-19).

Purpose: KAN-01 (a card is now a richer object with an owner and a brief) and KAN-03 (the board reads like the Trello board the developer referenced). D-14 through D-19.
Output: an extended board data load, per-column create triggers, an extended create Dialog, and the Descrição/Responsável sections of the card detail Dialog.
</objective>

<phase_goal>
**As a** PM, **I want to** start a card in whichever column the work is really in and say who owns it and what it is about, **so that** the board carries the context I currently keep in WhatsApp and my head.
</phase_goal>

<rescope_notice>
This is the last of the three plans (03-07 server, 03-08 drag-and-drop, 03-09 rich cards) that implement the 2026-07-31 mid-execution re-scope recorded in `03-CONTEXT.md` as D-12 through D-19.

Everything this plan needs on the server already exists after plan 03-07:
- `createCard` accepts `stage`, `description`, and `assigneeId`, and takes the D-15 checklist snapshot itself when the target stage is revisão interna or later. **This plan must not reimplement or duplicate that logic** — it only passes the fields.
- `updateCardDetails({ cardId, description, assigneeId })` exists and maps the database's membership-trigger rejection to the Portuguese string `Este PM não está atribuído a este cliente.`
- `listClientPmRoster(clientId)` exists in `lib/actions/clients.ts` and already refuses to leak a roster to a caller who cannot see the client.

D-17 is a hard constraint: the description is rendered ONLY in the card detail Dialog. Do not add a description preview, a truncated snippet, or a tooltip to the board face.
</rescope_notice>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-production-kanban/03-CONTEXT.md
@.planning/phases/03-content-production-kanban/03-PATTERNS.md
@.planning/phases/03-content-production-kanban/03-UI-SPEC.md
@.planning/phases/03-content-production-kanban/03-07-SUMMARY.md
@.planning/phases/03-content-production-kanban/03-08-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

Shipped by 03-07:
  public.cards.description text null
  public.cards.assignee_id uuid null references public.profiles(id) on delete set null
  trigger cards_assignee_membership_trg — rejects an assignee not in pm_clients for the card's client
  lib/validation/cards.ts -> createCardSchema { clientId, title, cardType, stage?, description?, assigneeId? },
                             updateCardDetailsSchema { cardId, description: string|null, assigneeId: string|null },
                             CARD_STAGE_VALUES
  app/pm/board/actions.ts -> createCard, advanceStage, toggleChecklistItem, moveCard, updateCardDetails
  lib/actions/clients.ts  -> listClientPmRoster(clientId): Promise<{id: string; email: string}[]>,
                             resolvePmNames(ids): Promise<Record<string,string>>

Shipped by 03-08:
  app/pm/board/draggable-card.tsx  -> DraggableCard, CardDragHandle, cardDraggableId, cardIdFromDraggableId
  app/pm/board/droppable-column.tsx-> DroppableColumn, columnDroppableId, stageFromDroppableId
  app/pm/board/board-panel.tsx     -> BoardPanel wraps the five columns in DndContext; each column renders
                                      <DroppableColumn stage>{cards.map(c => <DraggableCard><BoardCardItem/></DraggableCard>)}</DroppableColumn>
                                      Column header (STAGE_LABELS + count StatusBadge) sits OUTSIDE DroppableColumn.

Existing in board-panel.tsx since 03-02/03-03:
  CreateCardButton({ clientId }) — Dialog + react-hook-form(zodResolver(createCardSchema)), title-only,
    cardType hard-coded "single", toast.success("Card criado."), used twice (PageTitle action slot and
    the "no cards yet" EmptyState action).
  BoardCardItem({ card }) — Dialog whose body is a flex-col gap-4 stack of sibling section blocks:
    stage StatusBadge, checklist section (03-03), then DialogFooter with the Avançar Button.

UI primitives:
  components/ui/textarea.tsx -> Textarea (already installed; zero new shadcn installs this phase)
  components/ui/select.tsx   -> Select, SelectTrigger, SelectValue, SelectContent, SelectItem
  components/ui/button.tsx, input.tsx, form.tsx, dialog.tsx, error-box.tsx
  components/layout/page-shell.tsx -> SectionTitle, EmptyState
  components/ui/data-card.tsx -> DataCard({title, description?, badge?, meta?, actions?, children?, className?})

Known Radix constraint (03-01-SUMMARY.md): a Radix `SelectItem` cannot carry an empty-string `value`.
Plan 03-01 solved the identical nullable-FK case with a `"none"` sentinel mapped to null before the
Server Action call — reuse that exact approach here, do not invent a second one.
</interfaces>
</context>

<copy_contract>
03-UI-SPEC.md predates this re-scope and has no rows for these surfaces. The strings below are authoritative for this plan and extend the UI-SPEC Copywriting Contract. Existing rows (`Criar card`, `Card criado.`, `Avançar`, the empty states) are unchanged and must not be reworded.

| Element | Copy (verbatim) |
|---------|-----------------|
| Per-column create trigger `aria-label` | `Criar card em {rótulo da etapa}` |
| Create Dialog title | `Criar card` (unchanged) |
| Create Dialog description, only when a stage was preset by a column trigger | `O card será criado na etapa {rótulo da etapa}.` |
| Field label — title | `Título` (unchanged) |
| Field label — description | `Descrição` |
| Field placeholder — description | `Opcional — contexto, briefing rápido, referências.` |
| Field label — assignee | `Responsável` |
| Assignee Select placeholder | `Sem responsável` |
| Assignee "unassigned" option label | `Sem responsável` |
| Assignee help text when the client has no other assigned PMs | `Nenhum PM atribuído a este cliente.` |
| Detail Dialog section heading — description | `Descrição` |
| Detail Dialog section heading — assignee | `Responsável` |
| Detail Dialog — empty description | `Sem descrição.` |
| Detail Dialog — save button | `Salvar alterações` / while pending: `Salvando...` |
| Success toast after saving card details | `Card atualizado.` |
| Board card `meta` line, when the card has an assignee | `Criado em {data} · Responsável: {email}` |
| Board card `meta` line, when it has none | `Criado em {data}` (unchanged from 03-02) |
| Error — assignee not on the client's team | Returned verbatim by the server (`Este PM não está atribuído a este cliente.`) and rendered in `ErrorBox`. Never retyped locally. |

`{rótulo da etapa}` always comes from `STAGE_LABELS[stage]`.
</copy_contract>

<tasks>

<task type="auto">
  <name>Task 1: Extend the board data load and add the per-column create triggers with the richer create form</name>
  <files>app/pm/board/page.tsx, app/pm/board/board-panel.tsx</files>
  <read_first>
    - app/pm/board/page.tsx (the current RSC: the cards select list, the batched checklist read from 03-03, the exported BoardCard/BoardColumn/BoardClient types, and the resolvePmNames call)
    - app/pm/board/board-panel.tsx (the current CreateCardButton and the column-header markup added in 03-08 — the "+" goes in that header)
    - lib/validation/cards.ts (createCardSchema's new optional fields and CARD_STAGE_VALUES)
    - lib/actions/clients.ts (listClientPmRoster's signature and its empty-array behaviour)
    - app/admin/checklist-templates/client-assignment.tsx (plan 03-01's `"none"` sentinel pattern for a nullable-FK Radix Select — copy this approach exactly)
    - components/ui/textarea.tsx (the Textarea props/classes)
    - lib/cards/stages.ts (STAGE_ORDER, STAGE_LABELS)
    - .planning/phases/03-content-production-kanban/03-UI-SPEC.md (Board screen structure, Spacing Scale, Color — no new hue, indigo reserved for primary submit)
  </read_first>
  <action>
**A. `app/pm/board/page.tsx`.**
  - Add `description` and `assignee_id` to the `cards` select list, so it reads `select("id, title, card_type, stage, parent_card_id, description, assignee_id, created_at")`. Extend the exported `BoardCard` type with `description: string | null` and `assignee_id: string | null`.
  - When `clientId` is set, call `listClientPmRoster(clientId)` and pass the result to `BoardPanel` as `pmRoster: { id: string; email: string }[]`. When no client is selected, pass `[]`. Add this call to the existing `Promise.all` rather than awaiting it separately.
  - Feed the distinct non-null `assignee_id` values into the SAME `resolvePmNames` call that already resolves `completed_by` ids (03-03), and pass the resulting map down as `pmNames`. The roster covers the normal case; `resolvePmNames` is the fallback for a card whose assignee has since been unassigned from the client and therefore no longer appears in the roster — without it that card would render a bare uuid.
  - Do not add any additional query. Reuse the existing `cardIds.length` guard.

**B. `app/pm/board/board-panel.tsx` — generalise the create Dialog.**
Rename `CreateCardButton` to `CreateCardDialog` and give it the props `{ clientId: string | null; stage?: CardStage; pmRoster: { id: string; email: string }[]; trigger: React.ReactNode }`. The caller supplies the trigger element, so the same Dialog serves three call sites with three different affordances. Keep the existing `react-hook-form` + `zodResolver(createCardSchema)` + `useTransition` + `toast.success("Card criado.")` + `ErrorBox` structure — this is an extension, not a rewrite.
  - `defaultValues` become `{ clientId: clientId ?? "", title: "", cardType: "single", stage: stage ?? "briefing", description: "" }`. `assigneeId` is deliberately NOT a form field (see below).
  - `handleOpenChange` resets to those same values, preserving the current behaviour of clearing state on each open.
  - Below the existing `Título` `Input`, add a `Descrição` `FormField` rendering a `Textarea` with `rows={4}` and the placeholder from the copy contract. It is optional — no `min(1)`, no required marker.
  - Below that, add the `Responsável` control. Because a Radix `SelectItem` cannot hold an empty-string value (03-01-SUMMARY.md), hold the selection in local state — `const NONE_VALUE = "none"; const [assigneeValue, setAssigneeValue] = useState(NONE_VALUE);` — rather than as a react-hook-form field, and map it at submit time: `const assigneeId = assigneeValue === NONE_VALUE ? undefined : assigneeValue;`. Options: one `SelectItem value={NONE_VALUE}` labelled `Sem responsável`, then one per `pmRoster` entry with `value={pm.id}` and the pm's email as its label. Reset `assigneeValue` to `NONE_VALUE` in `handleOpenChange`.
  - When `pmRoster.length === 0`, still render the Select (with only the `Sem responsável` option) and render `Nenhum PM atribuído a este cliente.` beneath it in `text-meta text-muted-foreground`. Never render a `Select` with zero `SelectItem` children.
  - When the `stage` prop is provided AND is not `"briefing"`, render a `DialogDescription` reading `O card será criado na etapa {STAGE_LABELS[stage]}.` so the PM is never surprised about where the card lands (D-14 allows any column, including gated ones).
  - `onSubmit` calls `createCard({ ...values, stage: stage ?? "briefing", assigneeId })`. The server owns the D-15 snapshot; do not add any client-side checklist handling here.

**C. `app/pm/board/board-panel.tsx` — the three call sites.**
  1. `PageTitle action={...}` — `<CreateCardDialog clientId={activeClientId} pmRoster={pmRoster} trigger={<Button type="button" disabled={!clientId}><PlusIcon className="size-4" />Criar card</Button>} />`, with no `stage` prop so it keeps creating in Briefing (D-14 explicitly keeps this button as-is).
  2. The "Nenhum card ainda" `EmptyState` action — same, unchanged in behaviour.
  3. **New, per column (D-14):** inside each column's header row — the one added in 03-08 that holds `STAGE_LABELS[column.stage]` and the count `StatusBadge`, and which sits OUTSIDE `DroppableColumn` — add a third element: `<CreateCardDialog clientId={activeClientId} stage={column.stage} pmRoster={pmRoster} trigger={<Button type="button" variant="ghost" size="icon" className="size-8" aria-label={`Criar card em ${STAGE_LABELS[column.stage]}`} disabled={!activeClientId}><PlusIcon className="size-4" /></Button>} />`. The trigger is icon-only, so the `aria-label` is mandatory (same rule the UI-SPEC already states for the remove-attachment trigger). Keep the header a single `flex items-center justify-between gap-2` row: label on the left, count badge and "+" grouped on the right.

Nothing in this task touches `DndContext`, `DroppableColumn`, `DraggableCard`, `BoardCardItem`'s Dialog body, or `app/pm/board/actions.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit`, `npm run lint`, and `npm run build` all exit 0.
    - `app/pm/board/page.tsx` contains `listClientPmRoster`, `assignee_id`, and `description`, and `grep -c 'resolvePmNames' app/pm/board/page.tsx` returns 1 (a single combined call, not two).
    - `app/pm/board/board-panel.tsx` exports/defines `CreateCardDialog` and `grep -c 'CreateCardDialog' app/pm/board/board-panel.tsx` returns at least 4 (the definition plus three call sites).
    - `app/pm/board/board-panel.tsx` contains the literal strings `Criar card em `, `O card será criado na etapa `, `Sem responsável`, `Nenhum PM atribuído a este cliente.`, and `Opcional — contexto, briefing rápido, referências.`
    - `grep -c 'Textarea' app/pm/board/board-panel.tsx` returns at least 2 (the import and the usage).
    - `grep -cE '"Briefing"|"Produção"|"Revisão interna"|"Aprovação do cliente"|"Agendamento"' app/pm/board/board-panel.tsx` returns 0 — every stage label, including in the new aria-labels and dialog description, comes from `STAGE_LABELS`.
    - `git diff --stat app/pm/board/actions.ts app/pm/board/draggable-card.tsx app/pm/board/droppable-column.tsx` is empty for this task.
    - No file touched by this task contains an inline `text-[` arbitrary font-size class.
  </acceptance_criteria>
  <done>Every column has its own "+" that creates a card directly in that stage, and the create form carries an optional description and an assignee restricted to the client's own PMs.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Descrição and Responsável sections to the card detail Dialog and the assignee line to the board card</name>
  <files>app/pm/board/board-panel.tsx</files>
  <read_first>
    - app/pm/board/board-panel.tsx (BoardCardItem's Dialog body as plan 03-03 left it: a flex-col gap-4 stack of sibling section blocks — stage badge, checklist section, DialogFooter with Avançar)
    - app/pm/board/actions.ts (the updateCardDetails signature and its exact return shape)
    - lib/validation/cards.ts (updateCardDetailsSchema — description and assigneeId are both nullable, not optional)
    - app/admin/checklist-templates/client-assignment.tsx (the `"none"` sentinel pattern, reused again here)
    - components/ui/textarea.tsx, components/ui/error-box.tsx
    - components/layout/page-shell.tsx (SectionTitle)
    - .planning/phases/03-content-production-kanban/03-UI-SPEC.md (Card detail Dialog ordering and typography tokens)
  </read_first>
  <action>
**A. `BoardCardItem` props.** It now needs `pmRoster` and `pmNames` in addition to `card`. Thread both down from `BoardPanel` through the existing `DraggableCard` wrapper (the wrapper takes `children`, so no change to `draggable-card.tsx` is needed).

**B. Board face (D-17 boundary).** On the `DataCard`, set `meta` to `Criado em {data}` when `card.assignee_id` is null, and to `Criado em {data} · Responsável: {nome}` when it is set, resolving the name from `pmNames[card.assignee_id]` and falling back to the raw id only if unresolvable. **The description is NOT rendered here in any form** — no `description` prop on `DataCard`, no truncation, no title attribute. D-17 locks the board face to title + badges + meta.

**C. Card detail Dialog — two new sibling sections.** Insert them into the existing flex-col stack in this order, BETWEEN the stage `StatusBadge` block and the checklist section:
  1. `SectionTitle` "Descrição", then a `Textarea` with `rows={5}` bound to local state seeded from `card.description ?? ""`. When the card has no description and the Textarea has not been touched, the placeholder is `Sem descrição.` — the Textarea is always editable, there is no separate read-mode/edit-mode toggle (D-18: editable later, same as the title-only flow was).
  2. `SectionTitle` "Responsável", then a `Select` using the same `NONE_VALUE = "none"` sentinel as Task 1, seeded from `card.assignee_id ?? NONE_VALUE`, with options `Sem responsável` plus one per `pmRoster` entry. When `pmRoster.length === 0`, render the Select with only the `Sem responsável` option plus `Nenhum PM atribuído a este cliente.` in `text-meta text-muted-foreground` beneath.
     Edge case that must be handled: if `card.assignee_id` is set but is NOT present in `pmRoster` (the PM was unassigned from the client after the card was assigned), append one extra `SelectItem` for that id, labelled with `pmNames[card.assignee_id]` — otherwise Radix renders an empty trigger and the PM silently loses the current value on the next save.
  3. A single `Salvar alterações` `Button` directly under the Responsável Select, covering BOTH fields in one call. `onClick` runs inside `useTransition` and calls
     `updateCardDetails({ cardId: card.id, description: draftDescription.trim().length > 0 ? draftDescription : null, assigneeId: draftAssignee === NONE_VALUE ? null : draftAssignee })`.
     On success `toast.success("Card atualizado.")`; on error render `result.error` verbatim in an `ErrorBox` inside this section — never paraphrase, because the assignee-membership rejection message comes from the server. While pending the button reads `Salvando...` and is disabled.
     Disable the button when neither draft differs from the card's current values, so the PM gets a clear "nothing to save" affordance.

**D. Do not change** the checklist section, the `Avançar` Button, its disabled logic, the gate message rendering, or anything in `DndContext`. This task adds two sibling blocks and one `meta` string; it restructures nothing.

Styling constraints from 03-UI-SPEC.md: only the `text-display`/`text-title`/`text-body`/`text-meta` tokens, `SectionTitle` for both new headings, indigo `--primary` on the `Salvar alterações` button (it is a primary submit), no destructive tone anywhere in this task, no new shadcn component installed.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit`, `npm run lint`, `npm run build`, and `npm test` all exit 0.
    - `app/pm/board/board-panel.tsx` contains the literal strings `Descrição`, `Responsável`, `Sem descrição.`, `Salvar alterações`, `Salvando...`, `Card atualizado.`, and `Responsável: `.
    - `grep -c 'updateCardDetails' app/pm/board/board-panel.tsx` returns at least 1.
    - `grep -c 'Este PM não está atribuído' app/pm/board/board-panel.tsx` returns 0 — the membership error reaches the UI only as `result.error` from the server.
    - The board face does not render the description: `grep -c 'card.description' app/pm/board/board-panel.tsx` returns at most 2, and neither occurrence is inside a `<DataCard` prop list (the two permitted uses are seeding the detail-dialog draft state and the has-changed comparison).
    - `grep -c 'description={' app/pm/board/board-panel.tsx` returns 0 — `DataCard`'s `description` slot is never used on a board card (D-17).
    - `git diff --stat app/pm/board/actions.ts app/pm/board/page.tsx app/pm/board/draggable-card.tsx app/pm/board/droppable-column.tsx` is empty for this task.
    - No file touched by this task contains an inline `text-[` arbitrary font-size class.
  </acceptance_criteria>
  <done>A card's description and assignee are visible and editable inside its detail dialog, the assignee also shows on the board card's meta line, and the description never appears on the board face.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — per-column creation, snapshot-on-create, description, and assignee</name>
  <files>(no file changes — verification only)</files>
  <read_first>.planning/phases/03-content-production-kanban/03-CONTEXT.md (D-14 through D-19)</read_first>
  <action>Pause and hand the running app to the developer. Present the verification steps below verbatim and wait for the resume signal. Do not self-approve, and do not proceed to the summary until the developer responds.</action>
  <what-built>A "+" trigger on every stage column that creates a card directly in that stage (with its checklist snapshot taken immediately when the stage is revisão interna or later), plus an optional plain-text description and an optional single assignee — both editable from the card detail dialog, with the assignee picker restricted to that client's own PMs.</what-built>
  <how-to-verify>
    1. `npm run dev`, log in as a PM, open `/pm/board`, and select a client that has a checklist template assigned and at least two PMs assigned to it (assign a second PM from `/admin/clients` as an Admin first if needed — otherwise step 7 has nothing to pick).
    2. Confirm each of the five column headers now shows a small "+" button on the right, next to the card count.
    3. Click the "+" on the **Produção** column. Confirm the dialog says "O card será criado na etapa Produção.", create a card titled "Teste coluna". Confirm it appears in **Produção**, not in Briefing.
    4. Confirm the top-level "Criar card" button still creates in **Briefing** (D-14 keeps both).
    5. Click the "+" on the **Revisão interna** column and create a card titled "Teste gate direto". Open it. Confirm its checklist is ALREADY listed with the client's template items, all unchecked, and that "Avançar" is disabled with the blocked reason shown — the card was born gated (D-15).
    6. Create a card from any column with a multi-line description filled in. Confirm the board card shows ONLY the title, badges, and the "Criado em ..." meta line — **no description text anywhere on the board face** (D-17). Open the card and confirm the description is there in full, with its line breaks preserved.
    7. In an existing card's dialog, open the "Responsável" select. Confirm it lists "Sem responsável" plus ONLY the PMs assigned to this client — a PM assigned to a different client must NOT appear (D-19). Pick one and click "Salvar alterações". Confirm the toast reads "Card atualizado." and the board card's meta line now ends with "· Responsável: {that PM's email}".
    8. Reopen the card, change the description text, and save. Confirm the change persists after a full page reload.
    9. Set the assignee back to "Sem responsável" and save. Confirm the meta line loses the "· Responsável: ..." part and the card is unassigned after a reload (D-19: optional, can be picked up later).
    10. Confirm "Salvar alterações" is disabled when you have changed nothing, and becomes enabled as soon as you edit either field.
    11. Switch to a different client in the client switcher. Confirm the "Responsável" options change to that client's PMs — no cross-client bleed.
    12. Confirm drag-and-drop (from plan 03-08) still works and that the "Avançar" button still works — the new fields changed neither.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what went wrong</resume-signal>
  <verify>
    <human-check>Developer confirms every numbered step above behaves as described, especially steps 5 (snapshot on direct creation), 6 (description never on the board face), and 7/11 (assignee scoped to the client's own PMs).</human-check>
  </verify>
  <acceptance_criteria>
    - The developer explicitly replied "approved" (or the equivalent) after walking every numbered step.
    - Any issue raised was fixed and re-verified before this task was marked done.
  </acceptance_criteria>
  <done>Developer signed off on the walkthrough.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| create Dialog → `createCard` | `stage`, `description`, and `assigneeId` all originate in the browser; `stage` can name a gated column |
| detail Dialog → `updateCardDetails` | `assigneeId` names an arbitrary `profiles` row |
| RSC → browser | `pmRoster` (id + email of a client's PMs) is serialised into the page payload |
| stored description → other users' browsers | A description written by one PM is rendered to another PM and, in plan 03-05, to an Admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-50 | Elevation of Privilege | Editing the client-side roster (or calling `updateCardDetails` directly) to assign a card to a PM outside the client's team | mitigate | The picker is a convenience only. `cards_assignee_membership_trg` (migration 0017, `security definer`, fires on INSERT and UPDATE) is the boundary and is proved by pgTAP assertions 3 and 4 in plan 03-07; the action surfaces its rejection as `Este PM não está atribuído a este cliente.` |
| T-03-51 | Repudiation | Creating a card directly in a gated column (D-14) as a way to reach aprovação do cliente with no checklist and no audit trail | mitigate | `createCard` takes the D-15 snapshot in the same request whenever the target stage is revisão interna or later, and deletes the card if the snapshot fails (plan 03-07, T-03-39). This plan only supplies the `stage` value; it adds no path around that logic |
| T-03-52 | Information Disclosure | `pmRoster` leaking the email addresses of PMs on clients the viewer is not assigned to | mitigate | `listClientPmRoster` performs an RLS-scoped `clients` visibility check before its privileged read and returns `[]` on failure (plan 03-07, T-03-41); this plan calls it with exactly one `clientId` — the client already resolved from the URL and already RLS-filtered by the cards query |
| T-03-53 | Cross-Site Scripting | A description containing markup being rendered to another PM or to the Admin audit view | mitigate | The description is rendered as a React text child and inside a `Textarea` `value` — never via `dangerouslySetInnerHTML`, and D-16 explicitly rules out markdown/rich text, so no renderer is introduced. React escapes it by construction |
| T-03-54 | Denial of Service | An enormous description inflating the board's RSC payload for every card | mitigate | `createCardSchema`/`updateCardDetailsSchema` bound it at 5000 characters, and D-17 keeps it off the board face — but note the RSC still ships it per card, which is acceptable at this project's documented small scale (PROJECT.md) and is the reason no separate detail fetch is introduced |
| T-03-55 | Tampering | An assignee that was later unassigned from the client silently being wiped on the next save because the Select could not represent it | mitigate | Task 2 appends an explicit `SelectItem` for a current `assignee_id` missing from the roster, labelled from `pmNames`, so the existing value round-trips instead of collapsing to the sentinel |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed in this plan — `@dnd-kit/*` was installed and audited in plan 03-08. If a task turns out to need one, stop and run the Package Legitimacy Gate protocol first |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm run build`, and `npm test` all pass.
- `npx supabase test db` — no regression across `0001`–`0009` (this plan adds no migration).
- `git diff --stat app/pm/board/actions.ts components/ui/data-card.tsx supabase/` is empty across the whole plan.
- Human checkpoint above signed off, including step 5 (D-15 snapshot on direct creation), step 6 (D-17 board face), and steps 7/11 (D-19 client-scoped roster).
</verification>

<success_criteria>
- Each of the five columns has a working "+" that creates a card in that stage, and the top-level button still creates in Briefing (D-14).
- A card created in revisão interna or later opens with its checklist already present and its gate already active (D-15).
- A card carries an optional multi-line plain-text description, visible only in the card detail dialog (D-16, D-17, D-18).
- A card carries an optional single assignee, pickable only from that client's assigned PMs, settable and clearable (D-19).
- Drag-and-drop and the Avançar button are both unchanged and still work.
</success_criteria>

<output>
Create `.planning/phases/03-content-production-kanban/03-09-SUMMARY.md` when done.
</output>
