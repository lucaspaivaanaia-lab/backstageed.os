---
phase: 03-content-production-kanban
plan: 07
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified:
  - supabase/migrations/0017_cards_description_assignee.sql
  - supabase/tests/0009_cards_assignee_membership_test.sql
  - lib/cards/move-rules.ts
  - lib/cards/move-rules.test.ts
  - lib/validation/cards.ts
  - lib/actions/clients.ts
  - app/pm/board/actions.ts
autonomous: true
requirements: [KAN-01, KAN-02, CHK-03]

must_haves:
  truths:
    - "A card can be moved to any stage column by a single server call that decides the move on its own, without the caller telling it what the current stage is (D-12 server half)"
    - "Moving a card out of revisão interna while any checklist item is unchecked is rejected with the exact same message the Avançar button already shows (D-13)"
    - "A forward move that would jump over revisão interna is rejected — drag can never be used to skip the review step that CHK-03 exists to enforce"
    - "Moving a card backwards out of revisão interna is allowed and is never gated — going back does not skip a review"
    - "A card created directly in revisão interna or a later stage receives its checklist snapshot in the same request, and if the snapshot fails the card is not left behind (D-15)"
    - "A card carries an optional plain-text description and an optional single assignee (D-16, D-18, D-19)"
    - "Assigning a card to a PM who is not in that client's pm_clients rows is rejected at the database layer, not only in the UI (D-19)"
  artifacts:
    - path: "supabase/migrations/0017_cards_description_assignee.sql"
      provides: "cards.description + cards.assignee_id, and the security-definer trigger enforcing D-19's pm_clients membership rule"
      contains: "assignee_not_assigned_to_client"
    - path: "supabase/tests/0009_cards_assignee_membership_test.sql"
      provides: "pgTAP proof that an assignee outside the client's pm_clients rows is rejected on INSERT and on UPDATE"
      contains: "throws_like"
    - path: "lib/cards/move-rules.ts"
      provides: "pure move-legality predicate shared by the drag handler (03-08) and the moveCard Server Action"
      exports: ["evaluateMove", "MOVE_SKIPS_REVIEW_MESSAGE"]
    - path: "lib/cards/move-rules.test.ts"
      provides: "unit coverage of every move branch including the skip-over-revisão-interna case"
      min_lines: 40
    - path: "app/pm/board/actions.ts"
      provides: "moveCard + updateCardDetails, and createCard extended with stage/description/assignee and the D-15 snapshot-on-create"
      exports: ["createCard", "advanceStage", "toggleChecklistItem", "moveCard", "updateCardDetails"]
  key_links:
    - from: "app/pm/board/actions.ts"
      to: "lib/cards/move-rules.ts"
      via: "evaluateMove re-run server-side over a server-side re-read of card_checklist_items"
      pattern: "evaluateMove"
    - from: "app/pm/board/actions.ts"
      to: "lib/cards/checklist-snapshot.ts"
      via: "moveCard and createCard both call snapshotChecklistForCard — no second copy of the snapshot logic"
      pattern: "snapshotChecklistForCard"
    - from: "supabase/migrations/0017_cards_description_assignee.sql"
      to: "public.pm_clients"
      via: "security-definer trigger checking assignee membership on insert and update of public.cards"
      pattern: "pm_clients"
    - from: "lib/actions/clients.ts"
      to: "public.pm_clients"
      via: "listClientPmRoster — privileged display-only read of a client's assigned PMs, gated by an RLS-scoped visibility check first"
      pattern: "listClientPmRoster"
---

<objective>
Build the entire server-side half of the 2026-07-31 re-scope (03-CONTEXT.md D-12 through D-19) before any UI touches it: a `moveCard` action that can place a card in any column while re-enforcing the checklist gate identically to the button path (D-13), the `cards.description` and `cards.assignee_id` columns with database-level enforcement of D-19's team-membership rule, a `createCard` that can start a card in any column and snapshot its checklist immediately (D-14/D-15), and `updateCardDetails` for editing the two new fields.

Purpose: KAN-02 (the flow is now reachable by drag as well as by button), CHK-03 (the gate must hold on every path), and KAN-01 (a card is now a richer object). D-12/D-13/D-14/D-15/D-16/D-18/D-19.
Output: migration 0017 applied live with its pgTAP proof, a unit-tested pure move-rules module, three new/extended Server Actions, and the privileged roster helper the assignee picker will consume.
</objective>

<phase_goal>
**As a** PM, **I want to** move a card to whichever column reflects reality and record who owns it and what it is about, **so that** the board matches how I actually work — without ever letting a card slip past the review gate.
</phase_goal>

<rescope_notice>
This plan exists because Phase 3 was re-scoped mid-execution on 2026-07-31, after plans 03-01 and 03-02 had already shipped and been human-verified. `03-CONTEXT.md` records the reversal: **D-05 (Avançar button only, no drag-and-drop) is superseded by D-12/D-13**, and D-14 through D-19 add per-column creation, a description field, and an assignee field.

This plan is the SERVER half only. It ships no UI. Plan 03-08 wires drag-and-drop into the board, and plan 03-09 builds the per-column "+" trigger and the description/assignee form controls. Splitting server from UI keeps each plan inside its context budget and lets the pgTAP/`node --test` gates prove the rules before any pixel depends on them.

**Migration renumbering notice:** the original wave plan assigned `0017` to `card_attachments` (plan 03-04) and `0018` to `card_checklist_overrides` (plan 03-05). Because this plan now executes at wave 4 — ahead of both — those numbers shifted: this plan owns `0017`, plan 03-04 owns `0018`, plan 03-05 owns `0019`. Supabase applies migrations in filename order and rejects an out-of-order push against remote history, so the numbers must follow the execution order, not the plan numbers. The same shift applies to the pgTAP files: this plan owns `0009`, 03-04 owns `0010`, 03-05 owns `0011`.
</rescope_notice>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-production-kanban/03-CONTEXT.md
@.planning/phases/03-content-production-kanban/03-RESEARCH.md
@.planning/phases/03-content-production-kanban/03-PATTERNS.md
@.planning/phases/03-content-production-kanban/03-02-SUMMARY.md
@.planning/phases/03-content-production-kanban/03-03-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

Shipped by plan 03-02 (live on main):
  public.cards (id, client_id, parent_card_id, card_type, title, stage, created_by, created_at, updated_at)
    constraint cards_package_has_no_stage  -- package => stage is null; single/piece => stage is not null
    constraint cards_piece_requires_parent
    policies cards_select_scoped / cards_insert_scoped / cards_update_scoped (flat client_id checks)
    grant select, insert, update on public.cards to authenticated
  public.card_stage enum: 'briefing' | 'producao' | 'revisao_interna' | 'aprovacao_cliente' | 'agendamento'
  lib/cards/stages.ts -> CardStage, STAGE_ORDER, STAGE_LABELS, nextStage
  lib/validation/cards.ts -> createCardSchema { clientId, title, cardType }, advanceStageSchema { cardId }
  app/pm/board/actions.ts -> createCard, advanceStage

Shipped by plan 03-03:
  public.card_checklist_items (id, card_id, label, sort_order, completed_at, completed_by, created_at)
  lib/cards/checklist-gate.ts   -> ChecklistItemState, checklistProgress, isGateBlocked, GATE_BLOCKED_MESSAGE
  lib/cards/checklist-snapshot.ts -> snapshotChecklistForCard(supabase, cardId, clientId), SNAPSHOT_FAILED_MESSAGE
  lib/validation/cards.ts       -> + toggleChecklistItemSchema
  app/pm/board/actions.ts       -> + toggleChecklistItem   (3x revalidatePath("/pm/board") at the start of this plan)

Shipped by Phase 5 / Phase 1:
  public.profiles (id, email, role, status, must_change_password, client_id, created_at)
    policy profiles_select_own_or_admin — a PM can read ONLY their own profile row
  public.pm_clients (pm_id, client_id) primary key (pm_id, client_id)
    policy pm_clients_select_own_or_admin — a PM can read ONLY rows where pm_id = auth.uid()
    grant select, insert, delete on public.pm_clients to authenticated  (no update grant)
  public.is_admin() / public.pm_assigned_clients() — both `security definer` with `set search_path = ''`
  lib/actions/clients.ts ("use server") -> createClientRecord, listPmRoster, resolvePmNames, updateBriefing, assignPms
    listPmRoster / resolvePmNames both use createAdminClient() as privileged DISPLAY-ONLY reads,
    documented as closing the profiles_select_own_or_admin / pm_clients_select_own_or_admin RLS gaps.

pgTAP fixture uuids (supabase/tests/rls_helpers.sql):
  client_a      = 11111111-1111-1111-1111-111111111111
  client_b      = 22222222-2222-2222-2222-222222222222
  pm_a          = aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa  (role=pm, approved, assigned to client_a ONLY)
  client_a_user = bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb  (role=client)
  admin_user    = cccccccc-cccc-cccc-cccc-cccccccccccc  (role=admin, approved, NOT in pm_clients for any client)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship the description/assignee columns with their membership trigger and pgTAP proof, plus the pure move-rules module</name>
  <files>supabase/migrations/0017_cards_description_assignee.sql, supabase/tests/0009_cards_assignee_membership_test.sql, lib/cards/move-rules.ts, lib/cards/move-rules.test.ts</files>
  <read_first>
    - supabase/migrations/0015_cards.sql (the table being altered — its constraints, policies, and header-comment convention)
    - supabase/migrations/0006_clients_full_record.sql (the ALTER TABLE convention, including the "RLS is already enabled, do not re-enable on ALTER" comment)
    - supabase/migrations/0004_rls_policies.sql lines 18-46 (the exact `security definer` + `set search_path = ''` function convention this codebase uses — note every reference inside such a function must be schema-qualified because the search path is empty)
    - supabase/migrations/0003_pm_clients.sql (the pm_clients shape the trigger checks against)
    - supabase/tests/0005_rls_client_files_scoping_test.sql (pgTAP skeleton)
    - supabase/tests/rls_helpers.sql (fixture uuids and the tests.set_auth helper)
    - lib/cards/stages.ts (CardStage, STAGE_ORDER)
    - lib/cards/checklist-gate.ts (ChecklistItemState, isGateBlocked, GATE_BLOCKED_MESSAGE — move-rules imports these, it does not reimplement them)
    - lib/chat/stale-response-guard.ts + lib/chat/stale-response-guard.test.ts (pure-module + node:test convention)
  </read_first>
  <behavior>
    Let `C = { completed_at: "2026-07-31T00:00:00Z" }` (a checked item) and `U = { completed_at: null }` (an unchecked item).
    - `evaluateMove("briefing", "briefing", [])` returns `{ allowed: true }` — a no-op move is always allowed.
    - `evaluateMove("briefing", "producao", [])` returns `{ allowed: true }`.
    - `evaluateMove("producao", "revisao_interna", [])` returns `{ allowed: true }` — landing ON revisão interna is always allowed.
    - `evaluateMove("briefing", "aprovacao_cliente", [])` returns `{ allowed: false, reason: MOVE_SKIPS_REVIEW_MESSAGE }` — a forward move may not jump over revisão interna.
    - `evaluateMove("producao", "agendamento", [C])` returns `{ allowed: false, reason: MOVE_SKIPS_REVIEW_MESSAGE }` — the skip rule does not care about checklist state.
    - `evaluateMove("revisao_interna", "aprovacao_cliente", [U])` returns `{ allowed: false, reason: GATE_BLOCKED_MESSAGE }` (D-13).
    - `evaluateMove("revisao_interna", "aprovacao_cliente", [C, U])` returns `{ allowed: false, reason: GATE_BLOCKED_MESSAGE }`.
    - `evaluateMove("revisao_interna", "aprovacao_cliente", [C, C])` returns `{ allowed: true }`.
    - `evaluateMove("revisao_interna", "aprovacao_cliente", [])` returns `{ allowed: true }` — a card with no checklist is not blocked (the Pitfall 3 fail-safe, identical to `isGateBlocked([])`).
    - `evaluateMove("revisao_interna", "agendamento", [U])` returns `{ allowed: false, reason: GATE_BLOCKED_MESSAGE }`.
    - `evaluateMove("revisao_interna", "producao", [U])` returns `{ allowed: true }` — a BACKWARD move out of revisão interna is never gated; going back does not skip a review.
    - `evaluateMove("agendamento", "briefing", [U])` returns `{ allowed: true }` — backward moves are unrestricted.
    - `MOVE_SKIPS_REVIEW_MESSAGE` equals the exact string `Um card precisa passar pela revisão interna antes de seguir para as etapas seguintes.`
  </behavior>
  <action>
Create `lib/cards/move-rules.ts` — a pure module with zero Supabase/React/Next imports, doc-comment in the style of `lib/chat/stale-response-guard.ts`, citing KAN-02/CHK-03 and decisions D-12/D-13. State explicitly that this module is imported by BOTH the drag handler in `app/pm/board/board-panel.tsx` (plan 03-08, for instant snap-back feedback) and the `moveCard` Server Action (the actual boundary), so the two can never disagree — the same shared-predicate discipline `lib/cards/checklist-gate.ts` established. Export:
  - `type MoveDecision = { allowed: true } | { allowed: false; reason: string }`
  - `const MOVE_SKIPS_REVIEW_MESSAGE = "Um card precisa passar pela revisão interna antes de seguir para as etapas seguintes."`
  - `function evaluateMove(from: CardStage, to: CardStage, items: ChecklistItemState[]): MoveDecision`, implementing exactly, in this order:
      1. `if (from === to) return { allowed: true }`.
      2. `const fromIdx = STAGE_ORDER.indexOf(from)`, `const toIdx = STAGE_ORDER.indexOf(to)`, `const revIdx = STAGE_ORDER.indexOf("revisao_interna")`.
      3. `if (fromIdx < revIdx && toIdx > revIdx) return { allowed: false, reason: MOVE_SKIPS_REVIEW_MESSAGE }` — this is the rule that stops drag-and-drop from becoming a one-gesture bypass of the entire CHK-03 gate. Comment it as such.
      4. `if (fromIdx === revIdx && toIdx > revIdx && isGateBlocked(items)) return { allowed: false, reason: GATE_BLOCKED_MESSAGE }` — D-13: the gate applies identically regardless of trigger, and it reuses the imported constant so the drag path shows the byte-identical message the Avançar button shows.
      5. `return { allowed: true }`.
  Import `CardStage`/`STAGE_ORDER` from `./stages` and `ChecklistItemState`/`isGateBlocked`/`GATE_BLOCKED_MESSAGE` from `./checklist-gate`. Never inline an ordered stage array and never retype the gate message.

Create `lib/cards/move-rules.test.ts` with `node:test` + `node:assert/strict` covering all thirteen behaviors above. The `lib/cards/*.test.ts` glob is already in `package.json`'s test script — do not modify `package.json`.

Create `supabase/migrations/0017_cards_description_assignee.sql`, opening with a header comment naming Phase 3, requirements KAN-01/KAN-02, decisions D-16/D-17/D-18 (optional plain-text description, detail-view only, no markdown) and D-19 (single optional assignee scoped to the client's assigned PMs), the note that RLS is already enabled on `public.cards` and an `ALTER TABLE` must NOT re-enable it (per `0006_clients_full_record.sql`'s precedent), and the note that no new GRANT is needed because `grant select, insert, update on public.cards to authenticated` from 0015 already covers new columns on the same table.

Body:
```
alter table public.cards
  add column description text,
  add column assignee_id uuid references public.profiles(id) on delete set null;

create index idx_cards_assignee_id on public.cards (assignee_id);
```
`on delete set null` is deliberate: deleting a PM profile must orphan the assignment, never cascade-delete the card. Comment it.

Then the D-19 enforcement. A plain CHECK constraint cannot be used because the rule needs a subquery, and a trigger function running as the invoking user cannot see another PM's `pm_clients` rows (`pm_clients_select_own_or_admin` restricts a PM to `pm_id = auth.uid()`), which would make every legitimate cross-PM assignment fail. So write a single `security definer` trigger function with the membership check inlined — do NOT create a separate callable helper function, because a standalone `is this PM on this client` function would be an authenticated-callable membership oracle:
```
create or replace function public.enforce_card_assignee_membership()
returns trigger
language plpgsql
security definer
set search_path = ''
as $$
begin
  if new.assignee_id is not null
     and not exists (
       select 1 from public.pm_clients
       where client_id = new.client_id and pm_id = new.assignee_id
     ) then
    raise exception 'assignee_not_assigned_to_client'
      using errcode = 'check_violation';
  end if;
  return new;
end;
$$;

create trigger cards_assignee_membership_trg
before insert or update on public.cards
for each row execute function public.enforce_card_assignee_membership();
```
Every reference inside the function is schema-qualified because `search_path` is empty — that is the exact convention `is_admin()`/`pm_assigned_clients()` follow in `0004_rls_policies.sql`. The literal token `assignee_not_assigned_to_client` is the contract the Server Actions match on to produce a Portuguese message; state that in a comment so nobody reworks the exception text.

Create `supabase/tests/0009_cards_assignee_membership_test.sql` on the `0005` skeleton, `select plan(4)`. Include `\ir rls_helpers.sql`, then `select tests.set_auth('aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa')` (pm_a, assigned to client_a only) and assert:
  1. `lives_ok` — inserting a `single` card for `client_a` with `stage = 'briefing'`, `created_by = pm_a`, `assignee_id = pm_a` succeeds (pm_a IS in `pm_clients` for client_a).
  2. `lives_ok` — inserting the same shape with `assignee_id = null` succeeds (D-19: the assignee is optional).
  3. `throws_like` — inserting the same shape with `assignee_id = 'cccccccc-cccc-cccc-cccc-cccccccccccc'` (admin_user, a real profile that is NOT in `pm_clients` for client_a) matching `'%assignee_not_assigned_to_client%'`.
  4. `throws_like` — `update public.cards set assignee_id = 'cccccccc-cccc-cccc-cccc-cccccccccccc'` on the card created in assertion 2, matching `'%assignee_not_assigned_to_client%'` — the trigger fires on UPDATE as well as INSERT, so an assignment cannot be smuggled in after creation.
Give each inserted card a distinct fixed uuid literal so assertion 4 can target the right row. End with `reset role; select set_config('request.jwt.claims', '', true); select * from finish(); rollback;`.

This task is BLOCKING on the schema push: export `SUPABASE_ACCESS_TOKEN` from `.env.local`, run `npx supabase db push` to apply 0017 to the linked hosted project, then `npx supabase db reset` locally (the local Docker instance only picks up new migrations on reset — the exact issue 03-02-SUMMARY.md logged) followed by `npx supabase test db`. A green `tsc`/`next build` does not prove the schema is live. If the push hits an unsuppressable interactive prompt, stop and report it.
  </action>
  <verify>
    <automated>npm test &amp;&amp; npx supabase migration list --linked 2>&amp;1 | grep -q 0017 &amp;&amp; grep -v '^--' supabase/migrations/0017_cards_description_assignee.sql | grep -q 'assignee_not_assigned_to_client' &amp;&amp; test "$(grep -v '^--' supabase/migrations/0017_cards_description_assignee.sql | grep -c 'enable row level security')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 and its output includes the thirteen `lib/cards/move-rules.test.ts` cases, including both `MOVE_SKIPS_REVIEW_MESSAGE` cases and the backward-move-out-of-revisão-interna case.
    - `npx supabase migration list --linked` lists `0017` in the Remote column.
    - After stripping comments, `0017_cards_description_assignee.sql` contains `add column description text`, `add column assignee_id uuid references public.profiles(id) on delete set null`, `security definer`, `set search_path = ''`, `create trigger cards_assignee_membership_trg`, and the literal `assignee_not_assigned_to_client`.
    - After stripping comments, `0017_cards_description_assignee.sql` contains ZERO occurrences of `enable row level security` (RLS is already on `public.cards`; re-enabling on an ALTER is the wrong pattern) and ZERO occurrences of `create policy` (0015's three policies already cover the new columns).
    - `npx supabase test db` produces zero `not ok` lines from `0009_cards_assignee_membership_test.sql` — specifically assertions 3 and 4 must both raise — and zero regressions in `0001`–`0008`.
    - `lib/cards/move-rules.ts` imports `isGateBlocked` and `GATE_BLOCKED_MESSAGE` from `./checklist-gate`: `grep -c "from \"./checklist-gate\"" lib/cards/move-rules.ts` returns 1, and `grep -c 'Existem itens do checklist' lib/cards/move-rules.ts` returns 0.
    - `grep -cE "from \"@/lib/supabase|from \"react|from \"next" lib/cards/move-rules.ts` returns 0 — the module stays importable by a Client Component.
  </acceptance_criteria>
  <done>The two new card columns are live with a database-level guarantee that an assignee is always one of that client's PMs, and move legality is a unit-tested pure function.</done>
</task>

<task type="auto">
  <name>Task 2: Build the moveCard Server Action</name>
  <files>lib/validation/cards.ts, app/pm/board/actions.ts</files>
  <read_first>
    - app/pm/board/actions.ts (current createCard / advanceStage / toggleChecklistItem — this task appends to the file, it does not rewrite it)
    - lib/cards/move-rules.ts (just written — evaluateMove, MOVE_SKIPS_REVIEW_MESSAGE)
    - lib/cards/checklist-gate.ts (ChecklistItemState, GATE_BLOCKED_MESSAGE)
    - lib/cards/checklist-snapshot.ts (snapshotChecklistForCard signature and its idempotence guarantee)
    - lib/cards/stages.ts (CardStage, STAGE_ORDER)
    - lib/validation/cards.ts (current schemas — this task appends one)
    - app/pm/chat/actions.ts (the never-trust-client re-read discipline)
  </read_first>
  <action>
Append to `lib/validation/cards.ts`:
```
export const CARD_STAGE_VALUES = [
  "briefing",
  "producao",
  "revisao_interna",
  "aprovacao_cliente",
  "agendamento",
] as const;

export const moveCardSchema = z.object({
  cardId: z.string().uuid(),
  toStage: z.enum(CARD_STAGE_VALUES),
});
export type MoveCardInput = z.infer<typeof moveCardSchema>;
```
`CARD_STAGE_VALUES` exists only because `z.enum` needs a literal tuple; add a comment that `lib/cards/stages.ts`'s `STAGE_ORDER` remains the single source of truth for ORDER and that this tuple is a validation-surface mirror of the Postgres `card_stage` enum, never a second ordering.

Append `moveCard(input: MoveCardInput): Promise<{ error?: string }>` to `app/pm/board/actions.ts`, with a header doc-comment stating plainly: unlike `advanceStage`, this action DOES accept a caller-supplied target stage, because drag-and-drop is inherently "put it here" rather than "go one step forward" (D-12). That makes the target an untrusted input, and the three things that make it safe are (a) the zod enum, which bounds it to the five real stages, (b) `evaluateMove` re-run server-side over a server-side re-read of the checklist, and (c) `cards_update_scoped`, which bounds the write to the caller's assigned clients. The browser's copy of `evaluateMove` is a UX affordance only.

Implementation, in this exact order:
  1. `moveCardSchema.safeParse(input)`; on failure return `CARD_NOT_FOUND_ERROR`.
  2. `const supabase = await createClient()`.
  3. Re-read the card through RLS: `supabase.from("cards").select("id, client_id, stage, card_type").eq("id", parsed.data.cardId).single()`. `!card || !card.stage` → `CARD_NOT_FOUND_ERROR` (a package parent has no stage of its own and is never movable — the same rule `advanceStage` already applies).
  4. `if (card.stage === parsed.data.toStage) return {}` — a drop back onto the origin column is a silent no-op, not an error.
  5. Re-read the checklist server-side: `supabase.from("card_checklist_items").select("completed_at").eq("card_id", card.id)`. Never accept a completion claim from the caller.
  6. `const decision = evaluateMove(card.stage, parsed.data.toStage, items ?? []); if (!decision.allowed) return { error: decision.reason };` — returning `decision.reason` verbatim is what makes the drag path show the identical `GATE_BLOCKED_MESSAGE` the button shows (D-13).
  7. If `STAGE_ORDER.indexOf(parsed.data.toStage) >= STAGE_ORDER.indexOf("revisao_interna")`, call `await snapshotChecklistForCard(supabase, card.id, card.client_id)` and return `{ error: snap.error }` when it is not ok. Snapshot BEFORE the stage update, same Pitfall 3 ordering `advanceStage` uses — a mid-request failure must leave the card where it was, never move it into a gated stage with no checklist. The routine is idempotent, so dragging a card back and forth across revisão interna never duplicates or resets its items.
  8. `update({ stage: parsed.data.toStage, updated_at: new Date().toISOString() }).eq("id", card.id)`; on error return `MOVE_FAILED_ERROR`.
  9. `revalidatePath("/pm/board")`, return `{}`.

Add the module-level constant `const MOVE_FAILED_ERROR = "Não foi possível mover o card. Tente novamente.";` next to the existing error constants.

Do not modify `advanceStage` — the Avançar button stays exactly as it is (D-12 keeps both paths), and it must not gain a target-stage parameter.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0, `npm run lint` exits 0, and `npm test` still passes.
    - `app/pm/board/actions.ts` exports `createCard`, `advanceStage`, `toggleChecklistItem`, and `moveCard`.
    - `grep -c 'evaluateMove' app/pm/board/actions.ts` returns at least 1 and `grep -c 'MOVE_SKIPS_REVIEW_MESSAGE' lib/cards/move-rules.ts` returns at least 1 — the action delegates the decision instead of reimplementing it.
    - `grep -c 'snapshotChecklistForCard' app/pm/board/actions.ts` returns at least 2 (advanceStage's call from plan 03-03 plus moveCard's) and `grep -c 'checklist_template_items' app/pm/board/actions.ts` returns 0.
    - `app/pm/board/actions.ts` contains the literal string `Não foi possível mover o card. Tente novamente.` and does NOT contain `Existem itens do checklist` (the gate message is only ever the imported constant or a value returned by `evaluateMove`).
    - `grep -c 'createAdminClient' app/pm/board/actions.ts` returns 0.
    - `grep -vE '^\s*(//|\*|/\*)' app/pm/board/actions.ts | grep -cE 'force|bypass|skipGate|override'` returns 0 — no bypass parameter was introduced on any path.
  </acceptance_criteria>
  <done>A card can be placed in any column by one server call that independently re-decides legality, blocks the D-13 gate case with the identical button-path message, and refuses any forward move that would jump the review step.</done>
</task>

<task type="auto">
  <name>Task 3: Extend createCard with stage/description/assignee and D-15 snapshot-on-create, add updateCardDetails and listClientPmRoster</name>
  <files>lib/validation/cards.ts, lib/actions/clients.ts, app/pm/board/actions.ts</files>
  <read_first>
    - app/pm/board/actions.ts (current createCard — this task extends it in place, keeping its existing re-resolve-the-client-via-RLS discipline)
    - lib/validation/cards.ts (createCardSchema as it stands — this task extends it and appends one more schema)
    - lib/actions/clients.ts (listPmRoster and resolvePmNames — the privileged display-only read convention this task follows, and the `"use server"` constraint that every export must be async)
    - lib/cards/checklist-snapshot.ts (snapshotChecklistForCard)
    - lib/cards/stages.ts (STAGE_ORDER)
    - supabase/migrations/0015_cards.sql (the cards_package_has_no_stage constraint createCard must keep satisfying)
    - supabase/migrations/0017_cards_description_assignee.sql (the `assignee_not_assigned_to_client` exception token this task maps to a Portuguese message)
  </read_first>
  <action>
**A. `lib/validation/cards.ts`.** Extend `createCardSchema` with three optional fields and append one new schema. Keep `clientId`, `title`, and `cardType` exactly as they are:
```
export const createCardSchema = z.object({
  clientId: z.string().uuid({ message: "Cliente inválido." }),
  title: z.string().trim().min(1, { message: "Título obrigatório." }).max(200),
  cardType: z.enum(["single", "package"]),
  // D-14: a card may be created directly in ANY of the five columns, not
  // only Briefing. Ignored for cardType "package" (D-02: a package row has
  // no stage of its own — cards_package_has_no_stage enforces it).
  stage: z.enum(CARD_STAGE_VALUES).optional(),
  // D-16/D-18: optional plain text, multi-line, no markdown.
  description: z.string().trim().max(5000).optional(),
  // D-19: optional single assignee; membership in the client's pm_clients
  // rows is enforced by the cards_assignee_membership_trg trigger, not here.
  assigneeId: z.string().uuid().optional(),
});

export const updateCardDetailsSchema = z.object({
  cardId: z.string().uuid(),
  description: z.string().trim().max(5000).nullable(),
  assigneeId: z.string().uuid().nullable(),
});
export type UpdateCardDetailsInput = z.infer<typeof updateCardDetailsSchema>;
```
Do NOT add `.default(...)` to any of these — `lib/validation/clients.ts` documents why (`zodResolver` needs the input and output types to stay identical for `useForm`), and plan 03-09 binds these fields to a `react-hook-form` form.

**B. `lib/actions/clients.ts`.** Append:
```
export async function listClientPmRoster(
  clientId: string
): Promise<{ id: string; email: string }[]>
```
Doc-comment citing D-19 and stating that this closes the same RLS gap `listPmRoster`/`resolvePmNames` close (`pm_clients_select_own_or_admin` lets a PM read only their own assignment rows, so the roster of a client's OTHER PMs is unreachable through RLS), and that it is display-only and never used for a write. Implementation:
  1. `const supabase = await createClient();` then `const { data: client } = await supabase.from("clients").select("id").eq("id", clientId).single();` and `if (!client) return [];`. This RLS-scoped visibility check runs FIRST and is what stops the privileged read below from becoming a cross-client membership oracle — a caller not assigned to the client gets an empty list, never a roster. This check is a deliberate hardening over `listPmRoster`, which takes no scoping argument at all; comment it as such.
  2. `const admin = createAdminClient();`
  3. `const { data: rows } = await admin.from("pm_clients").select("pm_id").eq("client_id", clientId);` → `const ids = (rows ?? []).map((r) => r.pm_id); if (ids.length === 0) return [];`
  4. `const { data: profiles } = await admin.from("profiles").select("id, email").in("id", ids).eq("role", "pm").eq("status", "approved");`
  5. Return `(profiles ?? []).map((p) => ({ id: p.id, email: p.email ?? "" }))` sorted by `email`.
Use two sequential queries rather than a PostgREST embedded join — the existing helpers in this file never use embeds, and the two-step form has no relationship-name dependency.

**C. `app/pm/board/actions.ts`.** Add the constant `const ASSIGNEE_NOT_ON_CLIENT_ERROR = "Este PM não está atribuído a este cliente.";` next to the other error constants, plus a tiny module-local helper:
```
function isAssigneeMembershipError(error: { message?: string } | null): boolean {
  return Boolean(error?.message?.includes("assignee_not_assigned_to_client"));
}
```
with a comment that this maps migration 0017's deliberate exception token to user-facing Portuguese, and that the trigger — not this mapping — is the actual boundary (D-19).

Extend `createCard`:
  - After the existing RLS re-resolve of the client, compute `const targetStage = parsed.data.cardType === "package" ? null : (parsed.data.stage ?? "briefing")`. Comment that defaulting to `briefing` preserves the pre-D-14 behaviour of the top-level "Criar card" button, which simply omits `stage`.
  - Include `description: parsed.data.description && parsed.data.description.length > 0 ? parsed.data.description : null` and `assignee_id: parsed.data.assigneeId ?? null` in the insert, and set `stage: targetStage` from the computed value rather than the current inline `cardType === "single" ? "briefing" : null` expression.
  - On insert error, `if (isAssigneeMembershipError(insertError)) return { error: ASSIGNEE_NOT_ON_CLIENT_ERROR };` before falling through to `CARD_CREATE_ERROR`.
  - **D-15 snapshot-on-create.** After a successful insert, when `targetStage !== null && STAGE_ORDER.indexOf(targetStage) >= STAGE_ORDER.indexOf("revisao_interna")`, call `await snapshotChecklistForCard(supabase, card.id, client.id)`. If it is not ok, delete the just-created row (`await supabase.from("cards").delete().eq("id", card.id)`) and return `{ error: snap.error }`. Comment this compensating delete explicitly: the card row and its checklist cannot be written in one transaction through the Supabase JS client, and D-15 requires that a card created directly in revisão interna or later ALWAYS carries its snapshot — so the only fail-safe outcome is "no card at all", never "a card sitting in a gated column with no checklist and therefore a vacuously-passing gate". Note that `cards` has no delete GRANT for `authenticated`, so if the delete itself fails the error is still surfaced to the user and the situation is visible rather than silent; if `supabase test db`/`db push` reveals the delete is rejected, add `grant delete on public.cards to authenticated` plus a `cards_delete_scoped` policy in migration 0017 and re-push, rather than dropping the compensating step.
  - Keep `revalidatePath("/pm/board")` and the `{ success: true, cardId }` return shape unchanged.

Add `updateCardDetails(input: UpdateCardDetailsInput): Promise<{ error?: string }>`:
  - `updateCardDetailsSchema.safeParse(input)`; failure → return the first issue message.
  - `createClient()`; `auth.getUser()`; null user → "Não autenticado."
  - Re-read the card through RLS: `supabase.from("cards").select("id").eq("id", parsed.data.cardId).single()`; null → `CARD_NOT_FOUND_ERROR`.
  - `update({ description: parsed.data.description && parsed.data.description.length > 0 ? parsed.data.description : null, assignee_id: parsed.data.assigneeId, updated_at: new Date().toISOString() }).eq("id", parsed.data.cardId)`. The action never accepts a `clientId` — the assignee's legality is decided by the trigger against the card's OWN `client_id`, which the browser cannot influence.
  - On error: `if (isAssigneeMembershipError(error)) return { error: ASSIGNEE_NOT_ON_CLIENT_ERROR };` else return `"Não foi possível salvar o card. Tente novamente."`.
  - `revalidatePath("/pm/board")`, return `{}`.

Continue to use the RLS-scoped `createClient()` only inside `app/pm/board/actions.ts` — the privileged read stays in `lib/actions/clients.ts`, where this codebase already keeps that class of helper.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0, `npm run lint` exits 0, and `npm test` still passes.
    - `app/pm/board/actions.ts` exports `createCard`, `advanceStage`, `toggleChecklistItem`, `moveCard`, and `updateCardDetails`.
    - `grep -c 'revalidatePath("/pm/board")' app/pm/board/actions.ts` returns 5.
    - `app/pm/board/actions.ts` contains the literal strings `assignee_not_assigned_to_client` and `Este PM não está atribuído a este cliente.`, and contains `assignee_id` at least twice (createCard's insert and updateCardDetails' update).
    - `grep -c 'snapshotChecklistForCard' app/pm/board/actions.ts` returns at least 3 (advanceStage, moveCard, createCard).
    - `grep -c 'createAdminClient' app/pm/board/actions.ts` returns 0, and `grep -c 'createAdminClient' lib/actions/clients.ts` returns at least 3 (the two pre-existing helpers plus `listClientPmRoster`).
    - `lib/actions/clients.ts` exports `listClientPmRoster`, and within its body a `from("clients")` RLS visibility check appears BEFORE the first `createAdminClient()` call in that function.
    - `lib/validation/cards.ts` exports `updateCardDetailsSchema` and `createCardSchema` now contains `stage`, `description`, and `assigneeId`; `grep -c '.default(' lib/validation/cards.ts` returns 0.
  </acceptance_criteria>
  <done>A card can be created in any column with an optional description and assignee, always carrying its checklist snapshot when it starts at revisão interna or later, and both fields can be edited afterwards with the team-membership rule enforced by the database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `moveCard` | Both `cardId` AND `toStage` are attacker-controlled — this is the first action in the phase where the target state is caller-supplied rather than derived |
| browser → `createCard` | `stage`, `description`, and `assigneeId` are new attacker-controlled fields; `stage` can name a gated column directly |
| browser → `updateCardDetails` | `assigneeId` is attacker-controlled and names a `profiles` row the caller may have no relationship with |
| Server Action → Postgres | Card insert and checklist snapshot are two separate round-trips with no transaction available |
| `listClientPmRoster` → service-role client | A privileged read of another PM's identity, reachable from an RSC render path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-36 | Tampering | `moveCard` called directly with `toStage: "agendamento"` on a card sitting in revisão interna with unchecked items, bypassing the drag UI entirely | mitigate | `evaluateMove` is re-run inside the action over a server-side re-read of `card_checklist_items`; the browser's copy is a UX affordance. Rule 4 of `evaluateMove` returns `GATE_BLOCKED_MESSAGE` — proved by the Task 1 unit tests |
| T-03-37 | Elevation of Privilege | Using a single drag (or a single direct `moveCard` call) to jump `briefing` → `aprovacao_cliente`, skipping the CHK-03 review gate that has no chance to fire because the card never sits in revisão interna | mitigate | `evaluateMove` rule 3 rejects any forward move where `fromIdx < revIdx && toIdx > revIdx` with `MOVE_SKIPS_REVIEW_MESSAGE`; two dedicated unit tests cover it. The only way past revisão interna is a move that starts there and passes the gate |
| T-03-38 | Tampering | `toStage` set to a value outside the five real stages, or to `null`, corrupting the `card_stage` column | mitigate | `moveCardSchema` bounds it with `z.enum(CARD_STAGE_VALUES)`; the Postgres `card_stage` enum rejects anything that somehow got past zod |
| T-03-39 | Repudiation | A card created directly in revisão interna or later (D-14) with no checklist, so its gate passes vacuously and no audit trail ever exists | mitigate | D-15 snapshot-on-create runs in the same request; on snapshot failure the card row is deleted (compensating action), so the fail-safe outcome is "no card", never "an ungated card" |
| T-03-40 | Tampering | Assigning a card to a PM outside that client's team, creating a visible-to-nobody or misleading ownership record | mitigate | `cards_assignee_membership_trg` is a `security definer` BEFORE INSERT OR UPDATE trigger checking `public.pm_clients`; it fires on both statements and cannot be reached around by the app layer. Proved by pgTAP assertions 3 and 4 |
| T-03-41 | Information Disclosure | `listClientPmRoster` used as a membership oracle — probing which PMs are assigned to a client the caller has no access to | mitigate | The function performs an RLS-scoped `clients` visibility check before touching the service-role client and returns `[]` when the caller cannot see the client; the privileged read is then bounded to that one `client_id` and returns only `id` + `email` of approved PMs |
| T-03-42 | Information Disclosure | A standalone `is this PM on this client` SQL function callable by any authenticated role | mitigate | The membership check is inlined inside the trigger function rather than exposed as a separate helper, so no callable oracle is created; the trigger function is only meaningful in trigger context |
| T-03-43 | Denial of Service | Unbounded `description` inflating rows and every board payload | mitigate | `createCardSchema` and `updateCardDetailsSchema` both bound `description` at 5000 characters (ASVS V5); the board face never renders it at all (D-17), so column payloads are unaffected |
| T-03-44 | Tampering | A second copy of the snapshot or gate logic drifting from the shared one as new paths are added | mitigate | `moveCard` and `createCard` both call `snapshotChecklistForCard`; the acceptance gates assert `checklist_template_items` appears zero times in `app/pm/board/actions.ts` and that `snapshotChecklistForCard` appears at least three times |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed in this plan — `@dnd-kit/*` is installed by plan 03-08, which carries its own legitimacy audit. If a task here turns out to need a package, stop and run the Package Legitimacy Gate protocol first |
</threat_model>

<verification>
- `npm test` green, including the thirteen `lib/cards/move-rules.test.ts` cases.
- `npx supabase migration list --linked` shows 0017 applied remotely.
- `npx supabase test db` — no `not ok` from `0009_cards_assignee_membership_test.sql`, no regression in `0001`–`0008`.
- `npx tsc --noEmit`, `npm run lint`, `npm run build` all pass.
- No UI change is expected from this plan; `/pm/board` must look and behave exactly as plan 03-03 left it.
</verification>

<success_criteria>
- `moveCard` places a card in any legal column, re-deciding legality server-side, and returns `GATE_BLOCKED_MESSAGE` verbatim for the D-13 case.
- A forward move that would jump over revisão interna is rejected on every path (CHK-03 holds under drag-and-drop).
- `cards.description` and `cards.assignee_id` are live, and an out-of-team assignee is rejected by the database on both INSERT and UPDATE (D-19).
- A card created directly in revisão interna or later always carries its checklist snapshot (D-15), or is not created at all.
- `listClientPmRoster` returns a client's assigned PMs only to a caller who can already see that client.
</success_criteria>

<output>
Create `.planning/phases/03-content-production-kanban/03-07-SUMMARY.md` when done.
</output>
