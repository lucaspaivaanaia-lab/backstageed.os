---
phase: 02-client-isolated-ai-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0010_messages.sql
  - supabase/tests/0004_rls_messages_scoping_test.sql
autonomous: false
requirements: [CTX-01, CTX-02]
must_haves:
  truths:
    - "A PM assigned to a client can insert and read messages for that client"
    - "A PM NOT assigned to a client cannot select or insert that client's messages (blocked at the RLS layer, not the UI)"
    - "The messages table and its GRANT/RLS exist on the hosted Supabase project (not only local)"
  artifacts:
    - path: "supabase/migrations/0010_messages.sql"
      provides: "messages table + RLS policies + GRANT to authenticated"
      contains: "create table public.messages"
    - path: "supabase/tests/0004_rls_messages_scoping_test.sql"
      provides: "pgTAP proof of PM message scoping"
      contains: "public.messages"
  key_links:
    - from: "public.messages.client_id"
      to: "public.clients.id"
      via: "foreign key on delete cascade"
      pattern: "references public.clients"
    - from: "messages_select_scoped / messages_insert_scoped"
      to: "public.pm_assigned_clients() / public.is_admin()"
      via: "RLS policy using existing helpers (no inlined cross-table subquery)"
      pattern: "pm_assigned_clients"
---

<objective>
Create the `messages` table that persists a single ongoing chat history per client (D-03), RLS-scoped so a PM only ever sees history for clients they are assigned to (D-02), and prove that isolation with a pgTAP test.

Purpose: This is the persistence + isolation substrate every other Phase 2 plan builds on — the chat Route Handler (02-04) and the curation Server Action (02-05) both read/write this table. Getting the GRANT into the SAME migration (not a follow-up like 0008 had to be) closes the recurring local-vs-hosted privilege gap from Phase 5.
Output: `0010_messages.sql`, `0004_rls_messages_scoping_test.sql`, and the migration applied to the hosted project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-client-isolated-ai-chat/02-RESEARCH.md
@.planning/phases/02-client-isolated-ai-chat/02-PATTERNS.md
@.planning/phases/02-client-isolated-ai-chat/02-VALIDATION.md

<interfaces>
Existing RLS helpers this migration MUST reuse (from supabase/migrations/0004_rls_policies.sql):
- public.is_admin() → boolean
- public.pm_assigned_clients() → setof uuid (client ids the caller is assigned to)

Existing pgTAP fixture uuids (from supabase/tests/rls_helpers.sql — reuse, do NOT invent new ones):
- client_a       = 11111111-1111-1111-1111-111111111111
- client_b       = 22222222-2222-2222-2222-222222222222
- pm_a           = aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa  (role=pm, assigned to client_a only)
- client_a_user  = bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb
- admin_user     = cccccccc-cccc-cccc-cccc-cccccccccccc
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the messages migration and its pgTAP scoping test</name>
  <files>supabase/migrations/0010_messages.sql, supabase/tests/0004_rls_messages_scoping_test.sql</files>
  <read_first>
    - supabase/migrations/0004_rls_policies.sql (policy shape + is_admin()/pm_assigned_clients() signatures)
    - supabase/migrations/0008_clients_grants.sql (GRANT pattern + the local/hosted privilege-gap rationale)
    - supabase/migrations/0006_clients_full_record.sql (clients columns this table references)
    - supabase/tests/0002_rls_client_scoping_test.sql (pgTAP structure: plan(), \ir rls_helpers.sql, tests.set_auth, results_eq, finish/rollback)
    - supabase/tests/rls_helpers.sql (fixture uuids — reuse client_a/client_b/pm_a)
    - .planning/phases/02-client-isolated-ai-chat/02-RESEARCH.md (Code Examples → recommended messages migration, lines 643-681; Pitfall #4)
    - .planning/phases/02-client-isolated-ai-chat/02-PATTERNS.md (0010_messages.sql + 0004_rls_messages_scoping_test.sql assignments)
  </read_first>
  <behavior>
    - A PM (pm_a) reading messages where client_id = client_a returns rows they inserted; reading client_b returns 0 rows.
    - A PM (pm_a) inserting a message with client_id = client_b is rejected by the insert policy.
    - The table has a client_id FK to public.clients with ON DELETE CASCADE, a role CHECK constraint limited to 'user'/'assistant', and created_at defaulting to now().
  </behavior>
  <action>
    Create `supabase/migrations/0010_messages.sql` defining `public.messages` with columns: `id uuid primary key default gen_random_uuid()`, `client_id uuid not null references public.clients(id) on delete cascade`, `role text not null check (role in ('user','assistant'))`, `content text not null`, `created_at timestamptz not null default now()`. Then `alter table public.messages enable row level security;`. Add policy `messages_select_scoped` FOR SELECT TO authenticated USING `(select public.is_admin()) or client_id in (select public.pm_assigned_clients())`. Add policy `messages_insert_scoped` FOR INSERT TO authenticated WITH CHECK using the same predicate. Reuse the existing helper functions — do NOT inline a cross-table subquery against pm_clients (Pitfall-1). Finally add `grant select, insert on public.messages to authenticated;` in THIS SAME migration (Pitfall #4 — do not defer the GRANT to a follow-up migration like 0008 had to). Do NOT add a thread_id column (D-03 keeps a single ongoing history; the deferred multi-thread idea is a trivial additive migration later).

    Create `supabase/tests/0004_rls_messages_scoping_test.sql` following the 0002 test structure: `begin;`, `select plan(N);`, `\ir rls_helpers.sql`, then within the transaction insert one fixture message row for client_a (role='user'). Authenticate as pm_a via `select tests.set_auth('aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa');`. Assert with `results_eq` that pm_a sees exactly 1 message for client_a (11111111-...) and 0 messages for client_b (22222222-...). Add a `throws_ok`/insert-rejection assertion (or a results_eq proving 0 rows persisted) that pm_a cannot insert a row with client_id = client_b. End with `reset role; select set_config('request.jwt.claims','',true); select * from finish(); rollback;`. Insert the client_a fixture message AFTER helpers seed clients but BEFORE set_auth, using the postgres role, so it exists to be read back.
  </action>
  <verify>
    <automated>test -f supabase/migrations/0010_messages.sql && grep -q "grant select, insert on public.messages to authenticated" supabase/migrations/0010_messages.sql && grep -q "pm_assigned_clients" supabase/migrations/0010_messages.sql && grep -q "references public.clients" supabase/migrations/0010_messages.sql && test -f supabase/tests/0004_rls_messages_scoping_test.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0010_messages.sql` contains `create table public.messages`, `enable row level security`, both `messages_select_scoped` and `messages_insert_scoped` policies referencing `pm_assigned_clients()`, and a `grant select, insert on public.messages to authenticated;` line.
    - The role column has `check (role in ('user', 'assistant'))`.
    - `client_id` is declared `references public.clients(id) on delete cascade`.
    - `supabase/tests/0004_rls_messages_scoping_test.sql` uses `\ir rls_helpers.sql`, `tests.set_auth`, and asserts both the "sees own client_a messages" and "blocked from client_b messages" cases.
    - No `thread_id` column exists in the migration.
  </acceptance_criteria>
  <done>Migration and pgTAP test files exist and pass the grep gate above.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply the messages migration and run the RLS suite</name>
  <files>supabase/migrations/0010_messages.sql</files>
  <read_first>
    - supabase/config.toml (local stack config)
    - .planning/STATE.md (history: local `supabase start` grant-parity gaps; hosted migrations 0008/0009 were applied by the user via their own connection)
    - .planning/phases/02-client-isolated-ai-chat/02-RESEARCH.md (Pitfall #4 — permission-denied vs not-ok distinction)
  </read_first>
  <action>
    Apply the new migration to the LOCAL Supabase stack and run the full pgTAP suite: run `npx supabase db reset` (rebuilds the local DB from all migrations including 0010) then `npx supabase test db`. Confirm the new `0004_rls_messages_scoping_test.sql` reports `ok` with zero `not ok` lines for its assertions, and that no `permission denied for table messages` error appears (that error means the GRANT is missing — Pitfall #4). Then apply 0010 to the HOSTED project: prefer the Supabase MCP `apply_migration` tool with name `messages` and the contents of `0010_messages.sql`. If the MCP tool lacks write access in this environment, do not fabricate success — leave hosted application to the human-verify checkpoint in Task 3. This is the mandatory schema-push step: build/type checks pass without it because Supabase types are config-derived, not live-DB-derived — verification would be a false positive if the table is not actually live.
  </action>
  <verify>
    <automated>npx supabase test db 2>&1 | grep -v '^#' | grep -q "messages" && echo "messages test ran"</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase test db` runs `0004_rls_messages_scoping_test.sql` with zero `not ok` lines attributable to the messages assertions.
    - No `permission denied for table messages` appears in the test output (proves the GRANT is effective locally).
    - Either the Supabase MCP `apply_migration` call for `messages` succeeded, OR the executor recorded that hosted application is deferred to the Task 3 checkpoint (never claimed as done without proof).
  </acceptance_criteria>
  <done>Local pgTAP suite green for the messages table; hosted application either completed via MCP or explicitly handed to Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Confirm messages migration is live on hosted</name>
  <action>Human confirms `public.messages` (with RLS + GRANT) is live on the hosted Supabase project, applying it via their own connection if the MCP path could not. This gate must pass before phase verification — build/type checks are false-positive-safe without a live table.</action>
  <what-built>The `messages` table migration (`0010_messages.sql`) with RLS + GRANT, verified green against the local pgTAP suite. Per project history, hosted migrations are typically applied by you through your own Supabase connection.</what-built>
  <how-to-verify>
    1. Confirm the migration is live on the hosted project `ancfwsgyzoostoidqzqj`: use the Supabase MCP `list_migrations` tool (or `list_tables`) and confirm a `messages` migration / `public.messages` table is present.
    2. If it is NOT present, apply `supabase/migrations/0010_messages.sql` via your own Supabase connection (the same path you used for 0008/0009), then re-check.
    3. Confirm `public.messages` has RLS enabled and a GRANT to `authenticated` (the migration includes both).
  </how-to-verify>
  <resume-signal>Type "approved" once `public.messages` is confirmed live on the hosted project, or describe what's missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated PM → messages table | A PM's queries against `messages` cross into per-client data that must be scoped by RLS, not by app-layer filtering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Elevation of Privilege / Tampering | messages RLS policies | mitigate | Scope both SELECT and INSERT via `pm_assigned_clients()`/`is_admin()` — the same helpers already proven in Phase 5; pgTAP proves a PM cannot read/insert another client's rows |
| T-2-05 | Information Disclosure | local vs hosted GRANT parity | mitigate | GRANT to `authenticated` ships in the SAME migration (Pitfall #4), preventing the local-only privilege gap seen in Phase 5's 0008/0009 |
</threat_model>

<verification>
- `npx supabase test db` green for `0004_rls_messages_scoping_test.sql` (no `not ok`, no `permission denied for table messages`).
- Hosted `public.messages` confirmed live via MCP `list_migrations`/`list_tables`.
</verification>

<success_criteria>
- messages table persists user/assistant turns per client with FK + role CHECK.
- RLS blocks cross-client read and insert (proven by pgTAP).
- Table is live on hosted, not only local.
</success_criteria>

<output>
Create `.planning/phases/02-client-isolated-ai-chat/02-01-SUMMARY.md` when done
</output>
