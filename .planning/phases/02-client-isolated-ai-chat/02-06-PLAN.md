---
phase: 02-client-isolated-ai-chat
plan: 06
type: execute
wave: 4
depends_on: [02-04, 02-05]
files_modified: []
autonomous: false
requirements: [CTX-01, CTX-02, CTX-03, CTX-04, CTX-05]
must_haves:
  truths:
    - "With live keys, a PM gets a Claude-generated response scoped to Client A's briefing/RAG and switching to Client B never surfaces Client A's information (CTX-01/CTX-02)"
    - "A curated selection saved from a chat reaches Tropicalia status:ready and is retrievable in a later chat (CTX-04)"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the two behaviors that cannot be pinned by automated tests without live credentials: (1) end-to-end streaming chat with real Claude generation and real cross-client isolation, and (2) the curation save round-trip through Tropicalia's async processing pipeline. This is the phase gate.

Purpose: The automated suite proves the pure logic and RLS scoping; only a live run with `ANTHROPIC_API_KEY` and `TROPICALIA_API_KEY` present can confirm the streamed generation, no-bleed switching, and the upload→ready→retrievable loop (Pitfall #5 — indexing is async). Both keys are confirmed ABSENT as of 2026-07-16, so this checkpoint may need to wait until they are supplied.
Output: Confirmed end-to-end behavior or a recorded gap list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-client-isolated-ai-chat/02-RESEARCH.md
@.planning/phases/02-client-isolated-ai-chat/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight — confirm keys present and full automated suite green</name>
  <files></files>
  <read_first>
    - .env.local (confirm ANTHROPIC_API_KEY and TROPICALIA_API_KEY are set — do NOT print their values)
    - .planning/phases/02-client-isolated-ai-chat/02-RESEARCH.md (Environment Availability table)
  </read_first>
  <action>
    Run the full automated suite: `npm test` (node:test across lib/security, lib/chat, lib/tropicalia) and `npx supabase test db`, and `npm run build`. Confirm all green. Then check whether `ANTHROPIC_API_KEY` and `TROPICALIA_API_KEY` are non-empty in the environment (report present/absent WITHOUT echoing the secret values). If either key is absent, record that the live checkpoint (Task 2/Task 3) is BLOCKED pending the user supplying the key(s) — this is expected per RESEARCH (both confirmed empty as of 2026-07-16) and is not a code defect.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -v '^#' | grep -qi "pass" && npm run build 2>&1 | tail -5 | grep -qiE "compiled|success|ready" </automated>
  </verify>
  <acceptance_criteria>
    - `npm test` and `npx supabase test db` report all green; `npm run build` succeeds.
    - Key presence for `ANTHROPIC_API_KEY` and `TROPICALIA_API_KEY` is reported as present/absent (values never printed).
    - If a key is absent, the blocking status of Tasks 2/3 is explicitly recorded.
  </acceptance_criteria>
  <done>Automated suite green and key availability recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live end-to-end chat isolation verification</name>
  <action>Human runs the app with live keys and verifies streamed generation, cross-client no-bleed on switch, stale-response safety, and degraded mode per the steps below.</action>
  <what-built>The /pm/chat screen with server-side client isolation, streamed Claude responses, and degraded-mode fallback. Automated tests prove the pure logic and RLS scoping; this step confirms the live end-to-end behavior.</what-built>
  <how-to-verify>
    Requires `ANTHROPIC_API_KEY` (and, for full RAG, `TROPICALIA_API_KEY`) set in `.env.local`; run `npm run dev` and sign in as an approved PM assigned to at least two clients.
    1. Open `/pm/chat`, select Client A. Ask a question specific to Client A's briefing. Confirm the response STREAMS token-by-token (D-08) and reads as a Claude-generated answer aligned to Client A (CTX-01/CTX-05).
    2. Switch the client switcher to Client B and ask a question that would only make sense for Client A. Confirm the answer contains NO Client A information — the knowledge base switched entirely (CTX-02).
    3. Start a message about Client A, then switch to Client B before it finishes. Confirm Client A's late tokens never appear in Client B's thread (stale-response guard).
    4. If `TROPICALIA_API_KEY` is empty for a client (or the client has no `tropicalia_project_id`), confirm the "Busca de contexto indisponível — respostas usam apenas o briefing do cliente." badge shows and chat still answers from the briefing (D-06/D-07).
  </how-to-verify>
  <resume-signal>Type "approved" if all four behaviors hold, or describe what failed (each failure becomes a gap for `/gsd:plan-phase --gaps`).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live curation save round-trip verification</name>
  <action>Human saves a curated selection with live keys and verifies the upload reaches Tropicalia status:ready and is retrievable in a later chat, with no auto-save at any point.</action>
  <what-built>The curation flow: checkbox per message + "Salvar como conhecimento" bar that uploads a curated .md to the client's Tropicalia project.</what-built>
  <how-to-verify>
    Requires `TROPICALIA_API_KEY` set and the active client to have a real `tropicalia_project_id`.
    1. In a chat with a RAG-provisioned client, check one PM question + its AI answer, click "Salvar como conhecimento". Confirm the success toast "Conhecimento salvo..." appears (CTX-03/CTX-04).
    2. Confirm the document reaches `status: "ready"` in Tropicalia before testing retrieval — use the Tropicalia list-documents view (indexing is ASYNC per Pitfall #5; do not test retrieval in the immediate next turn).
    3. Once ready, start a fresh question in the same client's chat that depends on the saved content and confirm the answer reflects it (retrievable in a later chat — CTX-04 success criterion).
    4. Confirm nothing was saved automatically at any earlier point — only the explicit checkbox+click produced an upload (CTX-03).
  </how-to-verify>
  <resume-signal>Type "approved" if the save→ready→retrievable round-trip works and no auto-save occurred, or describe the gap.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live PM session → chat + curation | Verifies the isolation and no-auto-save properties hold under real generation/retrieval, not just in unit tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Elevation of Privilege / Tampering | live cross-client switch | mitigate | Checkpoint step 2 is a manual leakage probe — a Client-B answer surfacing Client-A data is a blocking failure |
| T-2-01b | Tampering (curation scope) | live save round-trip | mitigate | Checkpoint step 4 confirms no auto-save; only explicit selection uploads |
</threat_model>

<verification>
- Automated suite green (Task 1).
- Both human-verify checkpoints approved (or gaps recorded for `--gaps` replanning).
</verification>

<success_criteria>
- Live streamed, client-scoped chat with no cross-client bleed.
- Curated save reaches Tropicalia `ready` and is retrievable in a later chat.
- No automatic saving occurs.
</success_criteria>

<output>
Create `.planning/phases/02-client-isolated-ai-chat/02-06-SUMMARY.md` when done
</output>
