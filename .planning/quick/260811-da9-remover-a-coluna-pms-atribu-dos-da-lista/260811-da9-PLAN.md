---
phase: quick/260811-da9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pm/clients/page.tsx
autonomous: true
requirements: []
---

<objective>
Remover a coluna "PMs atribuídos" da listagem de clientes do PM (`app/pm/clients/page.tsx`), mantendo a coluna intacta na listagem do Admin (`app/admin/clients/page.tsx`, arquivo separado, não tocado).

Purpose: item 5 do plano de ação de 2026-08-05 (reunião com Juliano) — a coluna de PMs atribuídos não faz sentido na visão do próprio PM, só na visão do Admin.
Output: `app/pm/clients/page.tsx` sem a coluna "PMs atribuídos" e sem código órfão associado (computação de `assignedPmEmails`, `pmIds`, `pmNames`, import de `resolvePmNames`, campo `pm_clients` no `ClientRow` e na query `.select()`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
</context>

<interfaces>
<!-- Current full content of app/pm/clients/page.tsx (37 lines relevant to this task) -->
<!-- Executor should edit this file directly — no codebase exploration needed. -->

Current imports (line 4): `import { resolvePmNames } from "@/lib/actions/clients";` — used ONLY to build `pmNames` in this file; not referenced anywhere else after this change.

Current `ClientRow` type (lines 22-30):
```typescript
type ClientRow = {
  id: string;
  name: string;
  objective: string | null;
  tone_of_voice: string | null;
  target_audience: string | null;
  content_pillars: string[] | null;
  pm_clients: { pm_id: string }[] | null;
};
```

Current `.select()` query (lines 42-44):
```
"id, name, objective, tone_of_voice, target_audience, content_pillars, pm_clients(pm_id)"
```

Current PM-resolution block (lines 51-56), consumed ONLY by the `assignedPmEmails` computation removed in this task:
```typescript
const pmIds = Array.from(
  new Set(
    clients.flatMap((c) => (c.pm_clients ?? []).map((pc) => pc.pm_id))
  )
);
const pmNames = await resolvePmNames(pmIds);
```

Current table header row (lines 90-97) — the `<TableHead>PMs atribuídos</TableHead>` at line 92 is the one to remove; `Nome`, `Briefing`, and the sr-only `Abrir` header stay.

Current `assignedPmEmails` computation (lines 107-110), used ONLY by the `<TableCell>` at line 137 being removed:
```typescript
const assignedPmEmails = (client.pm_clients ?? [])
  .map((pc) => pmNames[pc.pm_id])
  .filter(Boolean)
  .join(", ");
```

Current PM-column table cell (line 137): `<TableCell>{assignedPmEmails || "—"}</TableCell>` — sits between the Nome cell (lines 128-136) and the Briefing cell (lines 138-144).

Reference only — do NOT modify `app/admin/clients/page.tsx`. It is a fully separate file with its own copy of `pm_clients`/`resolvePmNames`/`pmNames`/`assignedPmEmails`/"PMs atribuídos" header+cell, which must remain untouched (it also has an extra `tag` column PM's page does not have).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove PMs atribuídos column and its orphaned computation from the PM client list</name>
  <files>app/pm/clients/page.tsx</files>
  <action>
Edit `app/pm/clients/page.tsx` only (never touch `app/admin/clients/page.tsx`):

1. Remove the `<TableHead>PMs atribuídos</TableHead>` line from the `<TableRow>` inside `<TableHeader>`.
2. Remove the `<TableCell>{assignedPmEmails || "—"}</TableCell>` line from the row-rendering `<TableRow>` (the cell that sat between the Nome cell and the Briefing cell).
3. Remove the now-orphaned `assignedPmEmails` computation (the block building it from `client.pm_clients` and `pmNames`) inside the `clients.map(...)` callback — nothing else in the callback references it after step 2.
4. Remove the now-orphaned `pmIds`/`pmNames` computation block (`pmIds` via `Array.from(new Set(...))` over `pm_clients`, and `const pmNames = await resolvePmNames(pmIds)`) — after step 3, nothing in the file reads `pmNames` or `pmIds`.
5. Remove the `import { resolvePmNames } from "@/lib/actions/clients";` import — after step 4, nothing in the file calls `resolvePmNames`.
6. Remove `pm_clients: { pm_id: string }[] | null;` from the `ClientRow` type — after step 4, nothing in the file reads `client.pm_clients`.
7. Remove `, pm_clients(pm_id)` from the `.select("id, name, objective, tone_of_voice, target_audience, content_pillars, pm_clients(pm_id)")` string, leaving `"id, name, objective, tone_of_voice, target_audience, content_pillars"` — after step 6 the fetched relation has no consumer left in the file.

Do not touch `UsersIcon`/`ChevronRightIcon` imports, `ClientListLink`, `resolvePmNames`'s own module (`lib/actions/clients.ts`), or anything in `app/admin/clients/page.tsx` (which keeps its own independent `pm_clients`/`resolvePmNames`/`pmNames`/`assignedPmEmails`/PMs column, unmodified).
  </action>
  <verify>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && npx tsc --noEmit && npx eslint app/pm/clients/page.tsx && grep -c "PMs atribuídos\|assignedPmEmails\|resolvePmNames\|pm_clients\|pmNames\|pmIds" app/pm/clients/page.tsx</automated>
  </verify>
  <done>
`app/pm/clients/page.tsx` renders a table with only Nome / Briefing / (sr-only Abrir) columns. `tsc --noEmit` and `eslint` both pass with zero errors (confirms no orphaned/unused identifiers left behind). The final `grep -c` above returns `0` (no trace of the removed PM-column code remains in the file). `git diff --stat app/admin/clients/page.tsx` shows no changes — the Admin listing and its "PMs atribuídos" column are untouched.
  </done>
</task>

</tasks>

<verification>
Run `cd /Users/lucaspaiva/projects/backstageed.OS && npx tsc --noEmit && npx eslint app/pm/clients/page.tsx && npm run build` — all must pass green. Confirm `git diff --stat` shows exactly one file changed: `app/pm/clients/page.tsx`. Confirm `app/admin/clients/page.tsx` has zero diff.
</verification>

<success_criteria>
- `app/pm/clients/page.tsx` no longer renders a "PMs atribuídos" column or cell.
- No orphaned code remains: `assignedPmEmails`, `pmIds`, `pmNames`, the `resolvePmNames` import, the `pm_clients` field in `ClientRow`, and `pm_clients(pm_id)` in the `.select()` query are all removed.
- `app/admin/clients/page.tsx` is byte-for-byte unchanged; its "PMs atribuídos" column still works exactly as before.
- `tsc --noEmit`, `eslint`, and `npm run build` all pass with zero errors.
</success_criteria>

<output>
Create `.planning/quick/260811-da9-remover-a-coluna-pms-atribu-dos-da-lista/260811-da9-SUMMARY.md` when done
</output>
</content>
