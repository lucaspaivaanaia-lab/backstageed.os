---
phase: quick
plan: 260715-ca2
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [AUTH-06, AUTH-07, AUTH-08]
files_modified:
  - supabase/tests/0001_rls_pm_scoping_test.sql
  - supabase/tests/0002_rls_client_scoping_test.sql
  - supabase/tests/0003_rls_admin_unrestricted_test.sql
  - supabase/tests/rls_helpers.sql
  - .planning/phases/05-access-roles/05-06-PLAN.md

must_haves:
  truths:
    - "All three RLS test files include the shared fixture via a cwd-independent path (\\ir), so `supabase test db`'s pg_prove container can locate rls_helpers.sql regardless of its working directory"
    - "The two `alter table auth.users ... trigger` statements in rls_helpers.sql run under a role that owns auth.users (supabase_auth_admin), so fixture setup no longer aborts with 'must be owner of table users'"
    - "No shipped migration, RLS policy, or trigger is modified — only the test fixtures under supabase/tests/ and the phase plan doc change"
  artifacts:
    - path: "supabase/tests/rls_helpers.sql"
      provides: "pgTAP fixture with transaction-scoped role switch around auth.users trigger DDL"
      contains: "set local role supabase_auth_admin"
    - path: "supabase/tests/0001_rls_pm_scoping_test.sql"
      provides: "AUTH-06 test with cwd-independent fixture include"
      contains: "\\ir rls_helpers.sql"
  key_links:
    - from: "supabase/tests/0001_rls_pm_scoping_test.sql"
      to: "supabase/tests/rls_helpers.sql"
      via: "\\ir include-relative directive"
      pattern: "\\\\ir rls_helpers.sql"
---

<objective>
Fix two concrete pgTAP test-file bugs surfaced by the first-ever live execution of the RLS suite (Phase 5, gap 2 in 05-VERIFICATION.md). Both are bugs in the test fixtures — NOT in the shipped migrations, which are already live-verified.

Bug 1: `\i supabase/tests/rls_helpers.sql` resolves relative to the invoking process cwd (not the test file's own directory), so it fails inside `supabase test db`'s pg_prove container.

Bug 2: `alter table auth.users disable/enable trigger on_auth_user_created` requires ownership of `auth.users`, which the connecting `postgres` role does not have in the local Supabase stack — causing "must be owner of table users".

Purpose: Unblock a green `supabase test db` run so AUTH-06/07/08 can be proven at the live Postgres RLS layer (05-06's outstanding checkpoint).
Output: Corrected test fixtures + a sync note in 05-06-PLAN.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-access-roles/05-VERIFICATION.md
@.planning/phases/05-access-roles/05-06-PLAN.md

# The exact fix is fully specified below. Do NOT re-derive the diagnosis.
# CONSTRAINT: touch ONLY files under supabase/tests/ (plus the 05-06-PLAN.md sync note).
# Do NOT modify supabase/migrations/0004_rls_policies.sql, 0001_profiles.sql, or any shipped RLS policy/trigger.
# This fix CANNOT be verified in this sandbox (no Docker). Verification is a static grep check only.
# The user re-runs `npx supabase test db` locally after this lands.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix include path + auth.users trigger ownership in the pgTAP fixtures</name>
  <files>supabase/tests/0001_rls_pm_scoping_test.sql, supabase/tests/0002_rls_client_scoping_test.sql, supabase/tests/0003_rls_admin_unrestricted_test.sql, supabase/tests/rls_helpers.sql</files>
  <action>
Bug 1 (include path) — in all THREE test files, replace the single line `\i supabase/tests/rls_helpers.sql` with `\ir rls_helpers.sql`. The line is at 0001_rls_pm_scoping_test.sql:18, 0002_rls_client_scoping_test.sql:15, 0003_rls_admin_unrestricted_test.sql:13. `\ir` (include-relative) resolves relative to the currently executing script's own directory; since all four files live in supabase/tests/, this works regardless of the pg_prove container's cwd. Do not change anything else in these three files.

Bug 2 (auth.users ownership) — in supabase/tests/rls_helpers.sql, wrap ONLY the two `alter table auth.users ... trigger on_auth_user_created` statements (line 56 disable, line 88 enable) in a transaction-scoped role switch to supabase_auth_admin, which owns auth.users and which the connecting postgres role is a member of. Before line 56, add `set local role supabase_auth_admin;` and immediately after the disable statement add `reset role;`. Do the same around the enable statement at line 88 (`set local role supabase_auth_admin;` before, `reset role;` after). Leave the `insert into auth.users (...) ... on conflict (id) do nothing;` block (lines 58-86) running under the connecting role UNCHANGED — its existing DML grants already work; do not switch its role. `set local role` requires only membership (not superuser) and is transaction-scoped, safe inside each test file's existing begin;/rollback; wrapping, and does not weaken any production permission.

Do NOT touch the tests.set_auth() helper, the profiles/clients/pm_clients inserts, or any comment block beyond what the two edits require.
  </action>
  <verify>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && test $(grep -lc '\\ir rls_helpers.sql' supabase/tests/0001_rls_pm_scoping_test.sql supabase/tests/0002_rls_client_scoping_test.sql supabase/tests/0003_rls_admin_unrestricted_test.sql | wc -l) -eq 3 && ! grep -rq '\\i supabase/tests/rls_helpers.sql' supabase/tests/ && test $(grep -c 'set local role supabase_auth_admin' supabase/tests/rls_helpers.sql) -eq 2 && test $(grep -c 'reset role' supabase/tests/rls_helpers.sql) -ge 2 && echo PASS</automated>
  </verify>
  <done>All 3 test files contain `\ir rls_helpers.sql`; no file contains `\i supabase/tests/rls_helpers.sql`; rls_helpers.sql has `set local role supabase_auth_admin` (x2) and `reset role` (>=2) surrounding both auth.users trigger ALTER statements; the auth.users INSERT block is unchanged; no supabase/migrations file was modified.</done>
</task>

<task type="auto">
  <name>Task 2: Sync 05-06-PLAN.md with the fix note</name>
  <files>.planning/phases/05-access-roles/05-06-PLAN.md</files>
  <action>
05-06-PLAN.md's objective already anticipates this: "if an assertion fails at runtime, that is a bug in the test file ... should be fixed in a follow-up, not here." This quick task IS that follow-up. Add a short note in 05-06-PLAN.md (in the objective/investigation-outcome area) recording that the first live `supabase test db` run surfaced two test-file bugs — (1) `\i` include path resolving relative to pg_prove's cwd, and (2) "must be owner of table users" on the auth.users trigger ALTER — and that both were fixed in the quick task at `.planning/quick/260715-ca2-fix-2-pgtap-test-bugs-from-phase-5-gap-2/` (change to `\ir` include-relative + transaction-scoped `set local role supabase_auth_admin` around the trigger DDL). Keep it concise (2-4 lines). Do not restructure the plan or alter its must_haves/frontmatter.
  </action>
  <verify>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && grep -q '260715-ca2-fix-2-pgtap-test-bugs-from-phase-5-gap-2' .planning/phases/05-access-roles/05-06-PLAN.md && echo PASS</automated>
  </verify>
  <done>05-06-PLAN.md contains a note referencing the quick task directory path and summarizing the two test-file fixes; frontmatter and must_haves are unchanged.</done>
</task>

</tasks>

<verification>
This fix cannot be executed against a live Postgres in this sandbox (no Docker). Verification is static only:
- `\ir rls_helpers.sql` present in all 3 test files; `\i supabase/tests/rls_helpers.sql` absent everywhere.
- `set local role supabase_auth_admin` wraps both auth.users trigger ALTER statements in rls_helpers.sql; each is followed by `reset role`.
- No file under supabase/migrations/ was modified.
- 05-06-PLAN.md references this quick task.

The user re-runs `npx supabase test db` locally to confirm the suite reaches green (that live run is 05-06's checkpoint, not this task's responsibility).
</verification>

<success_criteria>
- Bug 1 fixed: all 3 test files use `\ir rls_helpers.sql`.
- Bug 2 fixed: both auth.users trigger ALTERs run under supabase_auth_admin via transaction-scoped role switch; the auth.users INSERT is untouched.
- Zero changes to shipped migrations, RLS policies, or triggers.
- 05-06-PLAN.md notes the two fixes and links this quick task.
</success_criteria>

<output>
Create `.planning/quick/260715-ca2-fix-2-pgtap-test-bugs-from-phase-5-gap-2/260715-ca2-SUMMARY.md` when done.
</output>
