---
phase: quick/260811-du5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/chat/assemble-prompt.ts
  - lib/chat/assemble-prompt.test.ts
autonomous: true
requirements: [QUICK-260811-du5]

must_haves:
  truths:
    - "The chat system prompt forbids em dash (—) and asterisk (*) in generated content"
    - "The chat system prompt instructs short paragraphs (max 2 lines each) with a blank line between paragraphs, optimized for reading on LinkedIn"
    - "The chat system prompt instructs the model, when the user asks for a correction/adjustment to content already generated in the same conversation, to edit that existing content and return the full corrected version — never restart from scratch or generate a disconnected new piece"
    - "The new instructions live in the SAME trusted preamble block that already precedes briefingBlock/filesBlock, preserving the T-2-02 anti-prompt-injection ordering (instructions before untrusted client/file content)"
    - "Existing leakage-guard and briefing-rendering tests in assemble-prompt.test.ts continue to pass unmodified — no existing assertion is weakened or removed"
    - "app/api/chat/route.ts requires zero changes — it already resends the full conversation history (including prior assistant replies) to Claude on every turn, which is what makes 'edit in place' technically actionable by the model"
  artifacts:
    - path: "lib/chat/assemble-prompt.ts"
      provides: "assembleSystemPrompt's trusted preamble carries the literal formatting + edit-in-place instructions from the 2026-08-05 action plan (item 6), appended before briefingBlock/filesBlock"
      contains: "nunca use travessão"
    - path: "lib/chat/assemble-prompt.test.ts"
      provides: "New test cases proving the formatting rules and edit-in-place instruction are present in the assembled prompt, and that they appear before the client's briefing content (ordering/T-2-02 check)"
      contains: "otimize a formatação para leitura no LinkedIn"
  key_links:
    - from: "app/api/chat/route.ts"
      to: "lib/chat/assemble-prompt.ts (assembleSystemPrompt)"
      via: "const system = assembleSystemPrompt(client, files); passed as the `system` param to anthropic.messages.stream(...) unchanged"
      pattern: "assembleSystemPrompt\\(client, files\\)"
---

<objective>
Add the exact, already-approved formatting + edit-in-place instruction text (item 6 of the 2026-08-05 Juliano action plan) to the chat's content-generation system prompt: no em dash, no asterisk, short paragraphs, LinkedIn-optimized formatting, and — when the user asks for a correction on content already generated in this conversation — edit that content in place and return the full corrected version instead of starting over.

This is a pure prompt-text change to `lib/chat/assemble-prompt.ts`'s `assembleSystemPrompt`, the CHAT generation prompt (not `lib/ai/extraction-prompt.ts`, which is the separate structured-extraction prompt for checklist/briefing/validation — out of scope here, per this quick task's own scope note). `app/api/chat/route.ts` already resends the full conversation history — including the AI's own prior replies — to Claude on every turn, so "edit in place" is purely an instruction the model needs, not a capability gap.

`assembleSystemPrompt` was touched most recently by quick task 260810-ivr (client identification by `tag`) — this plan reads and builds on that CURRENT state, not any earlier version of the file.

Purpose: stop generated LinkedIn content from using markdown/prose conventions (em dash, asterisk, long paragraphs) that don't read well on LinkedIn, and stop correction requests from wastefully regenerating unrelated content from scratch.
Output: `assembleSystemPrompt` embeds the new instructions in its trusted preamble; `assemble-prompt.test.ts` gains test coverage proving the instructions are present and correctly ordered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@lib/chat/assemble-prompt.ts
@lib/chat/assemble-prompt.test.ts

<baseline>
- `assembleSystemPrompt(client: Briefing, files: ClientFileContext[])` returns one big string built from three pieces, in this order: (1) a trusted instruction preamble (a `+`-concatenated template-literal chain), (2) `briefingBlock` (the client's own briefing fields, including `tag`), (3) `filesBlock` (the client's uploaded file content, appended only when non-empty). This plan adds ONLY to piece (1) — no change to `briefingBlock`, `filesBlock`, the `Briefing` type, or the function signature.
- T-2-02 (documented in the file's top comment, accept-disposition): system instructions sit BEFORE briefing/file content specifically to reduce the chance that injected text inside a client's own uploaded files can override them. The new instructions MUST stay inside that same pre-content preamble — do not append them after `${briefingBlock}${filesBlock}`.
- The preamble currently ends with (verbatim, current file, lines 73-83): a sentence about not confusing the client with other companies/people named in reference files, followed immediately by `\n\n${briefingBlock}${filesBlock}`. The new instructions are inserted between that existing sentence and `${briefingBlock}`.
- `npm test` already runs `node --test ... lib/chat/*.test.ts ...` (see `package.json`) — no test-runner config change needed. `assemble-prompt.test.ts` currently imports `assembleSystemPrompt` from `./assemble-prompt.ts` and has 7 passing tests (leakage guard, degraded mode, files block, null fields, tag identification) — none of them assert on the exact preamble wording being extended here, so none will break from an additive preamble change.
- The instruction text below is user-approved verbatim (Juliano, 2026-08-05 meeting) — do not paraphrase, shorten, or reword any part of it, including its own literal em dash and parentheses.
</baseline>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add formatting + edit-in-place instructions to the chat system prompt, with test coverage</name>
  <files>lib/chat/assemble-prompt.ts, lib/chat/assemble-prompt.test.ts</files>
  <behavior>
    - `assembleSystemPrompt`: the returned prompt contains every one of these stable substrings (assert each independently, not as one giant regex): "nunca use travessão", "nem asterisco", "parágrafos curtos", "no máximo 2 linhas", "uma linha em branco entre parágrafos", "otimize a formatação para leitura no LinkedIn".
    - `assembleSystemPrompt`: the returned prompt contains the edit-in-place instruction, asserted via stable substrings: "edite o conteúdo existente", "devolva a versão corrigida completa", "nunca recomece do zero".
    - `assembleSystemPrompt`: the new instructions appear BEFORE the client's briefing content in the returned string (T-2-02 ordering) — assert `prompt.indexOf("nunca use travessão")` is less than `prompt.indexOf(client.name)` (using the existing `CLIENT_A` fixture, whose `name` only appears inside `briefingBlock`).
    - `assembleSystemPrompt`: all 7 existing tests in the file continue to pass unmodified — this task adds new test cases, it does not edit any existing `test(...)` block or fixture.
  </behavior>
  <action>
    In `lib/chat/assemble-prompt.ts`, add a new local constant inside `assembleSystemPrompt`, named `formattingBlock`, defined as a single template-literal string holding this EXACT text, verbatim, with no paraphrasing (it includes its own literal em dash and double quotes — keep them exactly as shown, the double quotes are safe unescaped inside a backtick template literal): "Ao gerar conteúdo para publicação, siga estas regras de formatação: nunca use travessão (—) nem asterisco (*) no texto; escreva parágrafos curtos, de no máximo 2 linhas cada, com uma linha em branco entre parágrafos; otimize a formatação para leitura no LinkedIn. Se o usuário pedir uma correção ou ajuste em um conteúdo que você já gerou nesta conversa (ex: "corrija X", "ajuste o começo"), edite o conteúdo existente e devolva a versão corrigida completa — nunca recomece do zero nem gere um conteúdo novo e desconectado do anterior."

    Insert `formattingBlock` into the function's final `return (...)` template literal, between the existing preamble's last sentence (ends "...identificado por esse código.") and `${briefingBlock}${filesBlock}`. Change the tail of that return statement from `` identificado por esse código.\n\n${briefingBlock}${filesBlock}` `` to `` identificado por esse código.\n\n${formattingBlock}\n\n${briefingBlock}${filesBlock}` `` — this keeps `formattingBlock` fully inside the trusted pre-content preamble, preserving T-2-02.

    Update the file's top doc comment: add one short paragraph noting quick task 260811-du5 (item 6 of the 2026-08-05 Juliano action plan) added user-approved LinkedIn formatting rules (no em dash, no asterisk, short paragraphs) and an edit-in-place instruction for correction requests, both inside the same trusted preamble as the existing T-2-02 note — purely additive text, zero change to the client-identification logic from 260810-ivr.

    In `lib/chat/assemble-prompt.test.ts`, add the new test cases described in `<behavior>` above (a reasonable split is one test for the formatting-rules substrings, one for the edit-in-place substrings, one for the before-briefing ordering check) — do not modify any of the 7 existing `test(...)` blocks or the `CLIENT_A`/`CLIENT_B` fixtures.
  </action>
  <verify>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && npx tsc --noEmit 2>&1 | tail -40</automated>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && npx eslint lib/chat/assemble-prompt.ts lib/chat/assemble-prompt.test.ts 2>&1 | tail -40</automated>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && node --test lib/chat/assemble-prompt.test.ts 2>&1 | tail -60</automated>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && grep -c 'travessão' lib/chat/assemble-prompt.ts</automated>
  </verify>
  <done>`tsc --noEmit` and `eslint` are clean on both files. `node --test lib/chat/assemble-prompt.test.ts` reports 0 failures across all tests (7 pre-existing + new ones added by this task). `assembleSystemPrompt`'s returned string contains the literal formatting-rules and edit-in-place text inside the trusted preamble, before `briefingBlock`/`filesBlock`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System preamble (trusted, developer-authored) → briefingBlock/filesBlock (client-controlled content) | Unchanged by this plan — the new instructions are pure static text added to the ALREADY-trusted preamble, positioned before any client-supplied content, exactly like every other sentence already there |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-du5-01 | Tampering | Preamble/content ordering (T-2-02) | accept | `formattingBlock` is inserted strictly between the existing preamble's last sentence and `${briefingBlock}${filesBlock}` — never after client/file content — preserving the file's existing accept-disposition rationale for T-2-02 unchanged; the new ordering test in Task 1 pins this |
| T-du5-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — pure prompt text + test additions, zero `package.json` change |
</threat_model>

<verification>
1. `npx tsc --noEmit` and `npx eslint` clean on both touched files.
2. `node --test lib/chat/assemble-prompt.test.ts` (or `npm test`, which includes this glob) passes with 0 failures.
3. `grep -c 'travessão' lib/chat/assemble-prompt.ts` returns a non-zero count (the new instruction text is present in source).
4. Manual read of the diff confirms the inserted text matches the user-approved instruction verbatim, with zero paraphrasing.
</verification>

<success_criteria>
- `assembleSystemPrompt`'s output includes the exact, unparaphrased formatting rules (no em dash, no asterisk, short paragraphs with blank lines between them, LinkedIn-optimized) and the edit-in-place correction instruction.
- The new text sits inside the same trusted preamble block that already precedes `briefingBlock`/`filesBlock`, preserving T-2-02.
- `lib/ai/extraction-prompt.ts` is untouched (out of scope, confirmed by `files_modified` containing only the two `lib/chat/assemble-prompt.*` files).
- `app/api/chat/route.ts` is untouched — it already resends full conversation history every turn.
- All existing tests in `assemble-prompt.test.ts` still pass, plus new coverage for the added instructions and their ordering.
- `tsc`/`eslint`/`test` all green.
</success_criteria>

<output>
Create `.planning/quick/260811-du5-reescrever-o-prompt-de-gera-o-de-conte-d/260811-du5-SUMMARY.md` when done
</output>
</content>
