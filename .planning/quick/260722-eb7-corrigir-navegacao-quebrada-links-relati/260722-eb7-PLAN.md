---
phase: quick-260722-eb7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/clients/page.tsx
  - app/pm/clients/page.tsx
  - lib/actions/auth.ts
  - components/layout/app-nav.tsx
  - app/pm/layout.tsx
  - app/admin/layout.tsx
  - app/(auth)/login/page.tsx
  - app/(auth)/forgot-password/page.tsx
  - app/(auth)/forgot-password/actions.ts
  - app/auth/callback/route.ts
  - app/(auth)/reset-password/page.tsx
  - app/(auth)/reset-password/actions.ts
  - middleware.ts
autonomous: true
requirements: [NAV-FIX, NAV-SHELL, AUTH-RESET]

must_haves:
  truths:
    - "De /admin/clients, 'Criar cliente' navega para /admin/clients/new e clicar num cliente navega para /admin/clients/{id}"
    - "De /pm/clients, 'Criar cliente' navega para /pm/clients/new e clicar num cliente navega para /pm/clients/{id}"
    - "Um PM autenticado vê um nav persistente com links para Clientes (/pm/clients) e Chat (/pm/chat), mais um botão Sair"
    - "Um Admin autenticado vê um nav persistente com links para Clientes (/admin/clients) e Aprovações (/admin/approvals), mais um botão Sair"
    - "Clicar em Sair encerra a sessão (auth.signOut) e redireciona para /login"
    - "Na tela de login existe um link 'Esqueceu sua senha?' apontando para /forgot-password"
    - "Enviar um e-mail em /forgot-password mostra sempre uma mensagem de sucesso genérica, sem revelar se o e-mail existe"
    - "O link do e-mail de recuperação passa por /auth/callback, troca o code por uma sessão de recuperação e chega em /reset-password"
    - "Definir uma nova senha em /reset-password troca a senha, encerra a sessão de recuperação e volta para /login"
    - "middleware continua redirecionando pending/rejected/must_change_password e cross-role para todas as rotas que não sejam as novas rotas públicas"
  artifacts:
    - path: "app/admin/clients/page.tsx"
      provides: "Links absolutos /admin/clients/new e /admin/clients/{id}"
      contains: "/admin/clients/new"
    - path: "app/pm/clients/page.tsx"
      provides: "Links absolutos /pm/clients/new e /pm/clients/{id}"
      contains: "/pm/clients/new"
    - path: "lib/actions/auth.ts"
      provides: "Server Action de logout (signOut + redirect /login)"
      contains: "signOut"
    - path: "components/layout/app-nav.tsx"
      provides: "Header/nav persistente reutilizavel com links + botao Sair"
    - path: "app/pm/layout.tsx"
      provides: "Layout de navegacao da area /pm com link para /pm/chat"
    - path: "app/admin/layout.tsx"
      provides: "Layout de navegacao da area /admin"
    - path: "app/(auth)/forgot-password/page.tsx"
      provides: "Formulario publico de recuperacao de senha"
    - path: "app/(auth)/forgot-password/actions.ts"
      provides: "Server Action resetPasswordForEmail com resposta generica anti-enumeracao"
      contains: "resetPasswordForEmail"
    - path: "app/auth/callback/route.ts"
      provides: "Route Handler que troca o code de recuperacao por sessao (exchangeCodeForSession)"
      contains: "exchangeCodeForSession"
    - path: "app/(auth)/reset-password/page.tsx"
      provides: "Formulario de nova senha (sessao de recuperacao)"
    - path: "app/(auth)/reset-password/actions.ts"
      provides: "Server Action updateUser(password) + signOut"
      contains: "updateUser"
    - path: "middleware.ts"
      provides: "PUBLIC_PATHS com /forgot-password, /reset-password, /auth/callback"
      contains: "/forgot-password"
  key_links:
    - from: "app/(auth)/login/page.tsx"
      to: "/forgot-password"
      via: "Link"
      pattern: "/forgot-password"
    - from: "app/(auth)/forgot-password/actions.ts"
      to: "/auth/callback"
      via: "resetPasswordForEmail redirectTo"
      pattern: "auth/callback"
    - from: "app/auth/callback/route.ts"
      to: "/reset-password"
      via: "exchangeCodeForSession + redirect"
      pattern: "exchangeCodeForSession"
    - from: "app/(auth)/reset-password/actions.ts"
      to: "supabase.auth"
      via: "updateUser + signOut"
      pattern: "updateUser"
    - from: "components/layout/app-nav.tsx"
      to: "lib/actions/auth.ts"
      via: "signOut server action"
      pattern: "signOut"
---

<objective>
Corrigir a navegação quebrada da plataforma e fechar as lacunas de acesso por clique:
1. Trocar os 6 hrefs relativos ("./new", `./${id}`) em admin/clients e pm/clients por caminhos absolutos.
2. Construir layouts de navegação persistente para /pm/* e /admin/* (header com links + botão Sair), incluindo o botão que hoje falta para chegar em /pm/chat.
3. Implementar o fluxo real de "Esqueci minha senha" via Supabase Auth (PKCE): link no login → /forgot-password → e-mail → /auth/callback → /reset-password.

Purpose: Hoje não existe forma de navegar por clique nas áreas PM/Admin, os botões de cliente levam para 404s de rota errada, não há logout, e não há como recuperar senha estando deslogado.
Output: Links corrigidos, dois layouts de nav, uma server action de logout e um fluxo de reset de senha em 3 páginas + route handler + ajuste de middleware.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Confirmado lendo o codigo — usar EXATAMENTE estas APIs, ja instaladas neste projeto. -->
<!-- @supabase/ssr ^0.12.0 + @supabase/supabase-js ^2.110.0 → fluxo PKCE por padrao. -->

lib/supabase/server.ts:
  export async function createClient(): Promise<SupabaseClient>
  // usa cookies() de next/headers; em Route Handlers e Server Actions consegue SETAR cookies.

lib/supabase/client.ts:
  export function createClient(): SupabaseClient  // browser (createBrowserClient)

APIs Supabase Auth confirmadas em uso no projeto:
  supabase.auth.signInWithPassword({ email, password })   // app/(auth)/login/actions.ts
  supabase.auth.getUser()                                 // middleware.ts, change-password/actions.ts
  supabase.auth.updateUser({ password })                  // app/(auth)/change-password/actions.ts
  // Para o fluxo de reset usar (mesma lib, PKCE):
  supabase.auth.resetPasswordForEmail(email, { redirectTo })
  supabase.auth.exchangeCodeForSession(code)              // troca ?code=... por sessao no route handler
  supabase.auth.signOut()

Design tokens / componentes ja estabelecidos:
  components/layout/page-shell.tsx → PageShell (width "default"|"wide"|"narrow"), PageTitle, SectionTitle, EmptyState
  components/ui/* → button, card, input, label, badge, table (shadcn/ui)
  Telas de auth (login, change-password) usam o padrao CENTRADO:
    <div className="flex flex-1 items-center justify-center p-4"> + <Card className="w-full max-w-sm">
    NAO usam PageShell. As novas telas /forgot-password e /reset-password DEVEM espelhar esse padrao centrado.
  Toaster (sonner) montado em app/layout.tsx → usar toast.success de "sonner" como em change-password/page.tsx.

Schema de validacao reutilizavel para o reset (min 8 chars + confirm igual):
  lib/validation/client-access.ts → changePasswordSchema ({ password, confirmPassword }), ChangePasswordInput
  Reutilizar este schema no /reset-password (nao criar um novo).

Padrao anti-enumeracao ja adotado no projeto (nunca revelar se e-mail existe):
  app/(auth)/login/actions.ts retorna erro generico unico — /forgot-password deve retornar SUCESSO generico sempre.

middleware.ts (estado atual):
  PUBLIC_PATHS = ["/login","/signup","/pending","/rejected","/change-password"]
  isPublicPath(pathname) → match exato OU startsWith(path + "/"); so afeta o branch `!user`.
  Apos o branch !user vem: gate pending/rejected/deactivated → must_change_password → role-routing (admin/pm/client).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir os 6 hrefs relativos em admin/clients e pm/clients</name>
  <files>app/admin/clients/page.tsx, app/pm/clients/page.tsx</files>
  <action>
Em app/admin/clients/page.tsx, trocar os 3 hrefs relativos por absolutos: os dois `href="./new"` (botão "Criar cliente" no header e no empty-state) para `href="/admin/clients/new"`, e o `href={`./${client.id}`}` da célula da tabela para `href={`/admin/clients/${client.id}`}`.
Em app/pm/clients/page.tsx, aplicar o equivalente: os dois `href="./new"` para `href="/pm/clients/new"`, e o `href={`./${client.id}`}` para `href={`/pm/clients/${client.id}`}`.
Não alterar nenhuma outra linha (query RLS, resolvePmNames, badges, PageShell) — apenas os 6 valores de href. Causa raiz: o Next resolve href relativo removendo o último segmento da URL atual, mandando /admin/clients/new → /admin/new.
  </action>
  <verify>
    <automated>test "$(grep -c '\./new\|\./\${client.id}' app/admin/clients/page.tsx app/pm/clients/page.tsx 2>/dev/null | grep -v ':0' | wc -l | tr -d ' ')" = "0" && grep -q '/admin/clients/new' app/admin/clients/page.tsx && grep -q '/admin/clients/\${client.id}' app/admin/clients/page.tsx && grep -q '/pm/clients/new' app/pm/clients/page.tsx && grep -q '/pm/clients/\${client.id}' app/pm/clients/page.tsx && echo OK</automated>
  </verify>
  <done>Nenhum href relativo "./" restante nos dois arquivos; os 4 links absolutos de destino presentes (2 new + 2 detalhe). Query e demais markup inalterados.</done>
</task>

<task type="auto">
  <name>Task 2: Server Action de logout + AppNav reutilizavel + layouts /pm e /admin</name>
  <files>lib/actions/auth.ts, components/layout/app-nav.tsx, app/pm/layout.tsx, app/admin/layout.tsx</files>
  <action>
Criar lib/actions/auth.ts com `"use server"`: exportar `signOut()` que cria o server client (lib/supabase/server.ts createClient), chama `supabase.auth.signOut()` e faz `redirect("/login")` (import redirect de "next/navigation"). Arquivo NOVO — não editar server actions existentes.

Criar components/layout/app-nav.tsx como Client Component (`"use client"`): recebe prop `links: { href: string; label: string }[]` e renderiza um header horizontal com a marca "BackstageEd.OS" à esquerda e a lista de links + botão Sair à direita. Usar `usePathname` (next/navigation) para destacar o link ativo (ex.: link ativo com classe text-primary/font-medium, inativo text-muted-foreground). Importar `signOut` de "@/lib/actions/auth" e renderizar o botão Sair como `<form action={signOut}>` com um `<Button variant="ghost" size="sm" type="submit">Sair</Button>` (shadcn Button). Client Component pode importar e passar uma Server Action para o action de um form. Usar tokens shadcn/brand existentes (border-b, bg-background, text-primary) — nada de cores hardcoded. Strings em português.

Criar app/pm/layout.tsx (Server Component) que renderiza `<AppNav links={[{href:"/pm/clients",label:"Clientes"},{href:"/pm/chat",label:"Chat"}]} />` seguido de `{children}` dentro de `<div className="flex min-h-full flex-1 flex-col">`. O link para /pm/chat é requisito explícito.

Criar app/admin/layout.tsx (Server Component) equivalente com `links={[{href:"/admin/clients",label:"Clientes"},{href:"/admin/approvals",label:"Aprovações"}]}`.

Não modificar app/pm/page.tsx nem app/admin/page.tsx.
  </action>
  <verify>
    <automated>grep -q 'signOut' lib/actions/auth.ts && grep -q 'redirect("/login")' lib/actions/auth.ts && grep -q 'usePathname' components/layout/app-nav.tsx && grep -q '/pm/chat' app/pm/layout.tsx && grep -q '/admin/approvals' app/admin/layout.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>signOut server action existe (signOut + redirect /login); AppNav renderiza links + form de logout com destaque de rota ativa; layout PM tem link /pm/chat e /pm/clients; layout Admin tem /admin/clients e /admin/approvals; typecheck passa.</done>
</task>

<task type="auto">
  <name>Task 3: Link no login + pagina/acao /forgot-password (anti-enumeracao)</name>
  <files>app/(auth)/login/page.tsx, app/(auth)/forgot-password/page.tsx, app/(auth)/forgot-password/actions.ts</files>
  <action>
Em app/(auth)/login/page.tsx, adicionar um link "Esqueceu sua senha?" apontando para /forgot-password. Colocar dentro do CardContent, logo abaixo do `<form>` (usar `Link` de "next/link", classe text-sm text-muted-foreground hover:underline, centralizado). Não mudar a lógica de signIn nem os handlers existentes.

Criar app/(auth)/forgot-password/actions.ts com `"use server"`: exportar `requestPasswordReset(formData): Promise<{ success: true }>` (SEMPRE retorna sucesso — anti-enumeração). Validar o e-mail com um zod `z.string().trim().email()` inline (ou reaproveitar loginSchema.pick só do email se preferir); se inválido, ainda retornar `{ success: true }` genérico (não vazar). Ler o `origin` via `headers()` de "next/headers" (`(await headers()).get("origin")`) — SEM fallback para env var. Next.js Server Actions enviam de forma confiável o header Origin em POSTs same-origin, então o origin sempre estará presente neste caminho; NÃO introduzir `process.env.NEXT_PUBLIC_SITE_URL` nem outra env var (essa variável não existe no projeto e produziria uma redirectTo quebrada "undefined/auth/callback"). Se por qualquer motivo o origin vier null, ainda retornar `{ success: true }` genérico sem chamar o Supabase (nunca montar redirectTo com origin ausente). Com origin presente, criar o server client e chamar `supabase.auth.resetPasswordForEmail(email, { redirectTo: `${origin}/auth/callback?next=/reset-password` })`. Ignorar qualquer erro do Supabase (não propagar) e retornar `{ success: true }` sempre. Mensagem de sucesso genérica é responsabilidade da página.

Criar app/(auth)/forgot-password/page.tsx como Client Component espelhando o padrão CENTRADO de login/page.tsx (div flex flex-1 items-center justify-center p-4 + Card max-w-sm, cabeçalho da marca opcional igual login). Um campo de e-mail + botão "Enviar link de recuperação". Ao submeter (useTransition), chamar `requestPasswordReset`; ao voltar, sempre mostrar um estado de sucesso inline: "Se existir uma conta com esse e-mail, enviamos um link para redefinir a senha." (NUNCA confirmar/negar existência). Incluir um link de volta para /login. Strings em português.
  </action>
  <verify>
    <automated>grep -q '/forgot-password' app/(auth)/login/page.tsx && grep -q 'resetPasswordForEmail' app/(auth)/forgot-password/actions.ts && grep -q 'auth/callback' app/(auth)/forgot-password/actions.ts && grep -q 'success: true' app/(auth)/forgot-password/actions.ts && ! grep -q 'NEXT_PUBLIC_SITE_URL' app/(auth)/forgot-password/actions.ts && grep -qi 'Se existir uma conta' app/(auth)/forgot-password/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Login tem link para /forgot-password; a action chama resetPasswordForEmail com redirectTo /auth/callback?next=/reset-password derivado de headers().get("origin") (sem env var / sem fallback quebrado) e retorna sempre sucesso; a página mostra mensagem genérica anti-enumeração; typecheck passa.</done>
</task>

<task type="auto">
  <name>Task 4: Route Handler de callback + pagina/acao /reset-password</name>
  <files>app/auth/callback/route.ts, app/(auth)/reset-password/page.tsx, app/(auth)/reset-password/actions.ts</files>
  <action>
Criar app/auth/callback/route.ts (Route Handler, `export async function GET(request: Request)`): ler `code` e `next` de `new URL(request.url).searchParams` (default next = "/reset-password"). Criar o server client (lib/supabase/server.ts createClient — em Route Handler ele consegue setar os cookies da sessão). Se houver `code`, chamar `await supabase.auth.exchangeCodeForSession(code)`. Em sucesso, `NextResponse.redirect(new URL(next, request.url))`; em erro ou sem code, redirecionar para `/login?error=reset` (ou /forgot-password) com mensagem. Importar NextResponse de "next/server". Este handler troca o code PKCE do e-mail por uma sessão de recuperação e é acessível deslogado (entra em PUBLIC_PATHS na Task 5).

Criar app/(auth)/reset-password/actions.ts com `"use server"`: exportar `updatePassword(formData): Promise<{ success: true } | { error: string }>`. Validar com `changePasswordSchema` de "@/lib/validation/client-access" (password + confirmPassword). Criar o server client; confirmar a sessão de recuperação via `supabase.auth.getUser()` (se não houver user → `{ error: "Sessão de recuperação inválida ou expirada." }`). Chamar `supabase.auth.updateUser({ password })`; em erro → erro genérico. Em sucesso, chamar `await supabase.auth.signOut()` (não deixar o usuário meio-logado por esse caminho) e retornar `{ success: true }`.

Criar app/(auth)/reset-password/page.tsx como Client Component espelhando change-password/page.tsx (padrão CENTRADO, Card max-w-sm, campos "Nova senha" + "Confirmar nova senha", validação client-side min 8 + iguais, useTransition). Ao sucesso: `toast.success("Senha redefinida com sucesso. Faça login com a nova senha.")` e `router.push("/login")` (a sessão já foi encerrada pela action). Strings em português.
  </action>
  <verify>
    <automated>grep -q 'exchangeCodeForSession' app/auth/callback/route.ts && grep -q 'updateUser' app/(auth)/reset-password/actions.ts && grep -q 'signOut' app/(auth)/reset-password/actions.ts && grep -q 'changePasswordSchema' app/(auth)/reset-password/actions.ts && grep -q 'router.push("/login")' app/(auth)/reset-password/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Callback troca code por sessão (exchangeCodeForSession → redirect next); reset action valida com changePasswordSchema, faz updateUser + signOut; página troca senha e volta para /login; typecheck passa.</done>
</task>

<task type="auto">
  <name>Task 5: Adicionar novas rotas publicas ao middleware (allowlist precisa)</name>
  <files>middleware.ts</files>
  <action>
Editar SOMENTE o array PUBLIC_PATHS em middleware.ts, adicionando exatamente três entradas: "/forgot-password", "/reset-password", "/auth/callback". Não alterar `isPublicPath`, o gate pending/rejected/deactivated, o gate must_change_password, o role-routing, nem o `config.matcher`. Racional de segurança: `isPublicPath` só afeta o branch `!user` (usuário deslogado clicando no link do e-mail precisa alcançar /auth/callback e /forgot-password sem sessão). Para as DEMAIS rotas nada muda — pending/rejected/must_change_password/role-routing continuam valendo. /reset-password normalmente é acessado já com a sessão de recuperação (então nem depende do allowlist), mas entra por robustez.
  </action>
  <verify>
    <automated>grep -q '"/forgot-password"' middleware.ts && grep -q '"/reset-password"' middleware.ts && grep -q '"/auth/callback"' middleware.ts && grep -q '"/login"' middleware.ts && grep -q '"/change-password"' middleware.ts && test "$(grep -c 'roleRoot\|must_change_password\|isPublicPath' middleware.ts)" -ge "3" && npx tsc --noEmit</automated>
  </verify>
  <done>PUBLIC_PATHS contém as 3 novas rotas mais as 5 originais; gates pending/rejected/must_change_password/role-routing intactos; typecheck passa.</done>
</task>

<task type="auto">
  <name>Task 6: Scope gate — confirmar que nada fora do escopo foi tocado</name>
  <files>(verificação — nenhum arquivo modificado)</files>
  <action>
Rodar um diff de escopo confirmando que apenas os arquivos previstos neste plano foram alterados. NÃO devem aparecer no diff: qualquer coisa em supabase/migrations/ (RLS/migrations), lib/validation/*.ts (schemas existentes — reutilizados, não editados), app/pm/clients/[id]/access/actions.ts, o client-detail-form, nem app/pm/chat/actions.ts / app/admin/approvals/actions.ts (Server Actions não relacionadas). Se qualquer arquivo fora da lista files_modified aparecer, parar e reportar.
  </action>
  <verify>
    <automated>CHANGED=$(git status --porcelain | awk '{print $2}'); echo "$CHANGED"; test -z "$(echo "$CHANGED" | grep -E 'supabase/migrations/|lib/validation/|clients/\[id\]/access/actions|chat/actions|approvals/actions|client-detail-form')" && echo SCOPE_OK</automated>
  </verify>
  <done>git status não mostra alterações em migrations, RLS, schemas de validação, nem nas Server Actions de access/chat/approvals/detail-form. Apenas os arquivos de files_modified aparecem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador deslogado → /forgot-password | entrada de e-mail não autenticada |
| link de e-mail → /auth/callback | code PKCE de recuperação cruza aqui |
| navegador → middleware | allowlist PUBLIC_PATHS decide acesso pré-auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-eb7-01 | Information Disclosure | /forgot-password | mitigate | Server Action retorna SEMPRE sucesso genérico; nunca revela se o e-mail existe (anti-enumeração), espelhando o padrão de login/actions.ts |
| T-eb7-02 | Elevation of Privilege | /reset-password | mitigate (com risco residual aceito) | Após updateUser BEM-SUCEDIDO, chamar signOut imediatamente encerra a sessão de recuperação no caminho de conclusão — força login limpo. RISCO RESIDUAL ACEITO: entre o redirect de /auth/callback e a conclusão (ou abandono/falha) do form de reset, a sessão de recuperação PKCE é uma sessão Supabase comum com acesso normal por papel. Isso é comportamento herdado do fluxo de recovery do Supabase, NÃO introduzido por este plano, e é aceito como tradeoff documentado do Supabase; restringir por tipo de sessão/AMR está fora do escopo deste quick fix. |
| T-eb7-03 | Elevation of Privilege | middleware PUBLIC_PATHS | mitigate | Adicionar somente 3 rotas específicas ao allowlist; gates pending/rejected/must_change_password/role-routing permanecem para todas as outras rotas (verificado na Task 5) |
| T-eb7-04 | Spoofing | /auth/callback | accept | exchangeCodeForSession valida o code PKCE emitido pelo próprio Supabase; code inválido/expirado → redirect para /login sem sessão |
</threat_model>

<verification>
- `npx tsc --noEmit` passa (typecheck global).
- Nenhum href relativo "./" restante em admin/clients e pm/clients (Task 1 grep).
- AppNav renderiza links + logout; layouts PM/Admin com os links exigidos (inclui /pm/chat).
- Fluxo de reset: login → /forgot-password (sucesso genérico) → /auth/callback (exchangeCodeForSession) → /reset-password (updateUser + signOut) → /login.
- middleware PUBLIC_PATHS = 5 originais + 3 novas; gates intactos.
- Scope gate (Task 6): nenhuma migration/RLS/validation/Server Action não relacionada no diff.
</verification>

<success_criteria>
- Botões "Criar cliente" e clique em cliente levam às rotas corretas em ambas as áreas.
- PM e Admin têm nav persistente por clique, com acesso a /pm/chat e logout funcional.
- Usuário deslogado consegue solicitar reset, receber o link, definir nova senha e voltar ao login.
- Nenhuma alteração em RLS, migrations ou Server Actions fora do escopo.
- Todas as strings de interface em português.
</success_criteria>

<output>
Create `.planning/quick/260722-eb7-corrigir-navegacao-quebrada-links-relati/260722-eb7-SUMMARY.md` when done
</output>
</content>
</invoke>
