---
phase: quick
plan: 260716-au8
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [AUTH-06, AUTH-07, AUTH-08]
files_modified:
  - supabase/migrations/0008_clients_grants.sql

must_haves:
  truths:
    - "A new migration grants SELECT/INSERT/UPDATE on public.clients to the authenticated role"
    - "npx supabase test db has been re-run locally after applying the grant, and the quick task's SUMMARY.md records the full verbatim output plus an explicit pass/fail statement for AUTH-06/AUTH-07/AUTH-08"
  artifacts:
    - path: "supabase/migrations/0008_clients_grants.sql"
      provides: "GRANT SELECT/INSERT/UPDATE on public.clients to authenticated"
      contains: "grant select, insert, update on public.clients to authenticated"
  key_links:
    - from: "supabase/migrations/0008_clients_grants.sql"
      to: "supabase/tests/0001_rls_pm_scoping_test.sql, 0002_rls_client_scoping_test.sql, 0003_rls_admin_unrestricted_test.sql"
      via: "authenticated role base-table privilege required before clients_select_scoped/clients_insert_admin_or_pm/clients_update_scoped RLS policies are ever evaluated"
      pattern: "grant select, insert, update on public.clients to authenticated"
---

<objective>
Close the blocker 05-06-SUMMARY.md diagnosed: `npx supabase test db` fails all three RLS requirement test files (0001/0002/0003) with `permission denied for table clients`, because no migration in this repo ever issues a `GRANT` on `public.clients` to the `authenticated` role — a gap independent of the (correct) RLS policies defined in `0004_rls_policies.sql` / `0007_clients_rls_fix.sql`. Confirmed by grep: zero `GRANT` statements exist across all 7 committed migrations. `supabase/config.toml`'s `[api] auto_expose_new_tables` comment confirms the underlying cause — local Supabase now defaults new public-schema tables to NOT auto-exposed to API roles, matching the cloud default, so an explicit `GRANT` is required.

Add a new migration `0008_clients_grants.sql` issuing `grant select, insert, update on public.clients to authenticated;` (RLS policies already correctly scope which rows are visible/writable — only the base table privilege is missing), then apply it locally and re-run `npx supabase test db`, recording the full verbatim result.

Purpose: Unblock runtime verification of AUTH-06 (PM scoped to assigned clients), AUTH-07 (Client scoped to own data), and AUTH-08 (Admin unrestricted) — all three are "policy correct by static review" but currently runtime-blocked one layer beneath RLS.
Output: `supabase/migrations/0008_clients_grants.sql` (new file, existing migrations untouched) + this quick task's SUMMARY.md recording the verbatim `npx supabase test db` output and an explicit pass/fail statement for AUTH-06/07/08.

Docker is confirmed available in this environment (`docker info` succeeds per orchestrator context) — run all commands directly, do not defer to a human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-access-roles/05-06-SUMMARY.md
@supabase/migrations/0002_clients_stub.sql
@supabase/migrations/0004_rls_policies.sql
@supabase/migrations/0007_clients_rls_fix.sql
@supabase/config.toml

<interfaces>
RLS policies already in place on public.clients that this GRANT must NOT duplicate or contradict (from 0004_rls_policies.sql / 0007_clients_rls_fix.sql — do not touch these, only add the table-level GRANT beneath them):
- `clients_select_scoped` (for select, to authenticated) — admin sees all, PM sees pm_assigned_clients(), client sees own client_id
- `clients_insert_admin_or_pm` (for insert, to authenticated) — admin or PM
- `clients_update_scoped` (for update, to authenticated) — admin or PM's assigned clients

Confirmed root cause chain (05-06-SUMMARY.md, verbatim error at test line 28 of 0001_rls_pm_scoping_test.sql):
`ERROR: permission denied for table clients` / `HINT: Grant the required privileges to the current role with: GRANT SELECT ON public.clients TO authenticated;` — this fires inside `tests.set_auth()`-scoped `results_eq()` calls, before RLS policy evaluation, in all three of 0001/0002/0003_*_test.sql.

`grep -n -i "grant" supabase/migrations/*.sql` returns zero matches across all 7 existing migrations — confirmed no prior migration ever grants base privileges on `public.clients` (unlike what might be assumed from `public.profiles`/`public.pm_clients`, which never hit this error only because `rls_helpers.sql`'s fixture bootstrap touches those two tables while running as the session-owner role, not as `authenticated`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 0008_clients_grants.sql migration</name>
  <files>supabase/migrations/0008_clients_grants.sql</files>
  <action>
    Create a new migration file `supabase/migrations/0008_clients_grants.sql` (next sequential number after 0007). It must contain exactly one functional statement: `grant select, insert, update on public.clients to authenticated;`. Precede it with a short header comment explaining this is a Phase 5 follow-up (quick task 260716-au8) closing the gap 05-06-SUMMARY.md diagnosed — RLS policies on `public.clients` (`clients_select_scoped`, `clients_insert_admin_or_pm`, `clients_update_scoped`) were already correct, but no migration ever granted the underlying table-level privileges to `authenticated`, so Postgres rejected every query before RLS was even evaluated. Reference `supabase/config.toml`'s `[api] auto_expose_new_tables` note (new public-schema tables are NOT auto-exposed to API roles by default) as the underlying cause. Do NOT modify any of the existing `supabase/migrations/0001..0007*.sql` files. Do NOT touch any file under `supabase/tests/`. Do NOT grant `delete` (delete on clients is out of scope for this phase per 0004/0007's policies, which define no delete policy on clients).
  </action>
  <verify>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && test -f supabase/migrations/0008_clients_grants.sql && grep -qi 'grant select, *insert, *update on public.clients to authenticated' supabase/migrations/0008_clients_grants.sql && ! git diff --name-only -- supabase/migrations/0001_profiles.sql supabase/migrations/0002_clients_stub.sql supabase/migrations/0003_pm_clients.sql supabase/migrations/0004_rls_policies.sql supabase/migrations/0005_fix_handle_new_user_status_cast.sql supabase/migrations/0006_clients_full_record.sql supabase/migrations/0007_clients_rls_fix.sql | grep -q . && echo PASS</automated>
  </verify>
  <done>supabase/migrations/0008_clients_grants.sql exists, contains the exact GRANT statement, and no existing migration file (0001-0007) or any file under supabase/tests/ was modified. Grep/git-diff verify prints PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the migration locally and re-run the pgTAP suite</name>
  <files>.planning/quick/260716-au8-add-missing-grant-select-insert-update-o/260716-au8-SUMMARY.md</files>
  <action>
    Docker is confirmed available — run these directly, do not stop for a human checkpoint:
    1. Run `docker info` to confirm the daemon is reachable.
    2. Run `npx supabase start` (idempotent — starts the local stack if not already running).
    3. Run `npx supabase db reset` to drop and recreate the local database, replaying all migrations 0001 through the new 0008 from scratch (this is the project's convention for picking up a newly added migration against an already-running local stack, per `supabase/config.toml`'s `[db.migrations]`/`[db.seed]` reset-time behavior).
    4. Run `npx supabase test db`, capturing the full verbatim stdout/stderr (e.g. via `tee`) into a scratch file, AND separately capture the true exit code (tee through a pipe can mask the real exit status — either check `${PIPESTATUS[0]}` or re-run the bare command once more to confirm the exit code, matching the approach 05-06-SUMMARY.md used).
    5. Run `npx supabase stop` to clean up afterward.
    6. Write `.planning/quick/260716-au8-add-missing-grant-select-insert-update-o/260716-au8-SUMMARY.md` including: the full verbatim `npx supabase test db` output (all lines, not truncated or paraphrased), the confirmed exit code, and an explicit, unambiguous statement of whether AUTH-06/AUTH-07/AUTH-08 now pass (all `ok` lines, zero `not ok`, `Result: PASS`, exit 0) or whether a new/different failure appears (quote it verbatim, and do not attempt any further schema change beyond the 0008 migration already added in Task 1 — if the suite still fails, mark this quick task's outcome as still-blocked and stop there). If the suite passes clean, explicitly note in the SUMMARY that Phase 5's `05-06-SUMMARY.md` and `ROADMAP.md` status will need a follow-up update reflecting AUTH-06/07/08 as runtime-verified — but do NOT edit ROADMAP.md yourself; quick tasks never touch ROADMAP.md per project convention, so leave that for a human or a later phase-tracking action.
  </action>
  <verify>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && test -f .planning/quick/260716-au8-add-missing-grant-select-insert-update-o/260716-au8-SUMMARY.md && grep -qi 'supabase test db' .planning/quick/260716-au8-add-missing-grant-select-insert-update-o/260716-au8-SUMMARY.md && grep -qiE 'AUTH-0[678]' .planning/quick/260716-au8-add-missing-grant-select-insert-update-o/260716-au8-SUMMARY.md && echo PASS</automated>
  </verify>
  <done>Local Supabase stack started, `db reset` applied all migrations including 0008, `npx supabase test db` was run to completion (regardless of pass/fail outcome), and the full verbatim output plus an explicit pass/fail statement for AUTH-06/07/08 is recorded in 260716-au8-SUMMARY.md. Grep verify prints PASS.</done>
</task>

</tasks>

<verification>
- `supabase/migrations/0008_clients_grants.sql` exists, contains `grant select, insert, update on public.clients to authenticated;`, and no existing migration (0001-0007) or any `supabase/tests/*.sql` file was touched.
- `npx supabase test db` was actually executed locally (Docker confirmed available) after a `db reset` picked up the new migration.
- The quick task's SUMMARY.md contains the full verbatim `npx supabase test db` output and states explicitly, unambiguously, whether AUTH-06/AUTH-07/AUTH-08 now pass or whether a new/different failure remains.
- If still failing: no further schema change was attempted beyond the single 0008 grants migration; outcome recorded as still-blocked.
- If passing: SUMMARY.md notes the follow-up needed to Phase 5's 05-06-SUMMARY.md / ROADMAP.md status, without this quick task editing ROADMAP.md itself.
</verification>

<success_criteria>
- New migration `0008_clients_grants.sql` grants SELECT/INSERT/UPDATE on `public.clients` to `authenticated`, additive only (no existing migration or test file modified).
- `npx supabase test db` re-run to completion locally, full verbatim output captured in the quick task's SUMMARY.md.
- AUTH-06/AUTH-07/AUTH-08 status (pass or still-blocked-with-verbatim-new-failure) stated explicitly and unambiguously.
</success_criteria>

<output>
Create `.planning/quick/260716-au8-add-missing-grant-select-insert-update-o/260716-au8-SUMMARY.md` when done. Include the full verbatim `npx supabase test db` output, the confirmed exit code, and an explicit AUTH-06/07/08 pass/fail statement. If the suite passes clean, note that Phase 5's 05-06-SUMMARY.md / ROADMAP.md status needs a human or later phase-tracking follow-up update (this quick task does not edit ROADMAP.md itself, per project convention).
</output>
