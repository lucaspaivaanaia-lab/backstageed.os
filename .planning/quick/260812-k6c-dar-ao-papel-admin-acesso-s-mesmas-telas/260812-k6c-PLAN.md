---
phase: quick/260812-k6c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - middleware.ts
  - app/pm/board/page.tsx
  - app/pm/board/board-panel.tsx
  - app/pm/chat/page.tsx
  - app/pm/chat/chat-panel.tsx
  - app/admin/layout.tsx
autonomous: false
requirements: [QUICK-260812-k6c]

must_haves:
  truths:
    - "Um Admin visitando /pm/board ou /pm/chat (link direto ou digitando a URL) NAO e redirecionado de volta para /admin -- o middleware libera as duas rotas."
    - "Um Admin sem cliente ativo (primeira visita, localStorage vazio) que abre /pm/board e redirecionado para /admin/clients -- nunca para /pm/clients (que continua bloqueado) e nunca de volta para /admin em silencio."
    - "O mesmo fallback de primeira-visita funciona em /pm/chat para Admin -- redireciona para /admin/clients, nao para /pm/clients."
    - "Um Admin com cliente ativo em /pm/board que clica 'Editar briefing' vai para /admin/clients/{id}, nao /pm/clients/{id}."
    - "O sidebar do Admin (/admin/*) mostra exatamente 1 item novo, 'Produção', apontando para /pm/board -- nenhum item 'Chat' separado foi adicionado."
    - "A experiencia de um PM em /pm/board, /pm/chat, /pm/clients e /pm/editors fica byte-identica -- mesmos alvos de redirect, mesmo comportamento de middleware para o papel pm."
    - "/pm/clients e /pm/editors continuam bloqueados para Admin pelo middleware -- somente /pm/board e /pm/chat passam a ser permitidos."
    - "O redirect pos-'Salvar briefing' de client-detail-form.tsx (ja role-agnostic, aponta pra /pm/board?client=...) funciona pra Admin sem nenhuma mudanca nesse arquivo."
  artifacts:
    - path: "middleware.ts"
      provides: "Mapa extraAllowedPrefixes (role -> prefixos extras permitidos) checado ANTES do redirect cross-role existente, hoje com uma unica entrada: admin -> [/pm/board, /pm/chat]"
      contains: "extraAllowedPrefixes"
    - path: "app/pm/board/page.tsx"
      provides: "viewerIsAdmin resolvido server-side (profiles.role) e passado como prop pro BoardPanel"
      contains: "viewerIsAdmin"
    - path: "app/pm/board/board-panel.tsx"
      provides: "viewerIsAdmin na BoardPanelProps; os dois alvos hardcoded /pm/clients (fallback sem cliente ativo e botao 'Editar briefing') viram role-aware"
      contains: "viewerIsAdmin ? \"/admin/clients\""
    - path: "app/pm/chat/page.tsx"
      provides: "viewerIsAdmin resolvido server-side (profiles.role) e passado como prop pro ChatPanel"
      contains: "viewerIsAdmin"
    - path: "app/pm/chat/chat-panel.tsx"
      provides: "viewerIsAdmin na ChatPanelProps; o fallback hardcoded /pm/clients vira role-aware"
      contains: "viewerIsAdmin ? \"/admin/clients\""
    - path: "app/admin/layout.tsx"
      provides: "Novo item de nav 'Produção' -> /pm/board no sidebar do Admin"
      contains: "/pm/board"
  key_links:
    - from: "middleware.ts (extraAllowedPrefixes)"
      to: "middleware.ts (isAllowedExtra guard antes do redirect ownRoot)"
      via: "pathname === prefix || pathname.startsWith(prefix + \"/\")"
      pattern: "isAllowedExtra"
    - from: "app/pm/board/page.tsx (viewerIsAdmin)"
      to: "app/pm/board/board-panel.tsx (BoardPanel viewerIsAdmin prop)"
      via: "prop passada no JSX <BoardPanel ... viewerIsAdmin={viewerIsAdmin} />"
      pattern: "viewerIsAdmin={viewerIsAdmin}"
    - from: "app/pm/chat/page.tsx (viewerIsAdmin)"
      to: "app/pm/chat/chat-panel.tsx (ChatPanel viewerIsAdmin prop)"
      via: "prop passada no JSX <ChatPanel clients={roster} viewerIsAdmin={viewerIsAdmin} />"
      pattern: "viewerIsAdmin={viewerIsAdmin}"
    - from: "app/admin/layout.tsx (nav item Produção)"
      to: "middleware.ts (extraAllowedPrefixes.admin)"
      via: "href /pm/board so navega sem redirect de volta porque o middleware ja libera esse prefixo"
      pattern: "/pm/board"
---

<objective>
Dar ao papel Admin acesso as MESMAS telas de Produção (`/pm/board`) e Chat (`/pm/chat`) que o PM ja tem -- sem duplicar em `/admin/board`/`/admin/chat` -- para destravar o fluxo criar-cliente -> briefing -> produção quando quem esta logado e um Admin. Hoje o middleware bloqueia Admin em qualquer rota `/pm/*`, incluindo essas duas, e o redirect pos-"Salvar briefing" de `client-detail-form.tsx` (que ja aponta pra `/pm/board?client=...` pra QUALQUER papel) bate nesse bloqueio.

Nenhuma mudança de RLS/Server Action e necessaria -- confirmado pela pesquisa (`260812-k6c-RESEARCH.md`) que toda a superficie de dados de board/chat ja trata Admin corretamente via `assertPmOrAdminCaller`/`isBoardWriteAuthorized`/RLS admin-unrestricted (`0003_rls_admin_unrestricted_test.sql`). O UNICO boundary alterado e o middleware, mais 3 alvos de navegação client-side hardcoded pra `/pm/clients` que, sem correção, bateriam num redirect ainda corretamente bloqueado pra Admin e devolveriam o Admin em silencio pra `/admin` -- a `RESEARCH.md` Finding 2a, o achado mais importante desta pesquisa.

Purpose: um Admin logado precisa conseguir acompanhar/produzir conteudo pelas mesmas telas que o PM usa, sem que `/pm/clients`/`/pm/editors` (que continuam exclusivos do PM, Admin ja tem `/admin/clients`/`/admin/editors` proprios) fiquem acessiveis por engano.

Output: `middleware.ts` com um allow-list aditivo (`extraAllowedPrefixes`) restrito a `/pm/board`+`/pm/chat` pro papel admin; `board-panel.tsx`/`chat-panel.tsx` com os 3 alvos de navegação role-aware via uma prop `viewerIsAdmin` (mesmo padrão ja usado em `client-detail-form.tsx`); `app/admin/layout.tsx` com exatamente 1 item de nav novo ("Produção" -> `/pm/board`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/quick/260812-k6c-dar-ao-papel-admin-acesso-s-mesmas-telas/260812-k6c-RESEARCH.md
@middleware.ts
@app/pm/board/page.tsx
@app/pm/board/board-panel.tsx
@app/pm/chat/page.tsx
@app/pm/chat/chat-panel.tsx
@app/admin/layout.tsx
@components/clients/client-detail-form.tsx

<baseline>
- `middleware.ts`'s `roleRoot` gate (linhas 64-80 hoje) redireciona qualquer papel pra sua propria raiz sempre que a rota bate em `pathname === "/"` ou no root de OUTRO papel. Admin (`ownRoot = "/admin"`) tem `/pm`, `/client`, `/editor` como `otherRoots` -- `/pm/board` e `/pm/chat` caem dentro de `/pm` e sao bloqueados hoje.
- `client-detail-form.tsx` ja recebe e usa `viewerIsAdmin: boolean` (prop existente, resolvida no RSC loader via `profile?.role === "admin"`) para gatilhar UI role-aware (remover PM, botao "Excluir cliente"). Este plano aplica o MESMO padrao -- resolver `viewerIsAdmin` no `page.tsx` (RSC) e passar como prop pro Client Component -- em `board-panel.tsx`/`chat-panel.tsx`, que hoje NAO recebem nenhuma prop de papel.
- `app/pm/clients/[id]/page.tsx` (linhas 26-39) e o exemplo exato do padrao de resolucao server-side a reaproveitar: `const { data: { user } } = await supabase.auth.getUser();` seguido de uma leitura em `profiles` por `role`, dentro do MESMO `Promise.all` que ja busca os outros dados da pagina.
- `board-panel.tsx`/`chat-panel.tsx` tem exatamente 3 alvos hardcoded pra `/pm/clients` (RESEARCH.md Finding 2a): `board-panel.tsx:~1987` (`router.replace("/pm/clients")`, dispara quando nao ha `?client=` na URL nem `lastSelectedClientId` no localStorage -- o cenario de PRIMEIRA visita de um Admin), `board-panel.tsx:~2054` (link do botao "Editar briefing"), e `chat-panel.tsx:~162` (`router.push("/pm/clients")`, mesmo fallback de primeira-visita do chat). Sem corrigir os 3, abrir o middleware sozinho faz o Admin ser bounced de volta pra `/admin` em silencio na primeira vez que clicar no novo link do sidebar -- vai parecer que a feature nao funciona.
- Abordagem REJEITADA explicitamente pelo escopo (nao usar): adicionar `/pm/clients` ao allow-list do Admin. Admin ja tem `/admin/clients` como equivalente proprio -- os 3 alvos acima devem apontar pra la, nao pra `/pm/clients`.
</baseline>

<interfaces>
Trechos exatos do codigo atual e do codigo alvo -- usar diretamente, sem re-derivar.

middleware.ts, ANTES do bloco "const ownRoot = roleRoot[profile.role];" (linha 70 hoje):

    // Allow-list aditivo, fechado e explicito -- NAO um segundo roleRoot. Cada
    // prefixo aqui e uma sub-arvore de rota completa (proprio page.tsx + proprias
    // actions.ts), nunca um prefixo parcial/ambiguo (ex.: nunca "/pm/c", que
    // tambem bateria em "/pm/clients"). Quick task 260812-k6c: Admin ganha
    // acesso as MESMAS telas de Produção/Chat que o PM ja tem, sem abrir
    // /pm/clients nem /pm/editors (Admin ja tem /admin/clients, /admin/editors
    // proprios).
    const extraAllowedPrefixes: Record<string, string[]> = {
      admin: ["/pm/board", "/pm/chat"],
    };

    const ownRoot = roleRoot[profile.role];

    if (ownRoot) {
      const otherRoots = Object.values(roleRoot).filter((r) => r !== ownRoot);
      const allowedExtras = extraAllowedPrefixes[profile.role] ?? [];
      const isAllowedExtra = allowedExtras.some(
        (prefix) => pathname === prefix || pathname.startsWith(`${prefix}/`)
      );
      if (
        !isAllowedExtra &&
        (pathname === "/" ||
          otherRoots.some((root) => pathname === root || pathname.startsWith(`${root}/`)))
      ) {
        return NextResponse.redirect(new URL(ownRoot, request.url));
      }
    }

Isso substitui o bloco "if (ownRoot) { ... }" inteiro (linhas 72-80 hoje) -- todo papel sem entrada em extraAllowedPrefixes (pm, client, editor) tem allowedExtras = [], entao isAllowedExtra e sempre false pra eles e o comportamento fica byte-identico ao atual.

app/pm/board/page.tsx -- resolver viewerIsAdmin, mesmo padrao de app/pm/clients/[id]/page.tsx:

Logo apos "const supabase = await createClient();" (linha 108 hoje), adicionar:

    const {
      data: { user },
    } = await supabase.auth.getUser();

No Promise.all existente (linhas 110-139), adicionar UM item novo ao final do array de destructuring E do array de promises:

    const [{ data: clients }, { data: cards }, { data: activeClient }, pmRoster, editorRoster, { data: profile }] =
      await Promise.all([
        // ...os 5 itens existentes, inalterados...
        user
          ? supabase.from("profiles").select("role").eq("id", user.id).single()
          : Promise.resolve({ data: null as { role: string } | null }),
      ]);

    const viewerIsAdmin = profile?.role === "admin";

E no JSX final ("<BoardPanel ... />", linhas 261-273), adicionar "viewerIsAdmin={viewerIsAdmin}" como mais uma prop.

app/pm/chat/page.tsx -- mesmo padrao:

Logo apos "const supabase = await createClient();" (linha 17 hoje), adicionar:

    const {
      data: { user },
    } = await supabase.auth.getUser();

No Promise.all existente (linhas 19-28), adicionar UM item novo:

    const [{ data: clients }, { data: fileRows }, { data: profile }] = await Promise.all([
      // ...os 2 itens existentes, inalterados...
      user
        ? supabase.from("profiles").select("role").eq("id", user.id).single()
        : Promise.resolve({ data: null as { role: string } | null }),
    ]);

    const viewerIsAdmin = profile?.role === "admin";

E no return final: "<ChatPanel clients={roster} viewerIsAdmin={viewerIsAdmin} />".

app/pm/board/board-panel.tsx -- BoardPanelProps (linhas 142-175 hoje), adicionar UM campo no final do type, antes do fechamento:

      // Resolvido server-side (page.tsx) via profiles.role -- usado SOMENTE pra
      // escolher entre /admin/clients e /pm/clients nos dois fallbacks de
      // navegação abaixo (quick task 260812-k6c). Nunca usado pra alterar
      // visibilidade de dados, que continua inteiramente dirigida por RLS.
      viewerIsAdmin: boolean;

Na assinatura de "export function BoardPanel({ ... }: BoardPanelProps)" (linha 1794-1805 hoje), adicionar viewerIsAdmin a lista de props desestruturadas.

Alvo 1 (linha ~1987 hoje, dentro do useEffect de fallback sem cliente ativo):

    ANTES:
    router.replace("/pm/clients");

    DEPOIS:
    router.replace(viewerIsAdmin ? "/admin/clients" : "/pm/clients");

Alvo 2 (linhas ~2053-2058 hoje, botao "Editar briefing"):

    ANTES:
    <Button variant="outline" asChild>
      <Link href={`/pm/clients/${activeClient.id}`}>
        <FileEditIcon className="size-4" />
        Editar briefing
      </Link>
    </Button>

    DEPOIS:
    <Button variant="outline" asChild>
      <Link
        href={
          viewerIsAdmin
            ? `/admin/clients/${activeClient.id}`
            : `/pm/clients/${activeClient.id}`
        }
      >
        <FileEditIcon className="size-4" />
        Editar briefing
      </Link>
    </Button>

app/pm/chat/chat-panel.tsx -- ChatPanelProps (linhas 59-61 hoje):

    type ChatPanelProps = {
      clients: ClientOption[];
      // Resolvido server-side (page.tsx) via profiles.role -- usado SOMENTE pra
      // escolher entre /admin/clients e /pm/clients no fallback de navegação
      // abaixo (quick task 260812-k6c). Nunca usado pra alterar visibilidade de
      // dados, que continua inteiramente dirigida por RLS.
      viewerIsAdmin: boolean;
    };

Na assinatura "export function ChatPanel({ clients }: ChatPanelProps)" (linha 90 hoje), trocar por "export function ChatPanel({ clients, viewerIsAdmin }: ChatPanelProps)".

Alvo 3 (linha ~162 hoje, dentro do useEffect de fallback sem cliente ativo):

    ANTES:
    router.push("/pm/clients");

    DEPOIS:
    router.push(viewerIsAdmin ? "/admin/clients" : "/pm/clients");

app/admin/layout.tsx -- import + item de nav novo, logo apos "Clientes":

    import {
      UsersIcon,
      UserCogIcon,
      ClipboardCheckIcon,
      ListChecksIcon,
      KanbanIcon,
      BookOpenIcon,
      LayoutDashboardIcon,
    } from "lucide-react";

    items={[
      { href: "/admin/clients", label: "Clientes", icon: <UsersIcon /> },
      { href: "/pm/board", label: "Produção", icon: <LayoutDashboardIcon /> },
      { href: "/admin/editors", label: "Editores", icon: <UserCogIcon /> },
      // ...os demais itens existentes (Aprovações, Checklists, Cards,
      // Base de conhecimento), inalterados...
    ]}

Posicionado logo apos "Clientes" (nao no fim da lista) porque o fluxo que este quick task destrava e literalmente criar-cliente -> briefing -> produção -- o item fica ao lado do passo que o precede no fluxo do Admin. Exatamente 1 item novo -- "Chat" continua alcancavel via cross-nav (botao "Chat" de board-panel.tsx, ja liberado pelo middleware assim que /pm/chat entra em extraAllowedPrefixes), espelhando o padrao que o PM ja usa (comentario em app/pm/layout.tsx:18-23).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Middleware -- allow-list aditivo pra Admin em /pm/board e /pm/chat</name>
  <files>middleware.ts</files>
  <action>
    Editar middleware.ts adicionando o mapa extraAllowedPrefixes e o guard isAllowedExtra EXATAMENTE como especificado em interfaces acima, substituindo o bloco "if (ownRoot) { ... }" atual (linhas 72-80). Nao alterar mais nada no arquivo -- roleRoot, o gate pending/rejected/must_change_password, isPublicPath, PUBLIC_PATHS, e config.matcher ficam byte-identicos. extraAllowedPrefixes tem UMA UNICA entrada (admin: ["/pm/board", "/pm/chat"]) -- pm, client, editor nao entram no mapa, entao allowedExtras resolve pra [] (via "?? []") e isAllowedExtra e sempre false pra esses tres papeis, preservando o comportamento atual sem nenhuma mudanca de codigo pra eles. O boundary check usa a mesma forma exata "pathname === prefix || pathname.startsWith(`${prefix}/`)" que o arquivo ja usa pra otherRoots, fechado o suficiente pra nunca casar /pm/clients ou /pm/board-something por engano (Pitfall 2 da pesquisa).
  </action>
  <verify>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && grep -c "extraAllowedPrefixes" middleware.ts</automated>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && grep -q 'admin: \["/pm/board", "/pm/chat"\]' middleware.ts && echo OK</automated>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && grep -c "isAllowedExtra" middleware.ts</automated>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && npx tsc --noEmit 2>&1 | tail -30</automated>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && npx eslint middleware.ts 2>&1 | tail -30</automated>
  </verify>
  <done>extraAllowedPrefixes existe com exatamente a entrada admin: ["/pm/board", "/pm/chat"]; o guard isAllowedExtra esta posicionado ANTES do redirect cross-role existente; roleRoot/otherRoots/demais gates do arquivo inalterados; tsc --noEmit e eslint limpos.</done>
</task>

<task type="auto">
  <name>Task 2: Navegação role-aware em board/chat (Finding 2a) + item de nav no sidebar Admin</name>
  <files>app/pm/board/page.tsx, app/pm/board/board-panel.tsx, app/pm/chat/page.tsx, app/pm/chat/chat-panel.tsx, app/admin/layout.tsx</files>
  <action>
    Depende da Task 1 (o middleware precisa liberar /pm/board e /pm/chat pro Admin pra este trabalho ter efeito pratico -- mas este task compila e passa em tsc/eslint de forma independente).

    Em app/pm/board/page.tsx e app/pm/chat/page.tsx: resolver viewerIsAdmin server-side EXATAMENTE como especificado em interfaces acima (mesmo padrao ja usado em app/pm/clients/[id]/page.tsx, linhas 26-39: supabase.auth.getUser() seguido de uma leitura em profiles.role, dobrada dentro do Promise.all ja existente de cada arquivo). Passar viewerIsAdmin={viewerIsAdmin} como prop nova pro BoardPanel/ChatPanel respectivamente. Nenhuma outra query, campo ou linha desses dois arquivos muda.

    Em app/pm/board/board-panel.tsx: adicionar "viewerIsAdmin: boolean" a BoardPanelProps (com o comentario dado em interfaces) e desestruturar em "BoardPanel({ ..., viewerIsAdmin }: BoardPanelProps)". Corrigir os DOIS alvos hardcoded /pm/clients (Finding 2a #1 e #2) EXATAMENTE como especificado em interfaces -- o router.replace(...) do fallback sem cliente ativo (~linha 1987) e o href do botao "Editar briefing" (~linha 2054). Nenhum outro router.push/router.replace/Link href deste arquivo muda.

    Em app/pm/chat/chat-panel.tsx: adicionar "viewerIsAdmin: boolean" a ChatPanelProps e desestruturar em "ChatPanel({ clients, viewerIsAdmin }: ChatPanelProps)". Corrigir o UNICO alvo hardcoded /pm/clients (Finding 2a #3) EXATAMENTE como especificado em interfaces -- o router.push(...) do fallback sem cliente ativo (~linha 162). O botao "Produção" (cross-nav de volta pro board, ~linha 435) ja aponta pra /pm/board?client=..., que fica igualmente acessivel pra PM e Admin -- NAO muda.

    Em app/admin/layout.tsx: importar LayoutDashboardIcon de lucide-react e adicionar exatamente 1 item novo ({ href: "/pm/board", label: "Produção", icon: <LayoutDashboardIcon /> }) logo apos o item "Clientes" e antes de "Editores", EXATAMENTE como especificado em interfaces. Nao adicionar um segundo item "Chat" -- o cross-nav interno de board-panel.tsx/chat-panel.tsx ja cobre esse caminho pro Admin assim que a Task 1 libera /pm/chat, espelhando o padrao ja estabelecido no sidebar do PM (app/pm/layout.tsx:18-23, comentario "Navigation-flow correction 2026-08-05"). Nenhum outro item, ordem ou icone dos existentes muda.
  </action>
  <verify>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && npx tsc --noEmit 2>&1 | tail -60</automated>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && npx eslint app/pm/board/page.tsx app/pm/board/board-panel.tsx app/pm/chat/page.tsx app/pm/chat/chat-panel.tsx app/admin/layout.tsx 2>&1 | tail -60</automated>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && grep -c "viewerIsAdmin" app/pm/board/page.tsx app/pm/board/board-panel.tsx app/pm/chat/page.tsx app/pm/chat/chat-panel.tsx</automated>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && grep -c 'viewerIsAdmin ? "/admin/clients"' app/pm/board/board-panel.tsx app/pm/chat/chat-panel.tsx</automated>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && grep -q '/admin/clients/\${activeClient.id}' app/pm/board/board-panel.tsx && echo OK</automated>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && grep -q '"/pm/board", label: "Produção"' app/admin/layout.tsx && echo OK</automated>
    <automated>cd /Users/lucaspaiva/projects/backstageed.OS && npm run build 2>&1 | tail -40</automated>
  </verify>
  <done>viewerIsAdmin resolvido server-side em ambos os page.tsx (mesmo padrao de app/pm/clients/[id]/page.tsx) e passado como prop pros dois paineis. Os 3 alvos hardcoded /pm/clients (Finding 2a) agora sao role-aware (viewerIsAdmin ? "/admin/clients..." : "/pm/clients..."). app/admin/layout.tsx tem exatamente 1 item novo ("Produção" -> /pm/board), sem item "Chat" separado. tsc --noEmit, eslint e npm run build limpos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação ao vivo -- Admin navegando por Produção/Chat de ponta a ponta</name>
  <what-built>
    middleware.ts agora libera /pm/board e /pm/chat pro papel admin via um allow-list aditivo (extraAllowedPrefixes), mantendo /pm/clients e /pm/editors bloqueados. board-panel.tsx e chat-panel.tsx recebem uma prop viewerIsAdmin (resolvida server-side em cada page.tsx via profiles.role, mesmo padrao ja usado em client-detail-form.tsx) que torna role-aware os 3 alvos de navegação que antes apontavam sempre pra /pm/clients (o fallback de "sem cliente ativo" no board, o botao "Editar briefing", e o fallback de "sem cliente ativo" no chat) -- agora apontam pra /admin/clients[...] quando quem esta logado e Admin. app/admin/layout.tsx ganhou exatamente 1 item de nav novo, "Produção" -> /pm/board, logo apos "Clientes".
  </what-built>
  <how-to-verify>
    1. Rodar `npm run dev`. Logar como Admin (conta existente, ex.: juliano@backstageed.com).
    2. Confirmar que o sidebar do Admin mostra o novo item "Produção" logo apos "Clientes", e nenhum item "Chat" separado foi adicionado.
    3. Simular uma sessao NOVA (janela anonima/privada, ou limpar localStorage do dominio) e clicar em "Produção". Confirmar: (a) a URL fica em /pm/board (nao e redirecionado de volta pra /admin), e (b) como nao ha cliente ativo ainda, e redirecionado automaticamente pra /admin/clients -- NUNCA pra /pm/clients, e NUNCA de volta pra /admin em silencio.
    4. Em /admin/clients, abrir um cliente existente e clicar em "Salvar briefing" (mesmo sem editar nada, ou fazendo uma edição trivial). Confirmar que aterrissa em /pm/board?client=... com o board renderizado normalmente pro cliente escolhido.
    5. No board, clicar no botao "Chat". Confirmar que navega pra /pm/chat com o mesmo cliente ativo, sem ser bloqueado.
    6. Voltar pro board (botao "Produção" dentro do chat, ou o sidebar) e clicar em "Editar briefing". Confirmar que a URL e /admin/clients/{id} (nao /pm/clients/{id}).
    7. Numa aba nova, ainda logado como Admin, digitar manualmente a URL /pm/clients e depois /pm/editors. Confirmar que AMBAS continuam redirecionando de volta pra /admin (bloqueio inalterado).
    8. Deslogar e logar como um PM existente. Confirmar que /pm/board, /pm/chat, /pm/clients e /pm/editors se comportam exatamente como antes (nenhuma regressao), incluindo o fallback de "sem cliente ativo" continuando a mandar o PM pra /pm/clients (nao /admin/clients).
    9. Confirmar que a suite automatizada esta verde: `npx tsc --noEmit`, `npx eslint .`, `npm test`, `npm run build`.
    Limpar qualquer alteração de teste feita no passo 4 (ex.: reverter uma edição trivial de briefing), seguindo o habito ja estabelecido neste projeto.
  </how-to-verify>
  <resume-signal>Digite "approved" assim que Produção/Chat funcionarem de ponta a ponta pro Admin (incluindo o fallback de sessao nova e o botao "Editar briefing"), /pm/clients e /pm/editors continuarem bloqueados pro Admin, o PM nao apresentar nenhuma regressao, e a suite automatizada estiver verde. Caso contrario, descreva o que deu errado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| middleware.ts (route-level role gate) | Unico chokepoint que decide se uma requisição autenticada pode alcançar `/pm/board`/`/pm/chat` -- este plano estende o allow-list aqui, nao em nenhum outro lugar. |
| Server Actions de board/chat (`assertPmOrAdminCaller`/`isBoardWriteAuthorized`) | Ja incluiam Admin ANTES deste plano -- nenhuma mudança de codigo nesta camada. |
| RLS (`is_admin() OR ...` em toda policy relevante) | Ja incluia Admin ANTES deste plano (confirmado por `0003_rls_admin_unrestricted_test.sql`) -- nenhuma mudança de codigo nesta camada. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-k6c-01 | Elevation of Privilege | `extraAllowedPrefixes` prefix matching muito amplo, liberando sem querer uma sub-rota indesejada (ex.: `/pm/clients` via um prefixo mal fechado) | mitigate | Reaproveita EXATAMENTE a forma `pathname === prefix \|\| pathname.startsWith(`${prefix}/`)` que o arquivo ja usa pra `otherRoots` -- mesma garantia de boundary, e a lista tem apenas 2 entradas fixas e completas (`/pm/board`, `/pm/chat`), nunca um prefixo parcial |
| T-k6c-02 | Elevation of Privilege | Middleware libera `/pm/board`/`/pm/chat` pro Admin -- isso poderia, em tese, expor dado ou escrita que Admin nao deveria ter | accept (nao e uma exposição nova) | Confirmado pela pesquisa (Finding 2, exaustivo): toda leitura/escrita alcançavel a partir dessas duas rotas ja passava por `assertPmOrAdminCaller`/`isBoardWriteAuthorized` (que ja retorna `true` pra admin) ou por RLS ja admin-unrestricted (`0003_rls_admin_unrestricted_test.sql`) MESMO ANTES deste plano -- abrir a porta de rota nao abre nenhum dado/escrita que a RLS ja nao permitisse. Nenhum novo caminho de autorização e criado. |
| T-k6c-03 | Information Disclosure | Os 3 alvos de navegação corrigidos (Finding 2a) redirecionam Admin pra `/admin/clients[...]` em vez de `/pm/clients[...]` | accept | Nao e um problema de segurança -- e uma correção de UX (evitar um bounce silencioso de volta pra `/admin`). `/admin/clients` ja e uma rota que o Admin sempre teve acesso; nenhum dado novo e exposto por essa mudança de alvo. |
| T-k6c-SC | Tampering | npm/pip/cargo installs | n/a | Nenhum pacote novo instalado neste plano -- zero mudança em `package.json`/lockfile. |
</threat_model>

<verification>
1. `middleware.ts` libera exatamente `/pm/board` e `/pm/chat` pro papel admin via `extraAllowedPrefixes`; `/pm/clients`, `/pm/editors` e todo `otherRoots` continuam bloqueados; comportamento de `pm`/`client`/`editor` byte-identico ao anterior.
2. `viewerIsAdmin` resolvido server-side em `app/pm/board/page.tsx` e `app/pm/chat/page.tsx`, passado como prop pros respectivos Client Components.
3. Os 3 alvos hardcoded `/pm/clients` identificados pela pesquisa (Finding 2a) agora sao role-aware, apontando pra `/admin/clients[...]` quando `viewerIsAdmin` e `true`.
4. `app/admin/layout.tsx` tem exatamente 1 item de nav novo ("Produção" -> `/pm/board`), sem item "Chat" separado.
5. `npx tsc --noEmit`, `npx eslint .`, `npm test`, `npm run build` todos limpos.
6. Checkpoint da Task 3 aprovado pelo desenvolvedor, cobrindo o cenario de sessao nova (Admin sem cliente ativo) e a ausencia de regressao pro PM.
</verification>

<success_criteria>
- Admin acessa `/pm/board` e `/pm/chat` (as MESMAS telas do PM, sem duplicação em `/admin/board`/`/admin/chat`) sem ser bloqueado pelo middleware.
- `/pm/clients` e `/pm/editors` continuam exclusivos do PM -- Admin usa `/admin/clients`/`/admin/editors`, e os 3 alvos de navegação que antes apontavam pra `/pm/clients` agora respeitam isso.
- Um Admin numa sessao nova (sem cliente ativo em localStorage) que abre `/pm/board` ou `/pm/chat` e levado pra `/admin/clients`, nunca bounced de volta pra `/admin` em silencio.
- Sidebar do Admin ganha exatamente 1 item novo ("Produção"), reaproveitando o cross-nav interno (botao "Chat") ja existente entre board e chat.
- Nenhuma mudança de RLS, Server Action ou migration -- confirmado pelo threat model que a superficie de autorização ja incluia Admin antes deste plano.
- PM mantem comportamento byte-identico em todas as rotas.
- `tsc`/`eslint`/`test`/`build` verdes; checkpoint ao vivo aprovado.
</success_criteria>

<output>
Create `.planning/quick/260812-k6c-dar-ao-papel-admin-acesso-s-mesmas-telas/260812-k6c-SUMMARY.md` when done
</output>
