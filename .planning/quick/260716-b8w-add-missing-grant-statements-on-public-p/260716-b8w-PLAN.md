---
phase: quick
plan: 260716-b8w
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0009_profiles_pm_clients_grants.sql
autonomous: true
requirements: []

must_haves:
  truths:
    - "public.profiles has base-table GRANT select/insert/update/delete to authenticated"
    - "public.pm_clients has base-table GRANT select/insert/delete to authenticated (no update, matching its policy set)"
    - "npx supabase test db has been re-run after the new migration and the verbatim output is recorded, with an explicit AUTH-06/07/08 pass/fail verdict"
  artifacts:
    - path: "supabase/migrations/0009_profiles_pm_clients_grants.sql"
      provides: "GRANT statements for public.profiles and public.pm_clients to authenticated"
    - path: ".planning/quick/260716-b8w-add-missing-grant-statements-on-public-p/260716-b8w-SUMMARY.md"
      provides: "Verbatim npx supabase test db output and explicit AUTH-06/07/08 verdict"
  key_links:
    - from: "supabase/migrations/0009_profiles_pm_clients_grants.sql"
      to: "public.profiles / public.pm_clients"
      via: "grant ... to authenticated"
      pattern: "grant .* on public\\.(profiles|pm_clients) to authenticated"
---

<objective>
Close the second layer of the runtime blocker diagnosed by quick task 260716-au8: `npx supabase test db` now fails with `permission denied for table profiles` because no migration has ever issued a base-table GRANT on `public.profiles` or `public.pm_clients` to the `authenticated` role -- the same class of gap that 0008_clients_grants.sql fixed for `public.clients`.

Purpose: Unblock runtime verification of AUTH-06/AUTH-07/AUTH-08 by granting exactly the privileges each table's existing RLS policy set requires.

Output: New migration `supabase/migrations/0009_profiles_pm_clients_grants.sql`, applied locally via `supabase db reset`, with a fresh `npx supabase test db` run and its full verbatim output recorded in this quick task's SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260716-au8-add-missing-grant-select-insert-update-o/260716-au8-SUMMARY.md
@supabase/migrations/0008_clients_grants.sql
@supabase/migrations/0004_rls_policies.sql

<interfaces>
Privilege set required by existing RLS policies (0004_rls_policies.sql):

profiles: profiles_select_own_or_admin (SELECT), profiles_update_own_or_admin (UPDATE), profiles_admin_insert (INSERT), profiles_admin_delete (DELETE) -> needs select, insert, update, delete.

pm_clients: pm_clients_select_own_or_admin (SELECT), pm_clients_insert_admin_only (INSERT), pm_clients_delete_admin_only (DELETE) -> needs select, insert, delete only. No update policy exists on pm_clients -- do not grant update.

Pattern: 0008_clients_grants.sql -- header comment explaining root cause (RLS policies alone do not expose a table to API roles; base GRANT required separately), followed by grant statements. Additive only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 0009_profiles_pm_clients_grants.sql</name>
  <files>supabase/migrations/0009_profiles_pm_clients_grants.sql</files>
  <action>
  Create the new migration following the 0008_clients_grants.sql pattern: a header comment explaining root cause (260716-au8's re-run of npx supabase test db surfaced "permission denied for table profiles" one layer beneath the now-fixed clients gap -- no migration 0001-0008 ever issued a base-table GRANT on public.profiles or public.pm_clients to authenticated, independent of the correct RLS policies already defined in 0004_rls_policies.sql), then exactly these two statements:

  grant select, insert, update, delete on public.profiles to authenticated;
  grant select, insert, delete on public.pm_clients to authenticated;

  Note in the comment that pm_clients intentionally has no update grant because 0004_rls_policies.sql defines no update policy on pm_clients. Do not touch any file under supabase/migrations/0001-0008 or supabase/tests/.
  </action>
  <verify>
    <automated>bash -c "test -f supabase/migrations/0009_profiles_pm_clients_grants.sql && grep -v '^--' supabase/migrations/0009_profiles_pm_clients_grants.sql > /tmp/gap09.sql && grep -qc 'grant select, insert, update, delete on public.profiles to authenticated;' /tmp/gap09.sql && grep -qc 'grant select, insert, delete on public.pm_clients to authenticated;' /tmp/gap09.sql"</automated>
  </verify>
  <done>Migration file exists with the two exact GRANT statements and an explanatory header comment; no other migration or test file modified.</done>
</task>

<task type="auto">
  <name>Task 2: Apply migration and re-run pgTAP suite</name>
  <files>none (verification only)</files>
  <action>
  Run npx supabase start (if not already running), then npx supabase db reset to replay migrations 0001-0009 from scratch, then npx supabase test db to completion. Capture the full verbatim output. Record it in the quick task's SUMMARY.md along with an explicit statement of whether AUTH-06, AUTH-07, and AUTH-08 now pass (all ok, exit 0, no "not ok" lines across 0001/0002/0003 RLS test files) or whether a new/different failure appears. Do not attempt any further schema change beyond the 0009 migration regardless of outcome -- if the suite still fails, quote the failure verbatim and mark the outcome as still-blocked. Finish by running npx supabase stop to clean up local services.
  </action>
  <verify>
    <automated>npx supabase test db; echo "exit_code=$?"</automated>
  </verify>
  <done>SUMMARY.md contains the full verbatim npx supabase test db output from this run and an explicit pass/still-blocked verdict for AUTH-06/AUTH-07/AUTH-08; npx supabase stop has been run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated role -> public.profiles/pm_clients | PostgREST requests from logged-in users cross into these RLS-protected tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-b8w-01 | Elevation of Privilege | public.profiles GRANT | accept | Grant is scoped to exactly the four privileges (select/insert/update/delete) already gated by existing RLS policies (profiles_select_own_or_admin, profiles_update_own_or_admin, profiles_admin_insert, profiles_admin_delete) -- GRANT alone does not bypass RLS, it only permits Postgres to evaluate RLS at all. |
| T-quick-b8w-02 | Elevation of Privilege | public.pm_clients GRANT | accept | Grant deliberately omits UPDATE since no update policy exists on pm_clients in 0004_rls_policies.sql; adding an update grant without a corresponding policy would default-deny at the RLS layer but is avoided anyway to keep privilege surface minimal. |
</threat_model>

<verification>
- supabase/migrations/0009_profiles_pm_clients_grants.sql exists with exactly the two specified GRANT statements.
- No file under supabase/migrations/0001-0008 or supabase/tests/ was modified (git diff shows only the new 0009 file and the quick task's own SUMMARY.md/STATE.md).
- npx supabase test db was re-run locally after npx supabase db reset, with full verbatim output recorded in SUMMARY.md.
- SUMMARY.md states explicitly whether AUTH-06/AUTH-07/AUTH-08 now pass or remain still-blocked, citing the verbatim output.
</verification>

<success_criteria>
- 0009_profiles_pm_clients_grants.sql created with the exact grant statements specified.
- Local Supabase stack exercised (start/reset/test/stop) and the pgTAP re-run's full verbatim output captured in SUMMARY.md.
- AUTH-06/AUTH-07/AUTH-08 status explicitly recorded as passing or still-blocked (with verbatim evidence either way).
</success_criteria>

<output>
Create `.planning/quick/260716-b8w-add-missing-grant-statements-on-public-p/260716-b8w-SUMMARY.md` when done
</output>
