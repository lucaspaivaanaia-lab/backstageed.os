---
phase: quick-260728-uab
plan: 03
type: execute
wave: 3
depends_on: ["260728-uab-01", "260728-uab-02"]
autonomous: false
requirements: [UIUX-01, UIUX-02, UIUX-03, UIUX-04, UIUX-05, UIUX-06]
files_modified:
  - app/pm/chat/chat-panel.tsx
  - components/clients/client-access-panel.tsx
  - app/(auth)/login/page.tsx
  - app/(auth)/signup/page.tsx
  - app/(auth)/forgot-password/page.tsx
  - app/(auth)/reset-password/page.tsx
  - app/(auth)/change-password/page.tsx

must_haves:
  truths:
    - "O chat e a tela de acesso do cliente consomem os componentes novos (ErrorBox, StatusBadge, DataCard) e não markup ad-hoc"
    - "Todo badge de status (briefing vazio/preenchido, RAG degradado, acesso ativo/desativado) usa o StatusBadge com tons padronizados"
    - "Nenhuma tela do app monta caixa de erro ad-hoc — todas usam o componente ErrorBox compartilhado"
    - "A máquina de estado do chat (fetch/AbortController/streamResponse/handlers) permanece byte-idêntica — nenhuma lógica de rede nova foi introduzida"
    - "A copy contratual da tela de acesso do cliente (senha provisória, confirmação de desativação) está preservada verbatim"
    - "Nenhum arquivo de lógica (actions.ts, route.ts, lib/**, supabase/**, middleware.ts) foi modificado"
    - "npx tsc --noEmit passa sem erros, npm run lint não acusa novos erros e npm run build compila"
  artifacts:
    - path: "app/pm/chat/chat-panel.tsx"
      provides: "Chat com StatusBadge de RAG degradado, ErrorBox para interrupção/erro de envio e EmptyState com ícone — máquina de estado intocada"
      contains: "StatusBadge"
    - path: "components/clients/client-access-panel.tsx"
      provides: "Painel de acesso do cliente em DataCards com StatusBadge Ativo/Desativado e ErrorBox, copy travada preservada"
      contains: "DataCard"
    - path: "app/(auth)/login/page.tsx"
      provides: "Tela de login usando o ErrorBox compartilhado para erro de servidor"
      contains: "ErrorBox"
  key_links:
    - from: "app/pm/chat/chat-panel.tsx"
      to: "components/ui/error-box.tsx"
      via: "import ErrorBox"
      pattern: "ErrorBox"
    - from: "components/clients/client-access-panel.tsx"
      to: "components/ui/status-badge.tsx"
      via: "import StatusBadge"
      pattern: "StatusBadge"
    - from: "app/(auth)/login/page.tsx"
      to: "components/ui/error-box.tsx"
      via: "import ErrorBox"
      pattern: "ErrorBox"
---

<objective>
Plano 3 de 3 do upgrade de UI/UX do BackstageEd.OS. Fecha a adoção do sistema de design nas telas de MAIOR PRECISÃO — chat (máquina de estado de streaming) e tela de acesso do cliente (copy contratual) — mais as cinco telas de autenticação, e conduz o checkpoint humano final que valida o sistema de design INTEIRO (planos 01, 02 e 03).

Purpose: cumprir D-03 (chat e acesso do cliente não podem ficar com markup da geração anterior) e eliminar a última fonte de caixas de erro ad-hoc do app, encerrando a coexistência das duas gerações de padrão visual.

Output: `chat-panel.tsx` e `client-access-panel.tsx` migrados para `ErrorBox`/`StatusBadge`/`DataCard`, as cinco telas de auth usando `ErrorBox`, e o sign-off humano do sistema de design completo.

RESTRIÇÃO NÃO-NEGOCIÁVEL (verificada em toda task): zero mudança em lógica de negócio, RLS, Server Actions, queries ou fluxo de dados. `useState`/`useTransition`/handlers/`fetch`/`AbortController` existentes ficam byte-idênticos — só o markup e as classes ao redor mudam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260728-uab-upgrade-de-ui-ux-sistema-de-design-de-ve/260728-uab-CONTEXT.md

Fundação já existente (NÃO recriar — expandir):
@app/globals.css
@components/layout/page-shell.tsx

<already_built_by_prior_plans>
Os planos `260728-uab-01` e `260728-uab-02` JÁ FORAM EXECUTADOS e commitados. Os itens abaixo EXISTEM no codebase — apenas IMPORTAR, nunca recriar:

Do plano 01 (fundação):
- `app/globals.css` — já tem a escala de tipografia (`text-display`, `text-title`, `text-body`, `text-meta`), a escala de espaçamento (`p-page`/`px-page`/`gap-section`/`gap-stack`/`w-sidebar`) e os tokens de status (`success`, `warning`) em `:root` e `.dark`.
- `components/layout/page-shell.tsx` — `PageShell`, `PageTitle`, `SectionTitle`, `EmptyState` consomem a escala nova; `EmptyState` aceita o slot opcional `icon?: ReactNode`.
- `components/ui/status-badge.tsx` — exporta `StatusBadge` (props: `tone: "neutral" | "success" | "warning" | "danger" | "info"`, `icon?: ReactNode`, `children`, `className`), `statusBadgeVariants` e o tipo `StatusTone`.
- `components/ui/error-box.tsx` — exporta `ErrorBox` (props: `children`, `action?: ReactNode`, `className`); já renderiza `role="alert"` + `AlertCircleIcon`.
- `components/ui/data-card.tsx` — exporta `DataCard` (props: `title: ReactNode`, `description?`, `badge?`, `meta?`, `actions?`, `children?`, `className`). Genérico, nada Kanban (D-02).
- `components/ui/skeletons.tsx` — exporta `TableRowsSkeleton` e `CardSkeleton`.
- `components/layout/app-sidebar.tsx` — sidebar vertical fixa montada por `app/pm/layout.tsx` e `app/admin/layout.tsx`. `components/layout/app-nav.tsx` foi DELETADO — não referenciar.
- `app/pm/chat/chat-panel.tsx` — a `<div>` raiz do painel JÁ teve `h-screen` trocado por `h-full min-h-0` e o contêiner alargado para `max-w-3xl`. Não desfazer; o restante do arquivo ainda está no estado original e é o alvo da Task 6.

Do plano 02 (listagens e formulários):
- `components/ui/table.tsx` refinada; `app/pm/clients/page.tsx` e `app/admin/clients/page.tsx` uniformizadas com `PageShell`/`PageTitle`/`EmptyState`/`StatusBadge`.
- `app/pm/clients/loading.tsx`, `app/admin/clients/loading.tsx`, `app/pm/clients/[id]/loading.tsx` existem.
- `components/clients/client-create-form.tsx`, `client-detail-form.tsx` e `client-files-section.tsx` já usam `ErrorBox`/`DataCard` — NÃO refazer. `components/clients/client-access-panel.tsx` NÃO foi tocado e é o alvo da Task 6.
</already_built_by_prior_plans>

<locked_decisions>
- **D-01 — Sidebar fixa, sem colapsar.** Largura fixa, sempre visível, desktop-first. PROIBIDO: lógica de collapse/toggle, persistência em localStorage, breakpoint de auto-hide, botão de hambúrguer. (Entregue no plano 01 — não mexer, mas o checkpoint final valida.)
- **D-02 — Card genérico, não-Kanban.** O DataCard tem slots genéricos (título, badge, metadados, ações). PROIBIDO: drag-and-drop, lógica de coluna, props com nome de Kanban (`column`, `laneId`, `onDrop`, `status` enumerado de produção). O desenho específico do Kanban é do discuss-phase da Fase 3.
- **D-03 — Chat e tela de acesso do cliente adotam os componentes novos.** Não basta herdar a sidebar via layout: os badges e caixas de erro ad-hoc de `app/pm/chat/chat-panel.tsx` e `components/clients/client-access-panel.tsx` DEVEM ser trocados pelos componentes compartilhados desta rodada. **Este plano é o responsável por executar D-03.**
</locked_decisions>

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código atual — não explorar o codebase. -->

De `components/layout/page-shell.tsx`:
- `PageShell({ children, className, width })` — `width: "default" | "wide" | "narrow"` → `max-w-3xl | max-w-4xl | max-w-xl`
- `PageTitle({ children, className, action })` — h1, agora `text-display font-semibold`
- `SectionTitle({ children, className })` — h2, agora `text-title font-semibold`
- `EmptyState({ title, description, action, className, icon })` — `icon?: ReactNode` renderizado acima do título

De `lib/actions/auth.ts`:
- `signOut` — Server Action, invocada via `<form action={signOut}>` no rodapé da sidebar (plano 01).

De `components/ui/badge.tsx` (shadcn):
- `Badge({ className, variant, asChild, ...props })` — variants: `default | secondary | destructive | outline | ghost | link`; base já traz `rounded-full px-2 py-0.5 text-xs font-medium` e `[&>svg]:size-3`.

De `components/ui/skeleton.tsx` (shadcn, 13 linhas):
- `Skeleton({ className, ...props })` — div com `animate-pulse rounded-md bg-accent`.

De `components/ui/card.tsx` (shadcn): `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`, `CardAction`.

De `components/ui/table.tsx` (shadcn): `Table`, `TableHeader`, `TableBody`, `TableFooter`, `TableHead`, `TableRow`, `TableCell`, `TableCaption`.

De `lib/utils.ts`: `cn(...)` — merge de classes (clsx + tailwind-merge).

Root layout (`app/layout.tsx`, NÃO modificar): `<html className="h-full antialiased">` + `<body className="min-h-full flex flex-col font-sans">`.

Ícones lucide-react confirmados como exportados nesta versão (^1.23.0), usar a forma com sufixo `Icon` (precedente do codebase: `XIcon`, `CheckIcon`):
`AlertCircleIcon`, `UsersIcon`, `MessageSquareIcon`, `ClipboardCheckIcon`, `LogOutIcon`, `PlusIcon`, `ChevronRightIcon`, `InboxIcon`, `FileTextIcon`, `TriangleAlertIcon`.
</interfaces>

<scope_gate>
Baseline commit desta task: `0b45df7`.

O scope gate é este comando — a saída DEVE ser vazia em toda task:

git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts

Ele usa o SHA de baseline (não `git diff` puro) de propósito: pega alterações já commitadas por tasks anteriores, não só o working tree.
</scope_gate>
</context>

<tasks>

<task type="auto">
  <name>Task 6: Chat e tela de acesso do cliente adotam os componentes novos (D-03)</name>
  <files>app/pm/chat/chat-panel.tsx, components/clients/client-access-panel.tsx</files>
  <action>
Decisão travada D-03: essas duas telas não podem ficar com markup de status/erro da geração anterior. Trocar SOMENTE markup/classes — a máquina de estado do chat (`fetch`, `AbortController`, `streamResponse`, `handleSend`, `handleRetry`, `handleSwitchClient`, `toggleMessageChecked`, `handleSaveKnowledge`, refs e todos os `useState`/`useRef`) e os handlers do painel de acesso (`handleCreateSubmit`, `handleDeactivate`) ficam byte-idênticos.

**`app/pm/chat/chat-panel.tsx`**:
- Badge de RAG degradado (hoje `<Badge variant="outline" className="w-fit border-accent-foreground/20 bg-accent text-accent-foreground">{DEGRADED_NOTICE}</Badge>`) → `<StatusBadge tone="warning" icon={<TriangleAlertIcon />}>{DEGRADED_NOTICE}</StatusBadge>`. A constante `DEGRADED_NOTICE` e a condicional `!activeClient.hasRag` não mudam. Remover o import de `Badge` se ficar sem uso.
- Bloco `interrupted` (hoje uma `<div className="flex flex-col items-start gap-2 rounded-md bg-destructive/10 p-3">` com `<p>` + botão link "Tentar novamente") → `ErrorBox` com a mensagem `INTERRUPTED_ERROR` como children e o botão "Tentar novamente" passado no slot `action`. O `onClick={handleRetry}` continua exatamente o mesmo.
- Bloco `sendError` (hoje `<p className="rounded-md bg-destructive/10 ...">`) → `<ErrorBox>{sendError}</ErrorBox>`.
- Os dois estados vazios do chat ("Nenhum cliente selecionado" e "Nenhuma mensagem ainda") → componente `EmptyState` de `page-shell`, com `icon={<MessageSquareIcon className="size-5" />}` e `InboxIcon` respectivamente. As condicionais `!activeClient` e `messages.length === 0` não mudam.
- Bolhas de mensagem: manter a estrutura e o posicionamento (user à direita, assistant à esquerda, checkbox de curadoria só quando `message.id` existe). Ajuste puramente visual permitido: `text-sm` das bolhas vira `text-body`.

**`components/clients/client-access-panel.tsx`**:
- Trocar as DUAS caixas de erro ad-hoc (`createServerError` e `deactivateServerError`) por `ErrorBox`.
- Os três `Card` do painel (criar acesso / senha provisória / acesso ativo) e o bloco de "Acesso desativado" viram `DataCard`, com `title` e `description` mantendo a COPY TRAVADA verbatim que já está no arquivo (não reescrever nenhum texto — as strings de UI-SPEC são contratuais).
- Adicionar o badge de status que faltava: no card "Acesso do cliente" (estado `activeUserId`), `<StatusBadge tone="success">Ativo</StatusBadge>` no slot `badge`; no bloco "Acesso desativado", `<StatusBadge tone="neutral">Desativado</StatusBadge>`.
- O `AlertDialog` de confirmação de desativação, incluindo toda a sua copy travada e o `onClick={handleDeactivate}`, fica inalterado.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; grep -rn 'bg-destructive/10' app/pm/chat/chat-panel.tsx components/clients/client-access-panel.tsx || echo "0 caixas de erro ad-hoc"; grep -c 'ErrorBox\|StatusBadge' app/pm/chat/chat-panel.tsx components/clients/client-access-panel.tsx; grep -c 'DataCard' components/clients/client-access-panel.tsx; git diff 0b45df7 -- app/pm/chat/chat-panel.tsx | grep -E '^\+' | grep -v '^+++' | grep -icE 'fetch\(|abortcontroller|\.abort\(|streamresponse|listmessagesforclient|savecuratedknowledge'; grep -c 'Esta senha não será mostrada novamente\|Confirmar desativação' components/clients/client-access-panel.tsx; git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts</automated>
  </verify>
  <done>Zero `bg-destructive/10` nos dois arquivos; ambos importam e usam `ErrorBox` e `StatusBadge`; `client-access-panel.tsx` usa `DataCard` e ainda contém a copy travada ("Esta senha não será mostrada novamente", "Confirmar desativação"); o grep de primitivos de streaming em linhas ADICIONADAS do chat retorna 0 (nenhuma lógica de rede nova). `npx tsc --noEmit` passa. Scope gate vazio.</done>
</task>

<task type="auto">
  <name>Task 7: Telas de autenticação adotam o ErrorBox compartilhado</name>
  <files>app/(auth)/login/page.tsx, app/(auth)/signup/page.tsx, app/(auth)/forgot-password/page.tsx, app/(auth)/reset-password/page.tsx, app/(auth)/change-password/page.tsx</files>
  <action>
Fechar a última fonte de markup de erro ad-hoc no app. Em cada uma das cinco telas, trocar a caixa de erro de servidor (`<p className="rounded-md bg-destructive/10 p-3 text-sm text-destructive">{...}</p>`) por `<ErrorBox>{...}</ErrorBox>`, preservando a condicional exata que a envolve.

NÃO trocar os erros de campo (`<p className="text-sm text-destructive">{emailError}</p>` e similares) — esses são mensagens inline de validação de campo, não caixas de alerta; apenas trocar `text-sm` por `text-meta` neles para alinhar com a escala tipográfica nova.

`forgot-password/page.tsx` hoje só tem erro de campo (nenhuma caixa `bg-destructive/10`): aplicar só o ajuste de escala tipográfica e, se houver um estado de erro de servidor não renderizado, deixá-lo como está — não inventar nova lógica de erro.

Nenhuma mudança em `useState`, `useTransition`, handlers de submit, ou nas Server Actions importadas de `./actions`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; grep -rn 'bg-destructive/10' app components --include="*.tsx" | grep -v 'components/ui/error-box.tsx' || echo "0 caixas de erro ad-hoc no app inteiro"; grep -c 'ErrorBox' app/\(auth\)/login/page.tsx app/\(auth\)/signup/page.tsx app/\(auth\)/reset-password/page.tsx app/\(auth\)/change-password/page.tsx; git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts</automated>
  </verify>
  <done>Busca por `bg-destructive/10` em todo `app/` e `components/` retorna apenas `components/ui/error-box.tsx` (a definição). As quatro telas com erro de servidor importam e usam `ErrorBox`. `npx tsc --noEmit` passa. Scope gate vazio.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Verificação visual do sistema de design + scope gate final</name>
  <files>(nenhum arquivo modificado — task de verificação)</files>
  <action>Rodar os gates automatizados finais (typecheck, lint, build, scope gate) e então apresentar ao desenvolvedor o roteiro de inspeção visual abaixo, pausando para sign-off humano. Não editar nenhum arquivo nesta task; se algum gate falhar, corrigir o arquivo de apresentação responsável e re-rodar antes de apresentar.</action>
  <what-built>
Sistema de design completo (entregue pelos planos 01, 02 e 03): tokens formalizados de tipografia/espaçamento/status em `app/globals.css`; sidebar vertical persistente (`components/layout/app-sidebar.tsx`) substituindo o header horizontal AppNav (removido); quatro primitivos reutilizáveis (`StatusBadge`, `ErrorBox`, `DataCard`, skeletons); tabela refinada; e adoção desses componentes em listas de clientes, formulários de criação/briefing/arquivos, chat, tela de acesso do cliente e telas de autenticação.
  </what-built>
  <how-to-verify>
Rodar antes da inspeção visual:

1. `npx tsc --noEmit` — deve passar sem erros.
2. `npm run lint` — sem novos erros.
3. `npm run build` — deve compilar.
4. Scope gate final (a saída DEVE ser vazia):
   `git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts`

Depois, `npm run dev` e inspecionar:

5. `/pm/clients` — a sidebar aparece à esquerda, largura fixa, com wordmark no topo, "Clientes" e "Chat" com ícones, "Clientes" destacado como ativo, e "Sair" no rodapé da sidebar. Nenhum header horizontal no topo.
6. Clicar em "Chat" — o highlight muda para "Chat", a sidebar continua no lugar, e o painel de chat ocupa a altura disponível sem gerar barra de rolagem dupla.
7. `/pm/clients/[id]` — briefing, PMs atribuídos e Arquivos do cliente aparecem como cards distintos; salvar um briefing com erro forçado (ex: desconectar a rede) mostra a caixa de erro nova com ícone.
8. `/pm/clients/[id]/access` — o card de acesso mostra o badge "Ativo"/"Desativado" no canto do título.
9. `/admin/clients` — visual idêntico ao da lista PM (mesmo título, mesmo botão com ícone, mesma tabela), com "Aprovações" no lugar de "Chat" na sidebar.
10. `/login` com senha errada — a caixa de erro é a nova (borda + ícone), não a caixa lisa antiga.
11. Estado vazio: se houver cliente sem arquivos, a seção de arquivos mostra o empty state com ícone.

Confirmar que NENHUM comportamento mudou: login funciona, criar cliente funciona, salvar briefing funciona, upload de arquivo funciona, chat responde, logout funciona.
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que ficou visualmente errado / quebrado</resume-signal>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; npm run lint 2>&1 | tail -10; npm run build 2>&1 | tail -15; git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts</automated>
    <human-check>Roteiro de inspeção visual dos passos 5 a 11 acima, mais a confirmação de que login, criar cliente, salvar briefing, upload de arquivo, chat e logout continuam funcionando.</human-check>
  </verify>
  <done>`npx tsc --noEmit`, `npm run lint` e `npm run build` passam; o scope gate produz saída vazia; e o desenvolvedor respondeu "aprovado" ao roteiro de inspeção visual.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → Server Actions / rotas API | Toda entrada não confiável entra por aqui; esta task NÃO altera nenhum ponto desse contorno |
| RLS do Postgres (`is_admin()`, `pm_assigned_clients()`) | Camada de isolamento multi-tenant; fora do escopo desta task por construção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-uab-01 | Tampering | Edição acidental de Server Action / RLS / auth durante o restyling | mitigate | Toda task roda o scope gate `git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts`, cuja saída DEVE ser vazia; o checkpoint final (Task 8) re-roda contra o baseline cobrindo os três planos antes do sign-off |
| T-uab-03 | Information Disclosure | Copy contratual (senha provisória, confirmação de desativação) perdida ou reescrita ao trocar Card por DataCard | mitigate | Task 6 grepa pelas strings travadas (`Esta senha não será mostrada novamente`, `Confirmar desativação`) após a migração |
| T-uab-04 | Tampering | Regressão silenciosa na máquina de estado do chat (streaming/abort) ao mexer no markup | mitigate | Task 6 grepa linhas ADICIONADAS por `fetch(`/`AbortController`/`.abort(`/`streamResponse` — deve retornar 0; o checkpoint humano exercita o chat ao vivo |
| T-uab-SC | Tampering | Instalação de pacote npm | accept | Nenhuma dependência nova é instalada — `lucide-react`, `class-variance-authority`, `tailwind-merge` e o Skeleton do shadcn já estão no `package.json` |
</threat_model>

<verification>
1. `npx tsc --noEmit` passa sem erros.
2. `npm run lint` sem novos erros.
3. `npm run build` compila.
4. Scope gate vazio: `git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts`
5. `components/layout/app-nav.tsx` não existe e nenhum `.tsx` referencia `AppNav`.
6. `grep -rn 'bg-destructive/10' app components --include="*.tsx" | grep -v 'components/ui/error-box.tsx'` retorna vazio.
7. `grep -icE 'collaps|toggle|localstorage' components/layout/app-sidebar.tsx` retorna 0 (D-01).
8. `grep -icE 'ondrop|draggable|column|lane|kanban' components/ui/data-card.tsx` retorna 0 (D-02).
9. `components/ui/status-badge.tsx`, `error-box.tsx`, `data-card.tsx`, `skeletons.tsx` e `components/layout/app-sidebar.tsx` existem com seus exports.
10. Checkpoint humano (Task 8) aprovado.
</verification>

<success_criteria>
- Chat e tela de acesso do cliente consomem `ErrorBox`/`StatusBadge`/`DataCard` (D-03), não markup da geração anterior.
- Zero caixas de erro ad-hoc restantes no app inteiro (única ocorrência de `bg-destructive/10` é a definição do `ErrorBox`).
- A máquina de estado do chat e os handlers do painel de acesso permanecem byte-idênticos; copy contratual preservada verbatim.
- Nenhum arquivo de lógica modificado — scope gate vazio nas tasks deste plano e no checkpoint final, que verifica o baseline `0b45df7` cobrindo os três planos.
- `npx tsc --noEmit`, `npm run lint` e `npm run build` passam.
- Sistema de design completo aprovado no roteiro de inspeção visual humana (sidebar, listas, formulários, chat, acesso, auth).
- Fundação pronta para o `/gsd-discuss-phase` da Fase 3 desenhar o Kanban em cima do `DataCard` genérico, sem nada Kanban-específico entregue aqui (D-02).
</success_criteria>

<output>
Criar `.planning/quick/260728-uab-upgrade-de-ui-ux-sistema-de-design-de-ve/260728-uab-03-SUMMARY.md` ao concluir.
</output>
