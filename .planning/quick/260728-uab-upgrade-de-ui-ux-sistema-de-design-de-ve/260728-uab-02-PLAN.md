---
phase: quick-260728-uab
plan: 02
type: execute
wave: 2
depends_on: ["260728-uab-01"]
autonomous: true
requirements: [UIUX-04, UIUX-05]
files_modified:
  - components/ui/table.tsx
  - app/pm/clients/page.tsx
  - app/admin/clients/page.tsx
  - app/pm/clients/loading.tsx
  - app/admin/clients/loading.tsx
  - app/pm/clients/[id]/loading.tsx
  - components/clients/client-create-form.tsx
  - components/clients/client-detail-form.tsx
  - components/clients/client-files-section.tsx

must_haves:
  truths:
    - "As listas de clientes (PM e Admin) usam PageShell/PageTitle/EmptyState, a tabela refinada e ícones lucide, com a mesma estrutura visual nos dois papéis"
    - "Os badges de briefing (vazio/preenchido) das duas listas usam StatusBadge com tons padronizados, não o Badge do shadcn"
    - "Nenhum componente em components/clients/ monta caixa de erro ad-hoc — todos usam ErrorBox"
    - "As três rotas com carregamento lento (/pm/clients, /admin/clients, /pm/clients/[id]) têm loading.tsx com skeletons"
    - "Nenhuma linha de lógica de formulário mudou: useForm/useTransition/useFieldArray/handlers/Server Actions permanecem byte-idênticos"
    - "Nenhum arquivo de lógica (actions.ts, route.ts, lib/**, supabase/**, middleware.ts) foi modificado pelas tasks deste plano"
  artifacts:
    - path: "components/ui/table.tsx"
      provides: "Tabela refinada — headers em text-meta uppercase, células px-4 py-3, contêiner com borda arredondada; sem sort/filter/paginação"
      contains: "TableHead"
    - path: "app/admin/clients/page.tsx"
      provides: "Lista de clientes Admin migrada para PageShell/PageTitle/EmptyState/StatusBadge — estrutura idêntica à do PM"
      contains: "PageShell"
    - path: "app/pm/clients/loading.tsx"
      provides: "Loading state da lista PM com TableRowsSkeleton"
    - path: "app/admin/clients/loading.tsx"
      provides: "Loading state da lista Admin com TableRowsSkeleton"
    - path: "app/pm/clients/[id]/loading.tsx"
      provides: "Loading state do detalhe do cliente com CardSkeleton"
    - path: "components/clients/client-detail-form.tsx"
      provides: "Briefing e PMs atribuídos como DataCards, erros via ErrorBox — com a lógica de react-hook-form intacta"
      contains: "DataCard"
  key_links:
    - from: "components/clients/client-detail-form.tsx"
      to: "components/ui/data-card.tsx"
      via: "import DataCard"
      pattern: "DataCard"
    - from: "app/pm/clients/page.tsx"
      to: "components/ui/status-badge.tsx"
      via: "import StatusBadge"
      pattern: "StatusBadge"
    - from: "app/admin/clients/page.tsx"
      to: "components/layout/page-shell.tsx"
      via: "import PageShell/PageTitle/EmptyState"
      pattern: "PageShell"
---

<objective>
Plano 2 de 3 do upgrade de UI/UX do BackstageEd.OS. Adota os primitivos criados no plano 01 nas telas de LISTAGEM e nos FORMULÁRIOS de cliente: refino visual da tabela, uniformização das listas de clientes (PM e Admin), e polimento dos formulários de criação, briefing e arquivos do cliente — mais os três `loading.tsx`.

Este plano NÃO toca no chat, na tela de acesso do cliente, nem nas telas de autenticação — esses são do plano 03.

Purpose: eliminar a divergência visual entre a lista PM (já migrada) e a lista Admin (ainda com markup ad-hoc), e trocar as caixas de erro/seções soltas dos formulários de cliente pelos componentes compartilhados.

Output: `components/ui/table.tsx` refinada, `app/pm/clients/page.tsx` e `app/admin/clients/page.tsx` com estrutura idêntica, 3 arquivos `loading.tsx` novos, e os 3 componentes de `components/clients/` (create-form, detail-form, files-section) consumindo `ErrorBox`/`DataCard`.

RESTRIÇÃO NÃO-NEGOCIÁVEL (verificada em toda task): zero mudança em lógica de negócio, RLS, Server Actions, queries ou fluxo de dados. `useState`/`useTransition`/`useForm`/`useFieldArray`/handlers existentes ficam byte-idênticos — só o markup e as classes ao redor mudam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260728-uab-upgrade-de-ui-ux-sistema-de-design-de-ve/260728-uab-CONTEXT.md

Fundação já existente (NÃO recriar — expandir):
@app/globals.css
@components/layout/page-shell.tsx

<already_built_by_plan_01>
O plano `260728-uab-01` JÁ FOI EXECUTADO e commitado. Os itens abaixo EXISTEM no codebase — apenas IMPORTAR, nunca recriar:

- `app/globals.css` — já tem a escala de tipografia (`text-display`, `text-title`, `text-body`, `text-meta`), a escala de espaçamento (`p-page`/`px-page`/`gap-section`/`gap-stack`/`w-sidebar`) e os tokens de status (`success`, `warning`) em `:root` e `.dark`.
- `components/layout/page-shell.tsx` — `PageShell`, `PageTitle`, `SectionTitle`, `EmptyState` já consomem a escala nova; `EmptyState` já aceita o slot opcional `icon?: ReactNode`.
- `components/ui/status-badge.tsx` — exporta `StatusBadge` (props: `tone: "neutral" | "success" | "warning" | "danger" | "info"`, `icon?: ReactNode`, `children`, `className`), `statusBadgeVariants` e o tipo `StatusTone`.
- `components/ui/error-box.tsx` — exporta `ErrorBox` (props: `children`, `action?: ReactNode`, `className`); já renderiza `role="alert"` + `AlertCircleIcon`.
- `components/ui/data-card.tsx` — exporta `DataCard` (props: `title: ReactNode`, `description?`, `badge?`, `meta?`, `actions?`, `children?`, `className`). Genérico, nada Kanban (D-02).
- `components/ui/skeletons.tsx` — exporta `TableRowsSkeleton({ rows = 5, columns = 3, className })` e `CardSkeleton({ lines = 3, className })`.
- `components/layout/app-sidebar.tsx` — sidebar vertical fixa já montada por `app/pm/layout.tsx` e `app/admin/layout.tsx`. `components/layout/app-nav.tsx` foi DELETADO — não referenciar.
</already_built_by_plan_01>

<locked_decisions>
- **D-01 — Sidebar fixa, sem colapsar.** Largura fixa, sempre visível, desktop-first. PROIBIDO: lógica de collapse/toggle, persistência em localStorage, breakpoint de auto-hide, botão de hambúrguer. (Já entregue no plano 01 — não mexer.)
- **D-02 — Card genérico, não-Kanban.** O DataCard tem slots genéricos (título, badge, metadados, ações). PROIBIDO: drag-and-drop, lógica de coluna, props com nome de Kanban (`column`, `laneId`, `onDrop`, `status` enumerado de produção). O desenho específico do Kanban é do discuss-phase da Fase 3.
- **D-03 — Chat e tela de acesso do cliente adotam os componentes novos.** Não basta herdar a sidebar via layout: os badges e caixas de erro ad-hoc de `app/pm/chat/chat-panel.tsx` e `components/clients/client-access-panel.tsx` DEVEM ser trocados pelos componentes compartilhados desta rodada. (Execução no plano 03 — NÃO tocar nesses dois arquivos neste plano.)
</locked_decisions>

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código atual — não explorar o codebase. -->

De `components/layout/page-shell.tsx`:
- `PageShell({ children, className, width })` — `width: "default" | "wide" | "narrow"` → `max-w-3xl | max-w-4xl | max-w-xl`
- `PageTitle({ children, className, action })` — h1, agora `text-display font-semibold`
- `SectionTitle({ children, className })` — h2, agora `text-title font-semibold`
- `EmptyState({ title, description, action, className, icon })` — `icon?: ReactNode` renderizado acima do título

De `lib/actions/auth.ts`:
- `signOut` — Server Action, invocada via `<form action={signOut}>` no rodapé da sidebar (plano 01).

De `components/ui/badge.tsx` (shadcn):
- `Badge({ className, variant, asChild, ...props })` — variants: `default | secondary | destructive | outline | ghost | link`; base já traz `rounded-full px-2 py-0.5 text-xs font-medium` e `[&>svg]:size-3`.

De `components/ui/skeleton.tsx` (shadcn, 13 linhas):
- `Skeleton({ className, ...props })` — div com `animate-pulse rounded-md bg-accent`.

De `components/ui/card.tsx` (shadcn): `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`, `CardAction`.

De `components/ui/table.tsx` (shadcn): `Table`, `TableHeader`, `TableBody`, `TableFooter`, `TableHead`, `TableRow`, `TableCell`, `TableCaption`.

De `lib/utils.ts`: `cn(...)` — merge de classes (clsx + tailwind-merge).

Root layout (`app/layout.tsx`, NÃO modificar): `<html className="h-full antialiased">` + `<body className="min-h-full flex flex-col font-sans">`.

Ícones lucide-react confirmados como exportados nesta versão (^1.23.0), usar a forma com sufixo `Icon` (precedente do codebase: `XIcon`, `CheckIcon`):
`AlertCircleIcon`, `UsersIcon`, `MessageSquareIcon`, `ClipboardCheckIcon`, `LogOutIcon`, `PlusIcon`, `ChevronRightIcon`, `InboxIcon`, `FileTextIcon`, `TriangleAlertIcon`.
</interfaces>

<scope_gate>
Baseline commit desta task: `0b45df7`.

O scope gate é este comando — a saída DEVE ser vazia em toda task:

git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts

Ele usa o SHA de baseline (não `git diff` puro) de propósito: pega alterações já commitadas por tasks anteriores, não só o working tree.
</scope_gate>
</context>

<tasks>

<task type="auto">
  <name>Task 4: Refino visual da tabela + listas de clientes (PM e Admin) uniformizadas</name>
  <files>components/ui/table.tsx, app/pm/clients/page.tsx, app/admin/clients/page.tsx, app/pm/clients/loading.tsx, app/admin/clients/loading.tsx</files>
  <action>
**`components/ui/table.tsx`** — refino puramente visual, sem adicionar sort/filter/paginação e sem mudar assinaturas nem exports:
- `TableHead`: subir a densidade e criar hierarquia — `h-11 px-4 text-meta font-medium uppercase tracking-wide text-muted-foreground` no lugar de `h-10 px-2 ... text-foreground`.
- `TableCell`: `px-4 py-3` no lugar de `p-2`.
- `TableHeader`: manter `[&_tr]:border-b`; adicionar `border-border` explícito.
- `TableRow`: manter o hover existente (`hover:bg-muted/50`) e a transição; adicionar `last:border-0` já coberto por `TableBody`, não duplicar.
- `Table`: contêiner ganha `rounded-lg border` para a tabela virar um bloco delimitado (visual Linear/Notion). O `<table>` interno mantém `w-full caption-bottom text-body`.

**`app/pm/clients/page.tsx`** — manter a query e o cálculo `briefingEmpty`/`assignedPmEmails` byte-idênticos. Só o markup muda:
- Botão "Criar cliente" (no `PageTitle action` e no `EmptyState action`) ganha `PlusIcon` antes do texto.
- `EmptyState` ganha `icon={<UsersIcon className="size-5" />}`.
- Os `Badge variant="outline"`/`variant="secondary"` de Briefing viram `StatusBadge`: "Vazio" → `tone="neutral"`, "Preenchido" → `tone="success"`. Remover o import de `Badge`.
- A célula do nome vira um `<Link>` com `flex items-center gap-2 font-medium hover:text-primary` e um `ChevronRightIcon className="size-4 text-muted-foreground"` ao final da linha, numa quarta `TableHead` vazia (cabeçalho com `<span className="sr-only">Abrir</span>`) alinhada à direita — ou, se preferir menos colunas, dentro da própria célula do nome. Escolher uma das duas e aplicar igual nas duas telas.

**`app/admin/clients/page.tsx`** — hoje monta shell/heading/empty-state ad-hoc (`max-w-4xl mx-auto px-6 py-8`, `<h1 className="text-[28px]...">`, bloco de empty state inline). Migrar para exatamente a mesma estrutura da tela PM: `PageShell width="wide"` + `PageTitle` com `action` + `EmptyState` + `StatusBadge` + os mesmos ícones. Os hrefs continuam apontando para `/admin/clients/...`. Query e cálculos permanecem byte-idênticos.

**`app/pm/clients/loading.tsx` e `app/admin/clients/loading.tsx`** (novos) — Server Components que renderizam `PageShell width="wide"` + um `Skeleton` de título + `TableRowsSkeleton rows={5} columns={3}`. Sem props, sem dados.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; grep -n 'StatusBadge\|PlusIcon\|UsersIcon\|PageShell\|EmptyState' app/admin/clients/page.tsx | head -10; grep -c 'from("clients")' app/pm/clients/page.tsx app/admin/clients/page.tsx; grep -rn 'text-\[28px\]\|max-w-4xl mx-auto' app/admin/clients/page.tsx || echo "markup ad-hoc removido do admin"; grep -icE 'sort|orderby|filter\(|paginat' components/ui/table.tsx; test -f app/pm/clients/loading.tsx && test -f app/admin/clients/loading.tsx && echo "loading states criados"; git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts</automated>
  </verify>
  <done>`components/ui/table.tsx` tem headers em `text-meta uppercase text-muted-foreground`, células `px-4 py-3` e contêiner com borda arredondada, sem nenhum código de sort/filter/paginação. As duas listas de clientes usam `PageShell`/`PageTitle`/`EmptyState`/`StatusBadge`/ícones lucide com estrutura idêntica, e ambas ainda fazem exatamente uma chamada `from("clients")` com o mesmo select. Os dois `loading.tsx` existem. `npx tsc --noEmit` passa. Scope gate vazio.</done>
</task>

<task type="auto">
  <name>Task 5: Polimento dos formulários — criar cliente, briefing e arquivos do cliente</name>
  <files>components/clients/client-create-form.tsx, components/clients/client-detail-form.tsx, components/clients/client-files-section.tsx, app/pm/clients/[id]/loading.tsx</files>
  <action>
Adotar os primitivos novos nos formulários. **Nenhuma linha de lógica muda**: `useForm`, `useTransition`, `useFieldArray`, `onSubmit`, `togglePm`, `removePm`, `addPillar`, `savePmAssignment`, `handleUpload`, `handleDelete` e todas as chamadas de Server Action ficam byte-idênticas. Só JSX e classes.

**`components/clients/client-create-form.tsx`**:
- Trocar o `<p className="rounded-md bg-destructive/10 p-3 text-sm text-destructive">{serverError}</p>` por `<ErrorBox>{serverError}</ErrorBox>` (mesma condicional `serverError ? ... : null`).
- Botão "Adicionar PM" ganha `PlusIcon` (`size-4`) antes do texto.
- Botão de submit: manter `type="submit"` e o `disabled={isPending}`; manter o texto condicional "Criando..." / "Criar cliente".
- Os chips de PM selecionado continuam usando o `Badge secondary` do shadcn com o `XIcon` (são chips removíveis de input, não status pills) — não trocar por `StatusBadge`.

**`components/clients/client-detail-form.tsx`**:
- Envolver as duas `<section>` existentes em `DataCard`: "Briefing estratégico" (com `description` curta explicando que o briefing alimenta o contexto da IA daquele cliente) e "PMs atribuídos". O `SectionTitle` interno é substituído pelo `title` do `DataCard` — remover o `SectionTitle` duplicado dessas duas seções e o import se ficar sem uso.
- Trocar as DUAS caixas de erro ad-hoc (`briefingServerError` e `pmServerError`) por `ErrorBox`.
- Botão "Adicionar PM" ganha `PlusIcon`.
- Manter a ordem travada das seções: nome do cliente (`PageTitle`) → Briefing estratégico → PMs atribuídos → Arquivos do cliente (`ClientFilesSection`).
- Espaçamento do contêiner raiz passa a usar a escala (`gap-section` no lugar de `gap-8`).

**`components/clients/client-files-section.tsx`**:
- Trocar a caixa de erro ad-hoc (`uploadError`) por `ErrorBox`.
- Envolver a seção em `DataCard` com `title="Arquivos do cliente"` e `description` mencionando o limite de arquivos (usar a constante `FILE_LIMIT` já importada — não redefinir o número no texto), removendo o `SectionTitle` que vira o título do card.
- Cada arquivo listado ganha `FileTextIcon` (`size-4 text-muted-foreground`) antes do nome.
- Quando a lista está vazia, usar `EmptyState` com `icon={<FileTextIcon className="size-5" />}` no lugar do texto solto atual (se hoje não houver estado vazio explícito, adicionar um).

**`app/pm/clients/[id]/loading.tsx`** (novo) — Server Component: `PageShell` + `Skeleton` de título (`h-8 w-56`) + dois `CardSkeleton`. Sem props, sem dados.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; grep -rn 'bg-destructive/10' components/clients/ || echo "0 caixas de erro ad-hoc em components/clients"; grep -c 'ErrorBox' components/clients/client-create-form.tsx components/clients/client-detail-form.tsx components/clients/client-files-section.tsx; grep -c 'DataCard' components/clients/client-detail-form.tsx components/clients/client-files-section.tsx; grep -c 'useTransition\|useState\|useForm\|useFieldArray' components/clients/client-detail-form.tsx; git diff 0b45df7 -- components/clients/ | grep -E '^\+' | grep -v '^+++' | grep -icE 'updatebriefing\(|assignpms\(|createclientrecord\(|uploadclientfile\(|deleteclientfile\(|listclientfiles\(' ; test -f 'app/pm/clients/[id]/loading.tsx' && echo "loading de detalhe criado"; git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts</automated>
  </verify>
  <done>Zero ocorrências de `bg-destructive/10` em `components/clients/`; os três componentes importam e usam `ErrorBox`; `client-detail-form.tsx` e `client-files-section.tsx` usam `DataCard`; o grep de chamadas de Server Action em linhas ADICIONADAS retorna 0 (nenhuma invocação nova de lógica foi introduzida); `app/pm/clients/[id]/loading.tsx` existe. `npx tsc --noEmit` passa. Scope gate vazio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → Server Actions / rotas API | Toda entrada não confiável entra por aqui; esta task NÃO altera nenhum ponto desse contorno |
| RLS do Postgres (`is_admin()`, `pm_assigned_clients()`) | Camada de isolamento multi-tenant; fora do escopo desta task por construção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-uab-01 | Tampering | Edição acidental de Server Action / RLS / auth durante o restyling | mitigate | Toda task roda o scope gate `git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts`, cuja saída DEVE ser vazia; o checkpoint final (plano 03) re-roda antes do sign-off |
| T-uab-SC | Tampering | Instalação de pacote npm | accept | Nenhuma dependência nova é instalada — `lucide-react`, `class-variance-authority`, `tailwind-merge` e o Skeleton do shadcn já estão no `package.json` |
</threat_model>

<verification>
1. `npx tsc --noEmit` passa sem erros.
2. Scope gate vazio: `git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts`
3. `grep -rn 'bg-destructive/10' components/clients/` retorna vazio.
4. `grep -icE 'sort|orderby|filter\(|paginat' components/ui/table.tsx` retorna 0.
5. As duas listas de clientes fazem exatamente uma chamada `from("clients")` cada, com o mesmo select de antes.
6. `app/pm/clients/loading.tsx`, `app/admin/clients/loading.tsx` e `app/pm/clients/[id]/loading.tsx` existem.
</verification>

<success_criteria>
- Listas de clientes PM e Admin visualmente idênticas em estrutura; tabela com hierarquia visual refinada e sem sort/filter/paginação nova.
- Zero caixas de erro ad-hoc restantes em `components/clients/` (as do chat/acesso/auth são do plano 03).
- Briefing, PMs atribuídos e Arquivos do cliente aparecem como `DataCard`s, com a ordem travada de seções preservada.
- Três `loading.tsx` novos usando `TableRowsSkeleton`/`CardSkeleton` do plano 01.
- Nenhuma linha de lógica de formulário alterada — `useForm`/`useFieldArray`/`useTransition`/handlers/Server Actions byte-idênticos.
- Nenhum arquivo de lógica modificado — scope gate vazio nas 2 tasks.
</success_criteria>

<output>
Criar `.planning/quick/260728-uab-upgrade-de-ui-ux-sistema-de-design-de-ve/260728-uab-02-SUMMARY.md` ao concluir.
</output>
