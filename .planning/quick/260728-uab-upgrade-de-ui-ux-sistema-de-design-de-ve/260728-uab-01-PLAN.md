---
phase: quick-260728-uab
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [UIUX-01, UIUX-02, UIUX-03]
files_modified:
  - app/globals.css
  - components/layout/page-shell.tsx
  - components/ui/status-badge.tsx
  - components/ui/error-box.tsx
  - components/ui/data-card.tsx
  - components/ui/skeletons.tsx
  - components/layout/app-sidebar.tsx
  - components/layout/app-nav.tsx
  - app/pm/layout.tsx
  - app/admin/layout.tsx
  - app/pm/chat/chat-panel.tsx

must_haves:
  truths:
    - "Toda rota sob /pm/* e /admin/* renderiza uma sidebar vertical fixa (largura fixa, sem colapsar) com os links do papel, ícones lucide, highlight do link ativo, e o botão Sair no rodapé da sidebar"
    - "O header horizontal AppNav não existe mais em lugar nenhum do app"
    - "app/globals.css expõe uma escala nomeada de tipografia (text-display/title/body/meta), de espaçamento (page/section/stack/sidebar) e tokens de status (success/warning), consumidos por PageShell/PageTitle/SectionTitle"
    - "Os quatro primitivos reutilizáveis (StatusBadge, ErrorBox, DataCard, skeletons) existem e estão prontos para adoção pelos planos 02 e 03"
    - "Nenhum arquivo de lógica (actions.ts, route.ts, lib/**, supabase/**, middleware.ts) foi modificado pelas tasks deste plano"
  artifacts:
    - path: "app/globals.css"
      provides: "Escala de tipografia (--text-display/title/body/meta), escala de espaçamento semântico (--spacing-page/section/stack/sidebar) e tokens de status (--success/--warning) + sidebar indigo-tinted"
      contains: "--text-display"
    - path: "components/layout/app-sidebar.tsx"
      provides: "Sidebar vertical persistente com nav por papel, ícones, highlight ativo e logout no rodapé"
      exports: ["AppSidebar"]
      min_lines: 50
    - path: "components/ui/status-badge.tsx"
      provides: "Badge/status pill padronizado com tons success/warning/danger/neutral/info"
      exports: ["StatusBadge", "statusBadgeVariants"]
    - path: "components/ui/error-box.tsx"
      provides: "Caixa de erro compartilhada (substitui o markup bg-destructive/10 repetido)"
      exports: ["ErrorBox"]
    - path: "components/ui/data-card.tsx"
      provides: "Card de propósito geral com slots genéricos (título, badge, meta, ações) — base visual reutilizável, nada Kanban-específico"
      exports: ["DataCard"]
    - path: "components/ui/skeletons.tsx"
      provides: "Padrões de loading reutilizáveis sobre o Skeleton do shadcn"
      exports: ["TableRowsSkeleton", "CardSkeleton"]
  key_links:
    - from: "app/pm/layout.tsx"
      to: "components/layout/app-sidebar.tsx"
      via: "import AppSidebar"
      pattern: "AppSidebar"
    - from: "app/admin/layout.tsx"
      to: "components/layout/app-sidebar.tsx"
      via: "import AppSidebar"
      pattern: "AppSidebar"
    - from: "components/layout/app-sidebar.tsx"
      to: "lib/actions/auth.ts"
      via: "form action={signOut} no rodapé da sidebar"
      pattern: "action=\\{signOut\\}"
---

<objective>
Plano 1 de 3 do upgrade de UI/UX do BackstageEd.OS. Entrega a FUNDAÇÃO do sistema de design: tokens formalizados de espaçamento/tipografia/status em `app/globals.css`, os quatro primitivos reutilizáveis (`StatusBadge`, `ErrorBox`, `DataCard`, skeletons), e a sidebar de navegação vertical persistente substituindo o header horizontal AppNav.

Este plano NÃO toca em formulários com estado, no chat, nem nas telas de autenticação — esses são dos planos 02 e 03, que consomem o que este plano cria.

Purpose: dar aos planos seguintes (e à Fase 3) primitivos visuais prontos, para que nenhuma tela precise inventar markup ad-hoc. Direção clean/minimalista (referência Linear/Notion) em cima da paleta indigo e dos componentes PageShell/PageTitle/SectionTitle/EmptyState já estabelecidos pela passada anterior (260721-wqd).

Output: 5 arquivos novos (`status-badge.tsx`, `error-box.tsx`, `data-card.tsx`, `skeletons.tsx`, `app-sidebar.tsx`), tokens expandidos em `globals.css`, `page-shell.tsx` consumindo a escala nova, os dois layouts reescritos para o shell de duas colunas, e `app-nav.tsx` removido.

RESTRIÇÃO NÃO-NEGOCIÁVEL (verificada em toda task): zero mudança em lógica de negócio, RLS, Server Actions, queries ou fluxo de dados. `useState`/`useTransition`/handlers/fetch/AbortController existentes ficam byte-idênticos — só o markup e as classes ao redor mudam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260728-uab-upgrade-de-ui-ux-sistema-de-design-de-ve/260728-uab-CONTEXT.md

Fundação já existente (NÃO recriar — expandir):
@app/globals.css
@components/layout/page-shell.tsx
@components/layout/app-nav.tsx

<locked_decisions>
- **D-01 — Sidebar fixa, sem colapsar.** Largura fixa, sempre visível, desktop-first. PROIBIDO: lógica de collapse/toggle, persistência em localStorage, breakpoint de auto-hide, botão de hambúrguer.
- **D-02 — Card genérico, não-Kanban.** O DataCard tem slots genéricos (título, badge, metadados, ações). PROIBIDO: drag-and-drop, lógica de coluna, props com nome de Kanban (`column`, `laneId`, `onDrop`, `status` enumerado de produção). O desenho específico do Kanban é do discuss-phase da Fase 3.
- **D-03 — Chat e tela de acesso do cliente adotam os componentes novos.** Não basta herdar a sidebar via layout: os badges e caixas de erro ad-hoc de `app/pm/chat/chat-panel.tsx` e `components/clients/client-access-panel.tsx` DEVEM ser trocados pelos componentes compartilhados desta rodada. (A execução de D-03 é do plano 03 — este plano só prepara os primitivos e faz o ajuste de ALTURA do chat na Task 3.)
</locked_decisions>

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código atual — não explorar o codebase. -->

De `components/layout/page-shell.tsx` (já existe, será estendido na Task 1):
- `PageShell({ children, className, width })` — `width: "default" | "wide" | "narrow"` → `max-w-3xl | max-w-4xl | max-w-xl`
- `PageTitle({ children, className, action })` — h1, hoje `text-[28px] font-semibold leading-[1.2] tracking-tight`
- `SectionTitle({ children, className })` — h2, hoje `text-xl font-semibold leading-[1.2]`
- `EmptyState({ title, description, action, className })`

De `lib/actions/auth.ts`:
- `signOut` — Server Action, hoje invocada via `<form action={signOut}>` dentro do AppNav (Client Component pode passar a referência direto no `action` do form).

De `components/ui/badge.tsx` (shadcn):
- `Badge({ className, variant, asChild, ...props })` — variants: `default | secondary | destructive | outline | ghost | link`; base já traz `rounded-full px-2 py-0.5 text-xs font-medium` e `[&>svg]:size-3`.

De `components/ui/skeleton.tsx` (shadcn, 13 linhas):
- `Skeleton({ className, ...props })` — div com `animate-pulse rounded-md bg-accent`.

De `components/ui/card.tsx` (shadcn): `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter`, `CardAction`.

De `components/ui/table.tsx` (shadcn): `Table`, `TableHeader`, `TableBody`, `TableFooter`, `TableHead`, `TableRow`, `TableCell`, `TableCaption`.

De `lib/utils.ts`: `cn(...)` — merge de classes (clsx + tailwind-merge).

Layouts atuais (serão reescritos na Task 3):
- `app/pm/layout.tsx` — `<AppNav links={[{href:"/pm/clients",label:"Clientes"},{href:"/pm/chat",label:"Chat"}]} />` dentro de `<div className="flex min-h-full flex-1 flex-col">`
- `app/admin/layout.tsx` — `<AppNav links={[{href:"/admin/clients",label:"Clientes"},{href:"/admin/approvals",label:"Aprovações"}]} />`

Root layout (`app/layout.tsx`, NÃO modificar): `<html className="h-full antialiased">` + `<body className="min-h-full flex flex-col font-sans">`.

Ícones lucide-react confirmados como exportados nesta versão (^1.23.0), usar a forma com sufixo `Icon` (precedente do codebase: `XIcon`, `CheckIcon`):
`AlertCircleIcon`, `UsersIcon`, `MessageSquareIcon`, `ClipboardCheckIcon`, `LogOutIcon`, `PlusIcon`, `ChevronRightIcon`, `InboxIcon`, `FileTextIcon`, `TriangleAlertIcon`.
</interfaces>

<scope_gate>
Baseline commit desta task: `0b45df7`.

O scope gate é este comando — a saída DEVE ser vazia em toda task:

git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts

Ele usa o SHA de baseline (não `git diff` puro) de propósito: pega alterações já commitadas por tasks anteriores, não só o working tree.
</scope_gate>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokens de tipografia, espaçamento e status + PageShell consumindo a escala</name>
  <files>app/globals.css, components/layout/page-shell.tsx</files>
  <action>
Formalizar a escala de design em `app/globals.css`, expandindo o que já existe (paleta indigo, `--radius`, tokens de sidebar) — não recriar do zero.

No bloco `@theme inline` existente, adicionar (valores literais, o bloco é `inline`):

Escala tipográfica de 4 níveis, no formato de escala de texto do Tailwind v4 (cada nível com sua variante `--line-height` e, onde indicado, `--letter-spacing`), gerando utilitários `text-display`, `text-title`, `text-body`, `text-meta`:
- `--text-display`: 1.75rem, line-height 1.2, letter-spacing -0.02em (equivale ao `text-[28px]` hardcoded de hoje no PageTitle)
- `--text-title`: 1.125rem, line-height 1.3, letter-spacing -0.01em
- `--text-body`: 0.875rem, line-height 1.5
- `--text-meta`: 0.75rem, line-height 1.4

Escala de espaçamento semântico (gera `p-*`, `px-*`, `gap-*`, `w-*` etc.):
- `--spacing-page`: 2rem (padding horizontal/vertical de página)
- `--spacing-section`: 2rem (respiro entre seções)
- `--spacing-stack`: 1rem (respiro entre elementos empilhados dentro de uma seção)
- `--spacing-sidebar`: 15rem (largura fixa da sidebar — D-01)

Mapeamentos de cor de status novos: `--color-success: var(--success)`, `--color-success-foreground: var(--success-foreground)`, `--color-warning: var(--warning)`, `--color-warning-foreground: var(--warning-foreground)`.

Em `:root`, adicionar os tokens de status e re-tintar a família sidebar (hoje neutra em cinza puro) para o indigo da marca:
- `--success: oklch(0.52 0.13 155)`, `--success-foreground: oklch(0.99 0.01 155)`
- `--warning: oklch(0.52 0.12 75)`, `--warning-foreground: oklch(0.99 0.01 75)`
- `--sidebar: oklch(0.985 0.004 265)`, `--sidebar-foreground: oklch(0.145 0 0)`, `--sidebar-primary: oklch(0.45 0.15 265)`, `--sidebar-primary-foreground: oklch(0.98 0.005 265)`, `--sidebar-accent: oklch(0.94 0.03 265)`, `--sidebar-accent-foreground: oklch(0.35 0.1 265)`, `--sidebar-border: oklch(0.92 0.006 265)`, `--sidebar-ring: oklch(0.55 0.13 265)`

Em `.dark`, os pares equivalentes:
- `--success: oklch(0.72 0.15 155)`, `--success-foreground: oklch(0.16 0.02 155)`
- `--warning: oklch(0.78 0.14 75)`, `--warning-foreground: oklch(0.18 0.03 75)`
- `--sidebar: oklch(0.18 0.012 265)`, `--sidebar-foreground: oklch(0.985 0 0)`, `--sidebar-primary: oklch(0.72 0.13 265)`, `--sidebar-primary-foreground: oklch(0.16 0.02 265)`, `--sidebar-accent: oklch(0.32 0.06 265)`, `--sidebar-accent-foreground: oklch(0.9 0.03 265)`, `--sidebar-border: oklch(1 0 0 / 10%)`, `--sidebar-ring: oklch(0.6 0.13 265)`

Em `components/layout/page-shell.tsx`, trocar os tamanhos hardcoded pelos utilitários da escala nova, mantendo as assinaturas de props inalteradas (outras telas já importam esses componentes):
- `PageShell`: padding passa a usar a escala de página (`px-page py-page` no lugar de `px-6 py-8`); manter o mapa `WIDTH_CLASSES` como está.
- `PageTitle`: `text-[28px] font-semibold leading-[1.2] tracking-tight` vira `text-display font-semibold` (line-height e tracking vêm do token); margem inferior passa a usar a escala de seção (`mb-section`).
- `SectionTitle`: `text-xl font-semibold leading-[1.2]` vira `text-title font-semibold`.
- `EmptyState`: adicionar um slot opcional `icon?: ReactNode` renderizado acima do título, dentro de um contêiner circular discreto (fundo `bg-muted`, `size-10`, `rounded-full`, ícone centralizado, `text-muted-foreground`); quando `icon` não é passado, nada muda no layout atual. Manter `title`, `description`, `action`, `className` exatamente como estão.

Nada de lógica: nenhum import novo além de tipos React.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; grep -c -- '--text-display\|--spacing-sidebar\|--color-success\|--color-warning' app/globals.css; grep -n 'text-display\|text-title\|px-page\|mb-section' components/layout/page-shell.tsx; git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts</automated>
  </verify>
  <done>`app/globals.css` define `--text-display`, `--text-title`, `--text-body`, `--text-meta`, `--spacing-page/section/stack/sidebar`, os pares `--success`/`--warning` em `:root` e `.dark`, e os tokens de sidebar re-tintados em indigo. `components/layout/page-shell.tsx` consome `text-display`/`text-title`/`px-page` no lugar dos valores hardcoded e `EmptyState` aceita `icon`. `npx tsc --noEmit` passa. Scope gate vazio.</done>
</task>

<task type="auto">
  <name>Task 2: Primitivos reutilizáveis — StatusBadge, ErrorBox, DataCard, skeletons</name>
  <files>components/ui/status-badge.tsx, components/ui/error-box.tsx, components/ui/data-card.tsx, components/ui/skeletons.tsx</files>
  <action>
Criar os quatro primitivos que todas as telas vão adotar nas Tasks 4–7. Todos são componentes de apresentação puros: sem estado, sem efeitos, sem fetch, sem Server Action. Todos aceitam `className` e fazem merge via `cn`.

**`components/ui/status-badge.tsx`** — exporta `StatusBadge`, `statusBadgeVariants` e o tipo `StatusTone`. Construir com `cva` (mesmo padrão de `components/ui/badge.tsx`) sobre a base visual do Badge shadcn (`inline-flex w-fit shrink-0 items-center gap-1 rounded-full border border-transparent px-2 py-0.5 text-meta font-medium whitespace-nowrap [&>svg]:size-3`), com a prop `tone` (default `neutral`) e cinco variações de estilo soft/minimalista:
- `neutral`: `bg-muted text-muted-foreground`
- `success`: `bg-success/10 text-success`
- `warning`: `bg-warning/12 text-warning`
- `danger`: `bg-destructive/10 text-destructive`
- `info`: `bg-accent text-accent-foreground`
Props: `tone`, `icon?: ReactNode` (renderizado antes do texto), `children`, `className`, e o resto de `ComponentProps<"span">`. Sem `asChild`.

**`components/ui/error-box.tsx`** — exporta `ErrorBox`. Substitui o markup `rounded-md bg-destructive/10 p-3 text-sm text-destructive` repetido hoje em 11 pontos do app. Props: `children` (a mensagem), `action?: ReactNode` (slot para um botão tipo "Tentar novamente"), `className`. Renderiza um contêiner com `role="alert"`, `rounded-md border border-destructive/20 bg-destructive/10 p-3 text-body text-destructive`, layout flex com `AlertCircleIcon` (lucide, `size-4 shrink-0 mt-0.5`) à esquerda e o conteúdo à direita; quando `action` é passado, ele vai abaixo da mensagem numa coluna com gap. Sem estado, sem `useState`.

**`components/ui/data-card.tsx`** — exporta `DataCard`. Card de propósito geral com slots genéricos, construído sobre o `Card` do shadcn. **D-02: nada Kanban-específico** — proibido drag-and-drop, prop de coluna/lane, handler de drop, ou enum de status de produção. Props: `title: ReactNode`, `description?: ReactNode`, `badge?: ReactNode` (renderizado à direita do título), `meta?: ReactNode` (linha discreta de metadados em `text-meta text-muted-foreground`), `actions?: ReactNode` (rodapé), `children?: ReactNode` (corpo), `className`. Estrutura: `Card` → `CardHeader` com uma linha flex (título + badge à direita), `CardDescription` opcional e `meta` opcional; `CardContent` só renderiza se `children` existir; `CardFooter` só renderiza se `actions` existir. Título usa `text-title font-semibold`.

**`components/ui/skeletons.tsx`** — exporta `TableRowsSkeleton` e `CardSkeleton`, ambos sobre o `Skeleton` do shadcn (`components/ui/skeleton.tsx`, já instalado).
- `TableRowsSkeleton({ rows = 5, columns = 3, className })`: bloco que imita a densidade da tabela de clientes — uma linha de cabeçalho e `rows` linhas, cada célula um `Skeleton` de altura `h-4` com larguras variando por coluna, separadas por `border-b`.
- `CardSkeleton({ lines = 3, className })`: contorno de card (`rounded-lg border p-6`) com um `Skeleton` de título (`h-5 w-40`) e `lines` `Skeleton`s de corpo (`h-4`, larguras decrescentes).
Nenhum dos dois recebe dados nem faz fetch.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; grep -l 'export' components/ui/status-badge.tsx components/ui/error-box.tsx components/ui/data-card.tsx components/ui/skeletons.tsx; grep -n 'StatusTone\|statusBadgeVariants' components/ui/status-badge.tsx | head -3; grep -icE 'ondrop|draggable|column|lane|kanban' components/ui/data-card.tsx; git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts</automated>
  </verify>
  <done>Os 4 arquivos existem e exportam `StatusBadge`/`statusBadgeVariants`/`StatusTone`, `ErrorBox`, `DataCard`, `TableRowsSkeleton`/`CardSkeleton`. O grep de termos Kanban em `data-card.tsx` retorna 0 (D-02). `npx tsc --noEmit` passa. Scope gate vazio.</done>
</task>

<task type="auto">
  <name>Task 3: Sidebar vertical persistente substituindo o AppNav horizontal</name>
  <files>components/layout/app-sidebar.tsx, app/pm/layout.tsx, app/admin/layout.tsx, components/layout/app-nav.tsx, app/pm/chat/chat-panel.tsx</files>
  <action>
Criar `components/layout/app-sidebar.tsx` (Client Component, `"use client"`) exportando `AppSidebar` e o tipo `SidebarNavItem` (`{ href: string; label: string; icon: ReactNode }`).

Comportamento — preservar exatamente o que o AppNav atual faz, só reposicionado:
- Highlight do link ativo via `usePathname()`, com a MESMA regra de match de hoje: `pathname === link.href || pathname.startsWith(link.href + "/")`.
- Logout via `<form action={signOut}>` importando `signOut` de `@/lib/actions/auth` — mesma mecânica, agora no RODAPÉ da sidebar.

Estrutura visual (D-01: fixa, sem colapsar — PROIBIDO collapse/toggle/localStorage/hambúrguer):
- `<aside>` com `flex h-screen w-sidebar shrink-0 flex-col border-r border-sidebar-border bg-sidebar text-sidebar-foreground`.
- Cabeçalho: wordmark "BackstageEd.OS" reaproveitando o estilo do AppNav atual (`text-sm font-semibold tracking-tight text-sidebar-primary`), com padding da escala nova.
- Nav: `<nav>` em coluna, `flex-1`, cada item um `<Link>` com `flex items-center gap-2 rounded-md px-3 py-2 text-body transition-colors`, ícone `size-4`. Ativo: `bg-sidebar-accent font-medium text-sidebar-accent-foreground`. Inativo: `text-muted-foreground hover:bg-sidebar-accent/50 hover:text-sidebar-foreground`.
- Rodapé: `border-t border-sidebar-border`, contendo o form de logout com um `Button variant="ghost"` largura total, alinhado à esquerda, com `LogOutIcon` (`size-4`) + "Sair".

Reescrever `app/pm/layout.tsx` e `app/admin/layout.tsx` para o shell de duas colunas: `<div className="flex h-screen">` com `<AppSidebar items={...} />` e `<main className="flex min-w-0 flex-1 flex-col overflow-y-auto">{children}</main>`. Itens e ícones:
- PM: Clientes → `/pm/clients` (`UsersIcon`), Chat → `/pm/chat` (`MessageSquareIcon`)
- Admin: Clientes → `/admin/clients` (`UsersIcon`), Aprovações → `/admin/approvals` (`ClipboardCheckIcon`)
Manter os layouts como Server Components (só montam a sidebar e passam os itens — os ícones são elementos JSX passados como props, o que é permitido de Server para Client Component).

Deletar `components/layout/app-nav.tsx` — nenhum outro arquivo o importa depois desta task.

Ajuste de altura no chat: `app/pm/chat/chat-panel.tsx` linha ~269 usa hoje `mx-auto flex h-screen max-w-2xl flex-col`, que dentro do `<main>` rolável passaria da viewport. Trocar `h-screen` por `h-full min-h-0` e alargar o contêiner para `max-w-3xl`. É uma troca de classes na `<div>` raiz do painel — NÃO tocar em nada abaixo dela nesta task (badges/erros do chat são da Task 6).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5; test ! -f components/layout/app-nav.tsx && echo "app-nav removido"; grep -rn 'app-nav\|AppNav' app components --include="*.tsx" | grep -v '^\s*//' || echo "0 referencias a AppNav"; grep -n 'usePathname\|action={signOut}\|w-sidebar' components/layout/app-sidebar.tsx; grep -icE 'collaps|toggle|localstorage' components/layout/app-sidebar.tsx; grep -n 'h-screen' app/pm/chat/chat-panel.tsx || echo "h-screen removido do chat-panel"; git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts</automated>
  </verify>
  <done>`components/layout/app-sidebar.tsx` existe, exporta `AppSidebar`, usa `usePathname` com a regra de match original, tem `<form action={signOut}>` no rodapé e zero ocorrências de collapse/toggle/localStorage (D-01). `components/layout/app-nav.tsx` não existe mais e nenhum arquivo o referencia. Os dois layouts montam o shell de duas colunas com os ícones corretos por papel. `chat-panel.tsx` não usa mais `h-screen`. `npx tsc --noEmit` passa. Scope gate vazio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → Server Actions / rotas API | Toda entrada não confiável entra por aqui; esta task NÃO altera nenhum ponto desse contorno |
| RLS do Postgres (`is_admin()`, `pm_assigned_clients()`) | Camada de isolamento multi-tenant; fora do escopo desta task por construção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-uab-01 | Tampering | Edição acidental de Server Action / RLS / auth durante o restyling | mitigate | Toda task roda o scope gate `git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts`, cuja saída DEVE ser vazia; o checkpoint final (plano 03) re-roda antes do sign-off |
| T-uab-02 | Elevation of Privilege | Sidebar expondo links de Admin para PM (ou vice-versa) | mitigate | A sidebar não decide papel: cada layout (`app/pm/layout.tsx`, `app/admin/layout.tsx`) passa sua própria lista de itens, e o `middleware.ts` (intocado) segue sendo a autoridade de gating de rota por papel |
| T-uab-SC | Tampering | Instalação de pacote npm | accept | Nenhuma dependência nova é instalada — `lucide-react`, `class-variance-authority`, `tailwind-merge` e o Skeleton do shadcn já estão no `package.json` |
</threat_model>

<verification>
1. `npx tsc --noEmit` passa sem erros.
2. Scope gate vazio: `git diff --stat 0b45df7 -- 'app/**/actions.ts' 'app/api/**' 'app/auth/**' 'lib/**' 'supabase/**' middleware.ts`
3. `components/layout/app-nav.tsx` não existe e nenhum `.tsx` referencia `AppNav`.
4. `grep -icE 'collaps|toggle|localstorage' components/layout/app-sidebar.tsx` retorna 0 (D-01).
5. `grep -icE 'ondrop|draggable|column|lane|kanban' components/ui/data-card.tsx` retorna 0 (D-02).
6. `components/ui/status-badge.tsx`, `error-box.tsx`, `data-card.tsx`, `skeletons.tsx` e `components/layout/app-sidebar.tsx` existem com seus exports.
</verification>

<success_criteria>
- Sidebar vertical fixa presente em toda rota `/pm/*` e `/admin/*`, com ícones lucide, highlight do link ativo e logout no rodapé; header horizontal eliminado do codebase.
- `app/globals.css` expõe uma escala nomeada de tipografia (`text-display/title/body/meta`), de espaçamento (`page/section/stack/sidebar`) e tokens de status (`success`/`warning`), consumidos por `PageShell`/`PageTitle`/`SectionTitle`.
- Os quatro primitivos (`StatusBadge`, `ErrorBox`, `DataCard`, `TableRowsSkeleton`/`CardSkeleton`) existem com seus exports, mais o `EmptyState` com slot de ícone — prontos para os planos 02 e 03 importarem.
- Nada Kanban-específico no `DataCard` (D-02); nada de collapse/toggle na sidebar (D-01).
- Nenhum arquivo de lógica modificado — scope gate vazio nas 3 tasks.
</success_criteria>

<output>
Criar `.planning/quick/260728-uab-upgrade-de-ui-ux-sistema-de-design-de-ve/260728-uab-01-SUMMARY.md` ao concluir.
</output>
