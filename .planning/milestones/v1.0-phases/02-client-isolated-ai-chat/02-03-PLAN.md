---
phase: 02-client-isolated-ai-chat
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.local.example
  - lib/anthropic/client.ts
  - lib/tropicalia/client.ts
  - lib/tropicalia/client.test.ts
autonomous: true
requirements: [CTX-01, CTX-04, CTX-05]
user_setup:
  - service: anthropic
    why: "Claude Messages API generates all chat responses (CTX-05)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys (https://console.anthropic.com)"
      - name: ANTHROPIC_CHAT_MODEL
        source: "Optional override; defaults to claude-sonnet-4-5 in code"
  - service: tropicalia
    why: "Retrieval (search) and curated-knowledge upload; key already placeholdered in .env.local"
    env_vars:
      - name: TROPICALIA_API_KEY
        source: "Tropicalia dashboard (tropicalia.dev) — confirmed still empty as of 2026-07-16; degraded mode until supplied"
must_haves:
  truths:
    - "searchTropicaliaProject always sends generate_answer:false in its request body (CTX-05, Pitfall #2)"
    - "uploadTropicaliaDocument posts one multipart file to the client's project upload endpoint (CTX-04)"
    - "getAnthropicClient returns a server-only, singleton-cached Anthropic client and is never importable into the browser bundle"
  artifacts:
    - path: "lib/anthropic/client.ts"
      provides: "server-only Anthropic client factory (singleton)"
      exports: ["getAnthropicClient"]
    - path: "lib/tropicalia/client.ts"
      provides: "searchTropicaliaProject + uploadTropicaliaDocument added alongside createTropicaliaProject"
      exports: ["searchTropicaliaProject", "uploadTropicaliaDocument"]
    - path: "lib/tropicalia/client.test.ts"
      provides: "unit proof of generate_answer:false invariant"
      contains: "generate_answer"
  key_links:
    - from: "lib/anthropic/client.ts"
      to: "process.env.ANTHROPIC_API_KEY"
      via: "server-only env read, non-NEXT_PUBLIC"
      pattern: "process.env.ANTHROPIC_API_KEY"
    - from: "searchTropicaliaProject"
      to: "POST /v1/projects/{id}/search"
      via: "fetch with generate_answer:false"
      pattern: "generate_answer"
---

<objective>
Install `@anthropic-ai/sdk`, add the server-only Anthropic client factory, and extend `lib/tropicalia/client.ts` with the retrieval (`searchTropicaliaProject`, always `generate_answer:false`) and upload (`uploadTropicaliaDocument`, multipart) functions — the two external-service wrappers the chat Route Handler (02-04) and curation action (02-05) call. Pin the `generate_answer:false` invariant with a unit test (CTX-05, Pitfall #2).

Purpose: This is the first LLM SDK integration in the codebase and the first Tropicalia retrieval/upload surface. Keeping both keys server-only (Info-Disclosure mitigation) and forcing `generate_answer:false` (so Claude, not Tropicalia, generates) are the two load-bearing correctness properties here.
Output: `@anthropic-ai/sdk` dependency, `lib/anthropic/client.ts`, extended `lib/tropicalia/client.ts` (+ test), `.env.local.example` additions, `package.json` test-glob update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-client-isolated-ai-chat/02-RESEARCH.md
@.planning/phases/02-client-isolated-ai-chat/02-PATTERNS.md
@.planning/phases/02-client-isolated-ai-chat/02-VALIDATION.md

<interfaces>
Existing conventions in lib/tropicalia/client.ts to extend (do NOT fork):
- const TROPICALIA_BASE_URL = "https://api.tropicalia.dev"
- apiKey null-check throws; Bearer auth header; AbortSignal.timeout() defensive default; `if (!res.ok) throw` with endpoint name + status

Signatures to add (from 02-RESEARCH.md Pattern 2 lines 263-306, Pattern 3 lines 517-539):
- searchTropicaliaProject(projectId: string, query: string): Promise<TropicaliaRetrievalChunk[]>
    body: { query, retrieval_strategy: "hybrid", generate_answer: false, include_sources: true, limit: 10 }
    returns data.retrieval_contents ?? []
- uploadTropicaliaDocument(projectId: string, file: Blob, filename: string): Promise<{ document_id: string; filename: string }>
    multipart/form-data (FormData.append("file", file, filename)); NO manual Content-Type header; 30s timeout

Analog for the Anthropic factory:
- lib/supabase/admin.ts (server-only factory + doc-comment discipline); ADD a module-level singleton cache per 02-RESEARCH.md Pattern 2 lines 310-323
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @anthropic-ai/sdk, add the Anthropic client factory + env example</name>
  <files>package.json, .env.local.example, lib/anthropic/client.ts</files>
  <read_first>
    - lib/supabase/admin.ts (server-only factory shell + "never import from a Client Component" doc-comment)
    - package.json (current dependencies + test script)
    - .env.local.example (existing TROPICALIA_API_KEY block to mirror)
    - .planning/phases/02-client-isolated-ai-chat/02-RESEARCH.md (Package Legitimacy Audit — @anthropic-ai/sdk [OK] Approved; Pattern 2 factory; .env.local.example additions lines 683-694)
  </read_first>
  <action>
    Run `npm install @anthropic-ai/sdk` (target `^0.112.4` — the RESEARCH Package Legitimacy Audit rates it `[OK] Approved`, 26.8M downloads/wk, official anthropics repo, no postinstall script; no legitimacy checkpoint required). Create `lib/anthropic/client.ts` exporting `getAnthropicClient(): Anthropic` — construct `new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY })` behind a module-level `let cached: Anthropic | null = null` singleton (Pattern 2). Copy the SERVER-ONLY / never-import-from-Client-Component / secret-never-NEXT_PUBLIC doc-comment wording from `lib/supabase/admin.ts`. Append to `.env.local.example`: an `ANTHROPIC_API_KEY=` block (server-only, sourced from console.anthropic.com) and an optional `ANTHROPIC_CHAT_MODEL=` block (defaults to `claude-sonnet-4-5` in code) — mirror the comment style of the existing TROPICALIA_API_KEY block. Update `package.json`'s `test` script from `node --test lib/security/*.test.ts` to also cover the new suites: `node --test lib/security/*.test.ts lib/chat/*.test.ts lib/tropicalia/*.test.ts` (single owner of package.json across Wave 1).
  </action>
  <verify>
    <automated>node -e "require('@anthropic-ai/sdk'); console.log('sdk ok')" && grep -q "getAnthropicClient" lib/anthropic/client.ts && grep -q "ANTHROPIC_API_KEY" .env.local.example && grep -q "lib/chat/\*.test.ts" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `@anthropic-ai/sdk` appears in package.json dependencies and `require('@anthropic-ai/sdk')` resolves.
    - `lib/anthropic/client.ts` exports `getAnthropicClient`, uses a module-level singleton, and reads `process.env.ANTHROPIC_API_KEY` (never `NEXT_PUBLIC_`).
    - `.env.local.example` contains `ANTHROPIC_API_KEY=` and `ANTHROPIC_CHAT_MODEL=`.
    - `package.json` test script globs include `lib/chat/*.test.ts` and `lib/tropicalia/*.test.ts`.
  </acceptance_criteria>
  <done>SDK installed, factory + env example + test-glob in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend lib/tropicalia/client.ts with search + upload, TDD the generate_answer:false invariant</name>
  <files>lib/tropicalia/client.ts, lib/tropicalia/client.test.ts</files>
  <read_first>
    - lib/tropicalia/client.ts (existing createTropicaliaProject — the exact conventions to extend: apiKey null-check, Bearer header, AbortSignal.timeout, res.ok throw)
    - lib/security/client-access-authz.test.ts (node:test structure to mirror)
    - .planning/phases/02-client-isolated-ai-chat/02-RESEARCH.md (Pattern 2 search lines 263-306; Pattern 3 upload lines 517-539; Pitfall #2; Assumption A2 markdown upload)
    - .planning/phases/02-client-isolated-ai-chat/02-PATTERNS.md (lib/tropicalia/client.ts MODIFY assignment — CTX-05 divergence must be unit-tested)
  </read_first>
  <behavior>
    - searchTropicaliaProject's constructed request body ALWAYS has generate_answer === false, regardless of query input (assert by mocking global fetch and inspecting the JSON body).
    - searchTropicaliaProject returns data.retrieval_contents (or [] when absent).
    - uploadTropicaliaDocument builds a FormData with the file under key "file" and sets NO manual Content-Type header (so undici sets the multipart boundary).
  </behavior>
  <action>
    Write RED test first: `lib/tropicalia/client.test.ts` (node:test) that stubs `global.fetch`, sets `process.env.TROPICALIA_API_KEY` to a dummy value, calls `searchTropicaliaProject('proj','anything')`, and asserts the captured request body parses to an object with `generate_answer === false` and `query === 'anything'` — no real network call. Then extend `lib/tropicalia/client.ts`: export type `TropicaliaRetrievalChunk` (per RESEARCH Pattern 2), add `searchTropicaliaProject(projectId, query)` POSTing to `${TROPICALIA_BASE_URL}/v1/projects/${projectId}/search` with body `{ query, retrieval_strategy: "hybrid", generate_answer: false, include_sources: true, limit: 10 }`, Bearer auth, `AbortSignal.timeout(10_000)`, `if (!res.ok) throw`, returning `data.retrieval_contents ?? []`. Add `uploadTropicaliaDocument(projectId, file, filename)` POSTing multipart FormData (key `file`) to `${TROPICALIA_BASE_URL}/v1/projects/${projectId}/upload` with Bearer auth ONLY (no Content-Type header), `AbortSignal.timeout(30_000)`, `if (!res.ok) throw`, returning `res.json()`. Follow the existing file's apiKey null-check-throws backstop verbatim. Run GREEN.
  </action>
  <verify>
    <automated>node --test lib/tropicalia/client.test.ts && grep -q "generate_answer: false" lib/tropicalia/client.ts && grep -q "uploadTropicaliaDocument" lib/tropicalia/client.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test lib/tropicalia/client.test.ts` passes, including an assertion that the search body has `generate_answer === false`.
    - `lib/tropicalia/client.ts` exports `searchTropicaliaProject` and `uploadTropicaliaDocument` and retains `createTropicaliaProject` unchanged.
    - The search body literal contains `generate_answer: false`.
    - `uploadTropicaliaDocument` does not set a `Content-Type` header manually.
  </acceptance_criteria>
  <done>Tropicalia wrapper extended with search + upload; generate_answer:false pinned by test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → Tropicalia / Anthropic APIs | Outbound calls carry server-only secrets that must never reach the browser bundle |
| npm registry → package.json | New dependency crosses a supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-04 | Information Disclosure | lib/anthropic/client.ts, lib/tropicalia/client.ts | mitigate | Both read non-`NEXT_PUBLIC_` env secrets; SERVER-ONLY doc-comment; never imported from a Client Component (enforced by keeping them out of `"use client"` files) |
| T-2-01 | Tampering | searchTropicaliaProject | mitigate | `generate_answer:false` hard-coded in the request body and unit-tested — Tropicalia never generates; Claude does (CTX-05) |
| T-2-SC | Tampering (supply chain) | npm install @anthropic-ai/sdk | mitigate | RESEARCH Package Legitimacy Audit: `[OK] Approved`, official anthropics org, no postinstall script — no `[ASSUMED]`/`[SUS]` gate needed |
</threat_model>

<verification>
- `node --test lib/tropicalia/client.test.ts` green.
- `require('@anthropic-ai/sdk')` resolves.
- No `"use client"` file imports `lib/anthropic/client.ts` or `lib/tropicalia/client.ts`.
</verification>

<success_criteria>
- Anthropic SDK installed and wrapped in a server-only singleton factory.
- Tropicalia search (generate_answer:false) + upload wrappers exist and are unit-verified.
- Both secrets remain server-only.
</success_criteria>

<output>
Create `.planning/phases/02-client-isolated-ai-chat/02-03-SUMMARY.md` when done
</output>
