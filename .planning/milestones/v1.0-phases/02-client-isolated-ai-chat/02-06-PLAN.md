---
phase: 02-client-isolated-ai-chat
plan: 06
type: execute
wave: 4
depends_on: [02-04, 02-05]
files_modified: []
autonomous: false
requirements: [CTX-01, CTX-02, CTX-03, CTX-04, CTX-05]
must_haves:
  truths:
    - "With a live key, a PM gets a Claude-generated response scoped to Client A's briefing/client_files and switching to Client B never surfaces Client A's information (CTX-01/CTX-02)"
    - "A curated selection saved from a chat inserts a new row into public.client_files and is reflected in a later chat response IMMEDIATELY, with no async wait (CTX-04)"
  artifacts: []
  key_links: []
---

<objective>
Human-verify the two behaviors that cannot be pinned by automated tests without live credentials: (1) end-to-end streaming chat with real Claude generation and real cross-client isolation, and (2) the full upload -> chat -> curation round-trip against `public.client_files` (no external RAG service, no async indexing wait). This is the phase gate.

Purpose: The automated suite proves the pure logic and RLS scoping; only a live run with `ANTHROPIC_API_KEY` present can confirm the streamed generation, no-bleed switching, and that content uploaded/curated into `client_files` reflects in the next chat turn immediately — direct Supabase storage + full-content injection means there is no external indexing step and no `status:ready` wait.
Output: Confirmed end-to-end behavior or a recorded gap list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-client-isolated-ai-chat/02-RESEARCH.md
@.planning/phases/02-client-isolated-ai-chat/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight — confirm key present and full automated suite green</name>
  <files></files>
  <read_first>
    - .env.local (confirm ANTHROPIC_API_KEY is set — do NOT print its value)
  </read_first>
  <action>
    Run the full automated suite: `npm test` (node:test across lib/security, lib/chat, lib/extract), `npx supabase test db`, `npx tsc --noEmit`, and `npm run build`. Confirm all green. Then check whether `ANTHROPIC_API_KEY` is non-empty in the environment (report present/absent WITHOUT echoing the secret value) — it is the ONLY external key this phase depends on now; there is no second RAG-service key to check.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -v '^#' | grep -qi "pass" && npm run build 2>&1 | tail -5 | grep -qiE "compiled|success|ready" </automated>
  </verify>
  <acceptance_criteria>
    - `npm test` and `npx supabase test db` report all green; `npx tsc --noEmit` and `npm run build` succeed.
    - Key presence for `ANTHROPIC_API_KEY` is reported as present/absent (value never printed).
    - If the key is absent, the blocking status of Tasks 2/3 is explicitly recorded.
  </acceptance_criteria>
  <done>Automated suite green and key availability recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live end-to-end chat isolation verification</name>
  <action>Human runs the app with a live key and verifies streamed generation, cross-client no-bleed on switch, stale-response safety, and degraded mode per the steps below.</action>
  <what-built>The /pm/chat screen with server-side client isolation, streamed Claude responses, and degraded-mode fallback. Automated tests prove the pure logic and RLS scoping; this step confirms the live end-to-end behavior.</what-built>
  <how-to-verify>
    Requires `ANTHROPIC_API_KEY` set in `.env.local`; run `npm run dev` and sign in as an approved PM assigned to at least two clients.
    1. Open `/pm/chat`, select Client A. Ask a question specific to Client A's briefing. Confirm the response STREAMS token-by-token (D-08) and reads as a Claude-generated answer aligned to Client A (CTX-01/CTX-05).
    2. Switch the client switcher to Client B and ask a question that would only make sense for Client A. Confirm the answer contains NO Client A information — the knowledge base switched entirely (CTX-02).
    3. Start a message about Client A, then switch to Client B before it finishes. Confirm Client A's late tokens never appear in Client B's thread (stale-response guard).
    4. For a client with ZERO rows in `public.client_files`, confirm the "Nenhum arquivo de referência — respostas usam apenas o briefing do cliente." badge shows and chat still answers from the briefing (D-06/D-07). The badge must disappear once that client has >=1 file (see Task 3, step 2).
  </how-to-verify>
  <resume-signal>Type "approved" if all four behaviors hold, or describe what failed (each failure becomes a gap for `/gsd:plan-phase --gaps`).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live file upload + immediate chat reflection + curation round-trip</name>
  <action>Human uploads a real file via the new client-files UI, confirms it appears in chat immediately (no async wait), then tests manual curation from a chat.</action>
  <what-built>The client-files upload UI (components/clients/client-files-section.tsx, in /pm/clients/[id] or /admin/clients/[id]) storing extracted text directly in `public.client_files`, injected in full into the chat system prompt on every turn; and the "Salvar como conhecimento" curation flow inserting a markdown row into the same table.</what-built>
  <how-to-verify>
    1. Go to `/pm/clients/[id]` (or `/admin/clients/[id]`) for a test client with 0 files. Upload a real PDF/TXT/MD/DOCX file whose content includes a fact not present in the client's briefing (e.g. a specific number or name). Confirm it appears in the "Arquivos do cliente" list (name, type, date) immediately after upload.
    2. Go to `/pm/chat`, select that SAME client. Confirm the "Nenhum arquivo de referência..." badge is now GONE (>=1 file exists). Ask a question that depends on the fact from the uploaded file. Confirm the answer reflects it IMMEDIATELY — no waiting, no polling, no async indexing step of any kind.
    3. In the same chat, check one PM question + its AI answer, click "Salvar como conhecimento". Confirm the success toast appears (CTX-03/CTX-04) and a NEW row appears in the client's "Arquivos do cliente" list (filename starting with `conversa-`, file_type `markdown`) — the curated markdown stored directly in Supabase.
    4. Start a fresh question in the same client's chat that depends on the just-curated content and confirm the answer reflects it immediately (no async wait — CTX-04).
    5. Confirm nothing was saved automatically at any earlier point — only the explicit checkbox+click produced the new client_files row (CTX-03).
    6. (Optional, limit check) Upload files until the client has 3, then attempt a 4th upload and confirm it is blocked with a friendly message, without inserting a 4th row.
  </how-to-verify>
  <resume-signal>Type "approved" if all six behaviors hold, or describe the gap.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live PM session -> chat + file upload + curation | Verifies the isolation and no-auto-save properties hold under real generation/extraction, not just in unit tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Elevation of Privilege / Tampering | live cross-client switch | mitigate | Checkpoint Task 2 step 2 is a manual leakage probe — a Client-B answer surfacing Client-A data is a blocking failure |
| T-2-01b | Tampering (curation scope) | live save round-trip | mitigate | Checkpoint Task 3 step 5 confirms no auto-save; only explicit selection inserts a client_files row |
| T-hnm-03 | Tampering (context poisoning) | live upload of an unreadable file | mitigate | Checkpoint Task 3 can additionally probe an intentionally-scanned/blank PDF to confirm the friendly rejection message and that no client_files row is created (extends automated coverage from quick task 260722-hnm) |
</threat_model>

<verification>
- Automated suite green (Task 1) — includes lib/extract's text-extraction coverage.
- Both human-verify checkpoints approved (or gaps recorded for `--gaps` replanning).
</verification>

<success_criteria>
- Live streamed, client-scoped chat with no cross-client bleed.
- Uploaded/curated content lands in `public.client_files` and is reflected in the very next chat turn, with no async wait.
- No automatic saving occurs.
</success_criteria>

<output>
Create `.planning/phases/02-client-isolated-ai-chat/02-06-SUMMARY.md` when done
</output>
