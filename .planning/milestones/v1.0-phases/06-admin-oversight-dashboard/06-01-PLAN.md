---
phase: 06-admin-oversight-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cards/staleness.ts
  - lib/cards/staleness.test.ts
  - app/admin/page.tsx
  - app/admin/oversight-panel.tsx
  - app/admin/loading.tsx
  - app/admin/layout.tsx
  - components/layout/app-sidebar.tsx
autonomous: true
requirements: [ADM-01, ADM-02, ADM-03]

must_haves:
  truths:
    - "D-01: Admin opens /admin and sees one table listing cards from every client — not the 'Em construção' placeholder"
    - "The card untouched the longest is the first row"
    - "D-02: A card untouched for 7+ days carries a danger-toned 'Parado há N dias' badge; a card touched today carries a neutral 'Atualizado hoje' badge"
    - "D-03: Clicking (or Enter/Space on) any row lands the Admin on that card's client board at /pm/board?client={clientId}"
    - "The Admin sidebar shows a 'Visão geral' item pointing at /admin, highlighted only when the Admin is actually on /admin"
  artifacts:
    - path: "lib/cards/staleness.ts"
      provides: "Pure staleness computation (days since update, tier, badge copy, StatusBadge tone)"
      exports: ["daysSinceUpdate", "stalenessTier", "stalenessBadgeCopy", "stalenessTone", "StalenessTier"]
      min_lines: 30
    - path: "lib/cards/staleness.test.ts"
      provides: "node:test coverage of every tier boundary and every copy branch"
      min_lines: 40
    - path: "app/admin/page.tsx"
      provides: "Cross-client oversight server loader (replaces the placeholder)"
      contains: "OversightCard"
      min_lines: 60
    - path: "app/admin/oversight-panel.tsx"
      provides: "Client component: consolidated table, staleness badges, whole-row drill-down"
      contains: "use client"
      min_lines: 90
    - path: "app/admin/loading.tsx"
      provides: "Skeleton for the /admin segment"
      min_lines: 10
  key_links:
    - from: "app/admin/page.tsx"
      to: "public.cards"
      via: "RLS-scoped createClient() select, is_admin() unrestricted"
      pattern: "from\\(\"cards\"\\)"
    - from: "app/admin/page.tsx"
      to: "lib/cards/staleness.ts"
      via: "server-side daysSinceUpdate call, result passed down as a number"
      pattern: "daysSinceUpdate"
    - from: "app/admin/oversight-panel.tsx"
      to: "/pm/board"
      via: "router.push on row activation"
      pattern: "/pm/board\\?client="
    - from: "app/admin/layout.tsx"
      to: "/admin"
      via: "sidebar nav item"
      pattern: "Visão geral"
---

<objective>
Ship the thinnest end-to-end oversight slice: an Admin lands on `/admin` and immediately sees every card across every client in one table, worst-stalled first, with a colour-coded staleness badge, and can click any row to drill straight into that client's full Kanban board.

Purpose: this IS the phase goal ("Juliano can see the real status of any card, any client, at any moment, without anyone telling him") — ADM-01 and ADM-02 in full, ADM-03's client drill-down.
Output: `lib/cards/staleness.ts` (+ tests), a rewritten `app/admin/page.tsx` loader, a new `app/admin/oversight-panel.tsx` client component, `app/admin/loading.tsx`, plus the sidebar entry point.
</objective>

## Phase Goal

**As a** operation owner (Admin/Juliano), **I want to** see the real status of any card, any client, any PM at any moment in a single view, **so that** I no longer depend on someone telling me what is stuck.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-oversight-dashboard/06-CONTEXT.md
@.planning/phases/06-admin-oversight-dashboard/06-PATTERNS.md
@.planning/phases/06-admin-oversight-dashboard/06-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — do not go exploring for these. -->

From `lib/cards/stages.ts` (reuse verbatim, do not redefine):
- `type CardStage = "briefing" | "producao" | "revisao_interna" | "aprovacao_cliente" | "agendamento"`
- `const STAGE_ORDER: readonly CardStage[]`
- `const STAGE_LABELS: Record<CardStage, string>` — Briefing / Produção / Revisão interna / Aprovação do cliente / Agendamento

From `lib/actions/clients.ts`:
- `resolvePmNames(pmIds: string[]): Promise<Record<string, string>>` — id → display email, privileged read, display-only

From `components/ui/status-badge.tsx`:
- `type StatusTone = "neutral" | "success" | "warning" | "danger" | "info"`
- `<StatusBadge tone={...} icon={...}>{children}</StatusBadge>`

From `components/layout/page-shell.tsx`:
- `<PageShell width="wide">`, `<PageTitle>`, `<SectionTitle>`, `<EmptyState title description action? icon? />`

From `components/ui/error-box.tsx`:
- `<ErrorBox>{children}</ErrorBox>` — role="alert", destructive-toned container

From `components/ui/skeletons.tsx`:
- `<TableRowsSkeleton rows={number} columns={number} />`

From `components/layout/app-sidebar.tsx`:
- `type SidebarNavItem = { href: string; label: string; icon: ReactNode; requiresActiveClient?: boolean; linksToActiveClientDetail?: boolean }`
- active detection today: `pathname === href || pathname.startsWith(`${href}/`)`

`public.cards` columns relevant here: `id`, `client_id`, `title`, `card_type` ('single' | 'package' | 'piece'), `stage` (null only for `package`), `assignee_id`, `media_assignee_id`, `publish_at`, `created_at`, `updated_at`. `updated_at` is maintained explicitly by every write path (`app/pm/board/actions.ts`, `lib/actions/card-overrides.ts`, `app/client/actions.ts`, `app/editor/actions.ts`) — there is no DB trigger.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure staleness module + tests</name>
  <files>lib/cards/staleness.ts, lib/cards/staleness.test.ts</files>
  <read_first>
    - lib/cards/stages.ts (the pure-module convention to mirror: docstring explaining why it is I/O-free, plain exports, no React/Supabase import)
    - lib/cards/stages.test.ts (the exact `node:test` + `node:assert/strict` style, including the `./module.ts` relative import with the `.ts` extension)
    - lib/cards/package-rollup.ts (second example of the same pure-module shape)
    - .planning/phases/06-admin-oversight-dashboard/06-UI-SPEC.md — "Staleness Computation (D-02)" table, lines 70-80, and the Copywriting Contract rows for the five badge strings
    - components/ui/status-badge.tsx (the `StatusTone` union that `stalenessTone` must return a member of)
  </read_first>
  <behavior>
    - `daysSinceUpdate("<iso>", now)` with `now` exactly equal to the timestamp returns 0
    - `daysSinceUpdate` floors: 47h59m ago returns 1, 48h ago returns 2
    - `daysSinceUpdate` clamps a future `updated_at` (clock skew) to 0, never a negative number
    - `stalenessTier(2)` is "fresh"; `stalenessTier(3)` is "stalled"; `stalenessTier(6)` is "stalled"; `stalenessTier(7)` is "overdue"; `stalenessTier(0)` is "fresh"
    - `stalenessBadgeCopy(0)` is exactly "Atualizado hoje"
    - `stalenessBadgeCopy(1)` is exactly "Atualizado ontem"
    - `stalenessBadgeCopy(2)` is exactly "Atualizado há 2 dias"
    - `stalenessBadgeCopy(3)` is exactly "Parado há 3 dias"; `stalenessBadgeCopy(14)` is exactly "Parado há 14 dias"
    - `stalenessTone("fresh")` is "neutral"; `stalenessTone("stalled")` is "warning"; `stalenessTone("overdue")` is "danger"
  </behavior>
  <action>
    Create `lib/cards/staleness.ts` as a pure, I/O-free module in the exact shape of `lib/cards/stages.ts` — a leading docstring stating it is free of any Supabase/React import so its sibling `staleness.test.ts` can exercise it with Node's built-in test runner (no live DB, no Docker), and citing ADM-02 / D-02 / 06-UI-SPEC.md's staleness table as its source of truth.

    Export exactly four things plus one type:
    - `export type StalenessTier = "fresh" | "stalled" | "overdue"`
    - `daysSinceUpdate(updatedAt: string, now: Date = new Date()): number` — computes `Math.floor((now.getTime() - new Date(updatedAt).getTime()) / 86400000)` and clamps the result with `Math.max(0, ...)`. The injectable `now` default parameter is mandatory (not an inline `new Date()` in the body) — it is what makes the tier boundaries assertable against a fixed clock.
    - `stalenessTier(days: number): StalenessTier` — `days < 3` → "fresh", `days < 7` → "stalled", otherwise "overdue".
    - `stalenessBadgeCopy(days: number): string` — returns, verbatim from the UI-SPEC Copywriting Contract: "Atualizado hoje" for 0, "Atualizado ontem" for 1, `Atualizado há ${days} dias` for 2, and `Parado há ${days} dias` for 3 and above. These strings are a locked copy contract — never paraphrase, never change the accents, mirror the "Verbatim UI-SPEC copy -- never paraphrased" comment convention already used in `app/admin/cards/card-audit-panel.tsx`.
    - `stalenessTone(tier: StalenessTier): StatusTone` — "fresh" → "neutral", "stalled" → "warning", "overdue" → "danger". Import `StatusTone` as a `import type` from `@/components/ui/status-badge` so the union can never drift from the badge component; that is a type-only import and keeps the module I/O-free.

    Create `lib/cards/staleness.test.ts` covering every bullet in `<behavior>`. Use a fixed `now` (e.g. `new Date("2026-08-13T12:00:00.000Z")`) and construct the `updatedAt` inputs by subtracting exact millisecond offsets from it — never call `new Date()` inside a test. Import from `"./staleness.ts"` with the explicit `.ts` extension, matching `stages.test.ts`.

    No change to `package.json` is needed: the `test` script already globs `lib/cards/*.test.ts`.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5 && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 and its summary line shows a higher `# pass` count than before this task (new assertions ran, zero failures)
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - `grep -c 'export function daysSinceUpdate\|export function stalenessTier\|export function stalenessBadgeCopy\|export function stalenessTone' lib/cards/staleness.ts` returns 4
    - `grep -c 'from "@supabase\|from "react"\|"use client"\|"use server"' lib/cards/staleness.ts` returns 0 (module is genuinely pure)
    - `grep -c 'Atualizado hoje' lib/cards/staleness.ts` returns at least 1, and the same for `Atualizado ontem`, `Atualizado há`, and `Parado há`
    - `grep -c 'now: Date = new Date()' lib/cards/staleness.ts` returns 1 (injectable clock, not an inline call)
    - `grep -c 'new Date()' lib/cards/staleness.test.ts` returns 0 (tests use a fixed clock)
  </acceptance_criteria>
  <done>All four staleness helpers exist as pure exports, every tier boundary (2/3, 6/7) and every copy branch is asserted against a fixed clock, and the full suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: /admin becomes the consolidated cross-client oversight table</name>
  <files>app/admin/page.tsx, app/admin/oversight-panel.tsx</files>
  <read_first>
    - app/admin/page.tsx (the current "Em construção" placeholder being replaced — read it so nothing else in it is silently lost)
    - app/admin/cards/page.tsx (the exact cross-client RLS-read precedent: `createClient()` + `Promise.all` + `.is("archived_at", null)` clients query + `resolvePmNames` id-dedup, and its docstring lines 52-60 explaining why no service-role client is warranted)
    - app/admin/cards/card-audit-panel.tsx lines 1-260 (imports, `PageShell`/`PageTitle` shell, the `Table` header/body shape, the `CardAuditRow` `role="button"` / `tabIndex={0}` / Enter+Space keyboard-activation pattern, and the Etapa `StatusBadge` cell) — this is the template; ignore its Dialog/AlertDialog half entirely
    - lib/cards/staleness.ts (created in Task 1 — the helper signatures this loader/panel consume)
    - lib/cards/stages.ts (`CardStage`, `STAGE_LABELS`)
    - lib/actions/clients.ts lines 123-145 (`resolvePmNames` contract)
    - components/layout/page-shell.tsx (`PageShell` width options, `PageTitle`, `EmptyState` prop names)
    - components/ui/status-badge.tsx
    - components/ui/error-box.tsx
    - app/pm/board/page.tsx lines 100-110 (proof the drill-down target already accepts `searchParams: Promise<{ client?: string }>`)
    - .planning/phases/06-admin-oversight-dashboard/06-UI-SPEC.md (Screen Layout section lines 49-62 and the full Copywriting Contract)
  </read_first>
  <action>
    Replace `app/admin/page.tsx` entirely (delete the placeholder `Card`/`CardHeader`/`CardTitle`/`CardDescription` render and their imports) with an async server component loader modelled directly on `app/admin/cards/page.tsx`.

    Export two types from `app/admin/page.tsx` so the client panel imports them from `"./page"` the same way `card-audit-panel.tsx` imports `AuditCard`/`AuditClient`:
    - `OversightClient` = `{ id: string; name: string }`
    - `OversightCard` = `{ id: string; client_id: string; title: string; stage: CardStage; assignee_id: string | null; updated_at: string; daysSinceUpdate: number }`

    Note `stage` is non-nullable and `daysSinceUpdate` is a plain number — both are resolved server-side. Computing staleness in the loader (not in the client component) is deliberate: it keeps the panel a pure presentational component and removes any SSR-vs-hydration clock mismatch on a day boundary. Say so in a short comment.

    Loader body:
    - `const supabase = await createClient()` — the RLS-scoped client. Do NOT reach for `createAdminClient()` for `cards`/`clients`: `is_admin()` already grants unrestricted cross-client reads, exactly as `app/admin/cards/page.tsx` documents. Carry that reasoning into this file's own docstring.
    - One `Promise.all` with two queries: (a) `clients` selecting `id, name`, `.is("archived_at", null)`, `.order("name", { ascending: true })`; (b) `cards` selecting `id, client_id, title, stage, assignee_id, updated_at`, `.in("card_type", ["single", "piece"])`, `.order("updated_at", { ascending: true })`. The `card_type` filter enforces the UI-SPEC's package exclusion (a `package` row has `stage = null` and no meaningful Etapa cell); the ascending `updated_at` order IS the locked default sort — oldest-touched card is row 1, no client-side re-sorting anywhere.
    - Destructure the `error` off the cards query into a `loadError` boolean passed to the panel.
    - Build `const activeClientIds = new Set((clients ?? []).map((c) => c.id))` and post-filter the fetched cards to `activeClientIds.has(card.client_id) && card.stage !== null`. Rationale to record in a comment: cards belonging to a soft-archived client (`clients.archived_at`, migration 0019) are operational noise on a triage screen, and this also narrows `stage` to non-null without a second query. Use a TypeScript predicate narrow so `OversightCard.stage` types cleanly.
    - Compute `const now = new Date()` once, then map each surviving row to an `OversightCard` with `daysSinceUpdate: daysSinceUpdate(card.updated_at, now)` — one clock read for the whole page so two rows updated in the same second can never disagree.
    - Resolve display names: dedup non-null `assignee_id` values with `Array.from(new Set([...]))` exactly like `app/admin/cards/page.tsx` lines 124-131, then `await resolvePmNames(idsToResolve)`.
    - Return `<OversightPanel clients={...} cards={...} pmNames={...} loadError={...} />`.

    Create `app/admin/oversight-panel.tsx` as a `"use client"` component exporting `OversightPanel`. Import `useRouter` from `next/navigation`, `STAGE_LABELS` from `@/lib/cards/stages`, the three staleness helpers from `@/lib/cards/staleness`, `StatusBadge`, `ErrorBox`, the six `Table` primitives, and `PageShell`/`PageTitle`/`EmptyState`. Do NOT import `Dialog`, `AlertDialog`, `Badge`, `Button`, or `Select` — this screen has no modal and no CTA (filters arrive in plan 06-02).

    Render, inside `<PageShell width="wide">`:
    - `<PageTitle>Visão geral</PageTitle>` with no `action` slot.
    - When `loadError` is true, an `<ErrorBox>` immediately under the title containing verbatim: `Não foi possível carregar os dados. Atualize a página e tente novamente.`
    - When `cards.length === 0`, an `<EmptyState title="Nenhum card ainda" description="Nenhum card foi criado ainda em nenhum cliente." />` — verbatim UI-SPEC copy.
    - Otherwise a `<Table>` whose `TableHead` cells are, left to right and verbatim: `Cliente`, `Título`, `Responsável`, `Etapa`, `Atualizado`.
    - A `clientNames` `Map` built from the `clients` prop, same as `card-audit-panel.tsx` line 111, with `?? "—"` fallback.
    - One `OversightRow` subcomponent per card. It must set `className="cursor-pointer"`, `role="button"`, `tabIndex={0}`, an `onClick`, and an `onKeyDown` that `preventDefault()`s and activates on `"Enter"` or `" "` — copy that handler shape verbatim from `CardAuditRow`, but both handlers call `router.push(\`/pm/board?client=${card.client_id}\`)` instead of opening a Dialog (D-03; middleware.ts already allow-lists `/pm/board` for the Admin role via `extraAllowedPrefixes`, per quick task 260812-k6c). Add an `aria-label` on the row naming the destination so the row's purpose is announced, e.g. `Abrir o board de {clientName}`.
    - Cells: Cliente = resolved client name; Título = `card.title`; Responsável = `pmNames[card.assignee_id] ?? "—"` (guard the null id first); Etapa = `<StatusBadge tone="neutral">{STAGE_LABELS[card.stage]}</StatusBadge>` — no `card.stage ? ... : "Pacote"` ternary, packages cannot reach this table; Atualizado = `<StatusBadge tone={stalenessTone(stalenessTier(card.daysSinceUpdate))}>{stalenessBadgeCopy(card.daysSinceUpdate)}</StatusBadge>`.

    Give the panel a docstring stating it is a read-only triage surface — zero Server Actions, zero mutations, drill-down by navigation — and that it deliberately does not duplicate `/admin/cards`'s checklist/override audit purpose.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0, `npm run lint` exits 0, `npm run build` exits 0
    - `grep -c 'Em construção' app/admin/page.tsx` returns 0 (placeholder is gone)
    - `grep -c 'createAdminClient' app/admin/page.tsx` returns 0 (no service-role client for cards/clients)
    - `grep -c 'in("card_type", \["single", "piece"\])' app/admin/page.tsx` returns 1
    - `grep -c 'order("updated_at", { ascending: true })' app/admin/page.tsx` returns 1
    - `grep -c 'daysSinceUpdate' app/admin/page.tsx` returns at least 1
    - `grep -c 'archived_at' app/admin/page.tsx` returns at least 1
    - `grep -c '/pm/board?client=' app/admin/oversight-panel.tsx` returns 1
    - `grep -c 'Dialog\|AlertDialog' app/admin/oversight-panel.tsx` returns 0
    - `grep -cE 'Cliente|Título|Responsável|Etapa|Atualizado' app/admin/oversight-panel.tsx` returns at least 5
    - `grep -c 'Nenhum card ainda' app/admin/oversight-panel.tsx` returns 1 and `grep -c 'Não foi possível carregar os dados' app/admin/oversight-panel.tsx` returns 1
    - `grep -c 'role="button"' app/admin/oversight-panel.tsx` returns 1 and `grep -c 'tabIndex={0}' app/admin/oversight-panel.tsx` returns 1
    - `grep -cE '"use server"|from "@/lib/actions' app/admin/oversight-panel.tsx` returns 0 (no mutation surface introduced)
  </acceptance_criteria>
  <done>`/admin` renders a single table of every non-archived client's single/piece cards, oldest-touched first, each row carrying a tone-coded staleness badge, and activating a row navigates to `/pm/board?client={clientId}`.</done>
</task>

<task type="auto">
  <name>Task 3: Sidebar entry point (with exact-match highlighting) + /admin loading skeleton</name>
  <files>app/admin/layout.tsx, components/layout/app-sidebar.tsx, app/admin/loading.tsx</files>
  <read_first>
    - app/admin/layout.tsx (the full `items` array being edited, and the current `lucide-react` import list)
    - components/layout/app-sidebar.tsx (specifically the `SidebarNavItem` type and the `const isActive = pathname === href || pathname.startsWith(`${href}/`)` line — this file is shared by the PM, Admin, Client and Editor layouts, so any change must be strictly additive)
    - app/pm/layout.tsx (confirm which flags PM items already set, so the additive change provably cannot alter them)
    - app/admin/clients/loading.tsx (the skeleton shape to mirror: `PageShell width="wide"` + `Skeleton className="mb-section h-8 w-40"` + `TableRowsSkeleton`)
    - components/ui/skeletons.tsx (`TableRowsSkeleton` props)
    - .planning/phases/06-admin-oversight-dashboard/06-UI-SPEC.md line 25 (the nav item contract: label "Visão geral", `GaugeIcon`, inserted first)
  </read_first>
  <action>
    Three edits.

    (a) `components/layout/app-sidebar.tsx` — add an optional `exact?: boolean` field to `SidebarNavItem`, documented as: use it when an item's `href` is a prefix of its own sibling routes, so prefix matching would light up two items at once. Change the active computation to `const isActive = item.exact ? pathname === href : pathname === href || pathname.startsWith(\`${href}/\`)`. This is required, not cosmetic: `/admin` is a strict prefix of `/admin/clients`, `/admin/cards`, `/admin/approvals`, `/admin/checklist-templates`, `/admin/editors` and `/admin/shared-knowledge`, so without it the new "Visão geral" item renders as active on every single Admin screen alongside the genuinely active one. No existing item sets `exact`, so every current PM/Admin/Editor/Client nav item keeps byte-identical behaviour — including `/pm/clients` correctly staying highlighted on `/pm/clients/[id]`.

    (b) `app/admin/layout.tsx` — add `GaugeIcon` to the existing `lucide-react` import list (verified present in the installed `lucide-react` build) and insert `{ href: "/admin", label: "Visão geral", icon: <GaugeIcon />, exact: true }` as the FIRST element of the `items` array, before the `/admin/clients` entry. `GaugeIcon` specifically, not `LayoutDashboardIcon` — that one is already taken by the "Produção" item added in quick task 260812-k6c. Leave all seven existing items untouched and in their current order.

    (c) Create `app/admin/loading.tsx` mirroring `app/admin/clients/loading.tsx`: `PageShell width="wide"`, a `Skeleton className="mb-section h-8 w-40"` for the title, then a single `TableRowsSkeleton rows={6} columns={5}` matching the five-column main table. Deliberately ONE table skeleton, not two: a `loading.tsx` at `app/admin/` is Next.js's fallback for every nested Admin route that has none of its own (`/admin/cards`, `/admin/approvals`, `/admin/checklist-templates`, `/admin/editors`, `/admin/shared-knowledge` — only `/admin/clients` has its own), and all of those are single-table screens, so a phantom second table block would regress them. Record that reasoning in the file's docstring.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test 2>&1 | tail -3 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit`, `npm run lint`, `npm run build` all exit 0; `npm test` still reports 0 failures
    - `grep -c 'exact?: boolean' components/layout/app-sidebar.tsx` returns 1
    - `grep -c 'item.exact' components/layout/app-sidebar.tsx` returns 1
    - `grep -c 'exact' app/pm/layout.tsx` returns 0 (PM nav behaviour provably unchanged)
    - `grep -c 'GaugeIcon' app/admin/layout.tsx` returns 2 (import + usage)
    - `grep -c 'Visão geral' app/admin/layout.tsx` returns 1
    - `grep -c 'exact: true' app/admin/layout.tsx` returns 1
    - In `app/admin/layout.tsx`, the line number of the `href: "/admin"` entry is lower than the line number of the `href: "/admin/clients"` entry (verify with `grep -n 'href: "/admin"' app/admin/layout.tsx` and `grep -n 'href: "/admin/clients"' app/admin/layout.tsx`)
    - `grep -c 'TableRowsSkeleton' app/admin/loading.tsx` returns 2 (import + usage) and `grep -c 'columns={5}' app/admin/loading.tsx` returns 1
    - `grep -c 'width="wide"' app/admin/loading.tsx` returns 1
  </acceptance_criteria>
  <done>The Admin sidebar opens with a "Visão geral" item linking to `/admin`, highlighted only on `/admin` itself while every other item's highlighting is unchanged, and the `/admin` segment renders a matching table skeleton while loading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next.js server component (`/admin`) | Authenticated session cookie crosses here; role is asserted by `middleware.ts` before the route renders |
| Next.js server → Postgres | All `cards`/`clients` reads go through the RLS-scoped `createClient()`; `is_admin()` is the authorization decision |
| Next.js server → Postgres via `createAdminClient()` | `resolvePmNames` only — a privileged, display-only read that bypasses `profiles_select_own_or_admin` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure | `app/admin/page.tsx` cross-client `cards` read | mitigate | Read through the RLS-scoped `createClient()` only — never `createAdminClient()` for `cards`/`clients`, so a non-Admin session that somehow reaches the route gets an RLS-empty result rather than every client's data. Enforced by the `grep -c 'createAdminClient' app/admin/page.tsx` returns 0 gate in Task 2. |
| T-06-02 | Elevation of privilege | route reachability | mitigate | `/admin/*` is role-gated by `middleware.ts` (role-root routing); this phase adds no new route prefix and no new allow-list entry. Unchanged posture from `/admin/cards`. |
| T-06-03 | Information disclosure | `resolvePmNames` privileged read of `profiles.email` | accept | Already the established, reviewed pattern used by `/admin/cards`, `/pm/board` and the client list screens; display-only, never a write, and only reached from an already-role-gated Admin route. |
| T-06-04 | Tampering | `router.push('/pm/board?client=' + card.client_id)` | accept | `client_id` originates from an RLS-authorized server-side read, not from user input; the destination `/pm/board` independently re-reads under RLS and shows nothing the caller is not authorized for. |
| T-06-05 | Denial of service | unbounded cross-client `cards` query (no pagination) | accept | Operation scale is a handful of clients and low hundreds of cards; a `LIMIT`/pagination layer is deliberately not built for MVP. Revisit if the table exceeds ~1000 rows. |
| T-06-SC | Tampering | npm installs | mitigate | Zero package installs this phase (06-UI-SPEC.md: "zero new shadcn installs", all of `table`/`select`/`badge`/`skeleton` already present). No Package Legitimacy Gate applies because no install task exists. |
</threat_model>

<verification>
- `npm test` green with new `lib/cards/staleness.test.ts` assertions and zero regressions
- `npx tsc --noEmit`, `npm run lint`, `npm run build` all exit 0
- `app/admin/page.tsx` contains no `createAdminClient` call and no "Em construção" copy
- `app/admin/oversight-panel.tsx` imports no Dialog/AlertDialog and no Server Action
- No migration file added under `supabase/migrations/` — this phase changes zero schema (`git status --porcelain supabase/migrations/` is empty)
</verification>

<success_criteria>
- Opening `/admin` shows a five-column table of every non-archived client's `single`/`piece` cards, ordered oldest-`updated_at` first
- Each row's Atualizado cell shows the correct verbatim staleness copy and tone for its age (neutral <3d, warning 3-6d, danger ≥7d)
- Activating a row by mouse or keyboard navigates to `/pm/board?client={clientId}`
- The Admin sidebar's first item is "Visão geral" → `/admin`, active only on `/admin`
- Zero mutations, zero new tables, zero new packages introduced
</success_criteria>

<output>
Create `.planning/phases/06-admin-oversight-dashboard/06-01-SUMMARY.md` when done
</output>
