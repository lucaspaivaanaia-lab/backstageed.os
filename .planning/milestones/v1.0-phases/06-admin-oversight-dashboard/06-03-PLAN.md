---
phase: 06-admin-oversight-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified: []
autonomous: false
requirements: [ADM-01, ADM-02, ADM-03]

must_haves:
  truths:
    - "The full automated suite (types, lint, unit tests, production build) is green with the oversight dashboard in place"
    - "An Admin, logged in through a real browser session, lands on /admin and sees cards from more than one client in a single table"
    - "A backdated card visibly renders as 'Parado há N dias' in warning or danger tone while a freshly-touched card renders neutral"
    - "Both filters, individually and combined, narrow the table and survive a page reload"
    - "Activating a row lands the Admin on that client's /pm/board with the correct client preselected"
    - "The workload panel reflects real assignments and omits people with no active cards"
    - "No non-Admin role can reach /admin"
  artifacts:
    - path: ".planning/phases/06-admin-oversight-dashboard/06-03-SUMMARY.md"
      provides: "Recorded pre-flight output and the developer's per-step checkpoint verdict"
  key_links:
    - from: "live browser session"
      to: "/admin"
      via: "Admin login → role-root redirect"
      pattern: "/admin"
    - from: "/admin row activation"
      to: "/pm/board?client="
      via: "middleware extraAllowedPrefixes allow-list for the admin role"
      pattern: "/pm/board\\?client="
---

<objective>
Phase gate. Prove the whole oversight dashboard works against real data in a real browser session, the way plan 04-04 gated Phase 4 — a full automated pre-flight first, then a scripted live checkpoint the developer walks through and signs off.

Purpose: ADM-01, ADM-02 and ADM-03 are all "the Admin can SEE X" requirements. No amount of type-checking proves them; only a human looking at the screen does.
Output: `06-03-SUMMARY.md` recording the pre-flight results and the developer's verdict per step.
</objective>

## Phase Goal

**As a** operation owner (Admin/Juliano), **I want to** see the real status of any card, any client, any PM at any moment in a single view, **so that** I no longer depend on someone telling me what is stuck.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-oversight-dashboard/06-CONTEXT.md
@.planning/phases/06-admin-oversight-dashboard/06-UI-SPEC.md
@.planning/phases/06-admin-oversight-dashboard/06-01-SUMMARY.md
@.planning/phases/06-admin-oversight-dashboard/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated pre-flight</name>
  <files>(none — verification only, no source changes)</files>
  <read_first>
    - package.json (the exact `test` script glob, to confirm the three new `lib/cards/*.test.ts` files are actually picked up)
    - .planning/phases/06-admin-oversight-dashboard/06-01-SUMMARY.md
    - .planning/phases/06-admin-oversight-dashboard/06-02-SUMMARY.md
  </read_first>
  <action>
    Run the four automated gates in order and capture each one's exit code and tail output for the SUMMARY: `npx tsc --noEmit`, `npm run lint`, `npm test`, `npm run build`.

    Confirm from the `npm test` summary that assertions from all three new modules ran — `lib/cards/staleness.test.ts`, `lib/cards/oversight-filters.test.ts` and `lib/cards/workload.test.ts` — and that the total pass count exceeds the pre-phase baseline recorded in STATE.md, with zero failures.

    Do NOT run `npx supabase test db` as a required gate: this phase adds zero migrations and zero RLS changes (verify that claim rather than assuming it — `git diff --name-only main -- supabase/` must be empty). Record that verification in the SUMMARY as the reason the pgTAP suite is out of scope for this gate.

    If any gate fails, stop and fix it before Task 2. Do not present a failing build to the developer for live verification.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test 2>&1 | tail -8 && npm run build 2>&1 | tail -20 && git diff --name-only main -- supabase/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0
    - `npm test` exits 0 and its summary shows `# fail 0`
    - `npm run build` exits 0
    - `git diff --name-only main -- supabase/ | wc -l` returns 0 (zero schema change this phase)
    - `npm test 2>&1 | grep -c 'staleness\|oversight-filters\|workload'` returns at least 3, or the three files are otherwise shown to have executed
  </acceptance_criteria>
  <done>All four gates green, zero schema drift, and the three new test modules confirmed to have executed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live 12-step Admin checkpoint</name>
  <files>(none — verification only, no source changes)</files>
  <read_first>
    - .planning/phases/06-admin-oversight-dashboard/06-UI-SPEC.md (the copy strings and tone contract every step below checks against)
    - .planning/phases/06-admin-oversight-dashboard/06-CONTEXT.md (D-01 through D-04 — what the developer actually locked)
    - .planning/STATE.md (the 2026-08-05 entry describing the authorized one-off admin password-reset procedure, and the pre-phase test-count baseline)
    - app/admin/oversight-panel.tsx (the screen under verification, so the executor can answer the developer's questions without re-deriving behaviour)
  </read_first>
  <action>
    Start the app (local `npm run dev` unless the developer prefers production), apply the two-card `updated_at` backdating fixture described below against the LOCAL database only, recording both card ids and their original values first. Then present the 12 steps to the developer verbatim, one pass, and collect a pass/fail verdict per step. Do not fix anything inline during the walkthrough — record each reported gap as a named follow-up in the SUMMARY and let the developer decide whether it is closed now or tracked. Restore the backdated values at the end regardless of the verdict.
  </action>
  <what-built>
    A new oversight dashboard at `/admin`, replacing the "Em construção" placeholder:
    - a single cross-client table of every non-archived client's `single`/`piece` cards (Cliente · Título · Responsável · Etapa · Atualizado), ordered oldest-touched first
    - a staleness badge per row: neutral under 3 days, warning at 3-6 days, danger at 7+ days
    - two URL-backed filters (client, and person covering both Responsável and Designer/Mídia)
    - whole-row click/keyboard drill-down into `/pm/board?client={id}`
    - a "Carga de trabalho" panel counting each PM's and Editor's active cards with a per-stage breakdown
    - a "Visão geral" sidebar item as the Admin's first nav entry
    Zero schema changes, zero new packages, zero mutating actions.
  </what-built>
  <how-to-verify>
    Run against local `npm run dev` (production is also acceptable if preferred — this phase is read-only and cannot corrupt data either way). You need an Admin login; if no Admin password is at hand, follow the same one-off procedure recorded in STATE.md for 2026-08-05 (temporarily reset an admin account's password, then restore its `must_change_password` flag afterwards) — and only with explicit authorization.

    **Setup (to make the staleness tiers observable):** most real cards will be recently touched, so at least one row must be backdated to exercise the warning/danger tones. Against the LOCAL database only (never production), pick two cards belonging to different clients and set one's `updated_at` to `now() - interval '4 days'` and the other's to `now() - interval '10 days'`. Note both card ids and their original `updated_at` values so they can be restored at step 12.

    1. Log in as Admin. Confirm you land on `/admin` (not `/admin/clients`) and see the page title "Visão geral" — not the old "Em construção" card.
    2. Confirm the left sidebar's FIRST item is "Visão geral" with a gauge icon, and that it is the only highlighted item while you are on `/admin`.
    3. Click "Clientes" in the sidebar. Confirm that on `/admin/clients` only "Clientes" is highlighted — "Visão geral" must NOT also appear active. Return to `/admin`.
    4. Confirm the main table shows cards from MORE THAN ONE client (scan the Cliente column) — this is ADM-01, the whole point of the screen.
    5. Confirm no row shows an empty or "Pacote" Etapa cell — content packages are deliberately excluded from this table.
    6. Confirm the FIRST row is the card you backdated by 10 days, that its Atualizado cell reads "Parado há 10 dias" in a red/destructive tone, and that the 4-day card reads "Parado há 4 dias" in an amber/warning tone. Confirm a recently-touched card reads "Atualizado hoje" (or "ontem") in a plain grey tone. This is ADM-02.
    7. Use the first filter to select a single client. Confirm the table narrows to that client only, the URL becomes `/admin?client=…`, and reloading the page keeps the filter applied.
    8. With that client filter still active, use the second filter ("Todos os responsáveis") to select a person. Confirm the URL now carries BOTH `client=` and `pm=`, and that selecting the person did not silently drop the client filter.
    9. Reset the client filter back to "Todos os clientes" and pick an Editor (or a person you know is set as Designer/Mídia on a card, not as Responsável). Confirm their cards still appear — the person filter must match either assignment column. This is ADM-03's PM half.
    10. Choose a filter combination that matches nothing. Confirm the message reads "Nenhum card encontrado" (NOT "Nenhum card ainda") and that both filter dropdowns are still present and usable so you can recover.
    11. Clear the filters, then click anywhere on a card's row (and separately, tab to a row and press Enter). Confirm both land you on `/pm/board` with that card's client already selected. This is ADM-03's client half.
    12. Scroll to "Carga de trabalho". Confirm: every listed person has at least one active card (nobody shows 0); the stage chips read like "Revisão interna 3"; a card you know is already "Pronto para publicar" is NOT counted. **Expected behaviour worth confirming explicitly:** re-apply a single-client filter at the top and confirm the workload panel does NOT change — it is intentionally operation-wide, answering "who is overloaded overall". Say so if you would rather it followed the filter; that is a design call, not a bug.

    Finally, restore the two backdated `updated_at` values, and confirm a non-Admin (PM or Client) session hitting `/admin` is redirected away rather than shown the dashboard.
  </how-to-verify>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
    <human-check>Developer walks all 12 steps above and reports pass/fail per step</human-check>
  </verify>
  <acceptance_criteria>
    - Developer explicitly confirms steps 1-12, or reports specific failures
    - Step 4 confirmed: cards from at least two distinct clients visible in one table (ADM-01)
    - Step 6 confirmed: three distinct staleness tones observed on real rows, with the most-stale card first (ADM-02)
    - Steps 9 and 11 confirmed: both the person-filter drill-down and the client row drill-down work (ADM-03)
    - Step 12 confirmed: workload panel omits zero-card people and excludes `publish_at`-set cards
    - Backdated `updated_at` values restored to their originals
    - Non-Admin access to `/admin` confirmed blocked
  </acceptance_criteria>
  <done>Developer has walked all 12 steps and returned a verdict, the two backdated `updated_at` values are restored to their originals, and every reported gap is recorded as a named follow-up in 06-03-SUMMARY.md.</done>
  <resume-signal>Type "approved" or list the failing step numbers with what you saw</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-Admin session → `/admin` | Verified live at step 12's tail — middleware role-routing is the gate |
| verification fixture → database | The backdating step writes to `cards.updated_at` and must touch the LOCAL database only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | the checkpoint's own `updated_at` backdating fixture | mitigate | Restricted to the local database, limited to two named card ids, original values recorded before the change and restored at the end of the checkpoint (explicit acceptance criterion). |
| T-06-11 | Elevation of privilege | a PM or Client reaching `/admin` | mitigate | Explicitly verified live at the end of step 12; no code change this phase touches `middleware.ts`'s role routing. |
| T-06-12 | Repudiation | admin password reset for verification access | mitigate | Only with explicit authorization, following the exact procedure already recorded in STATE.md (2026-08-05), including restoring the `must_change_password` flag afterwards, and recorded in the SUMMARY. |
| T-06-SC | Tampering | npm installs | mitigate | Zero installs — this plan changes no source files at all. |
</threat_model>

<verification>
- Four automated gates green before the developer is asked to look at anything
- Zero files under `supabase/` changed across the whole phase
- 12-step live walkthrough completed with a recorded verdict per step
</verification>

<success_criteria>
- ADM-01, ADM-02 and ADM-03 each have a specific, named live verification step confirmed by the developer
- Any gap the developer reports is captured in the SUMMARY as a named follow-up rather than silently patched inline
- Verification fixtures fully reverted
</success_criteria>

<output>
Create `.planning/phases/06-admin-oversight-dashboard/06-03-SUMMARY.md` when done
</output>
