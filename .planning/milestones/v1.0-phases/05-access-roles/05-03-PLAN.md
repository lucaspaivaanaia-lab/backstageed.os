---
phase: 05-access-roles
plan: 03
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - supabase/tests/rls_helpers.sql
  - supabase/tests/0001_rls_pm_scoping_test.sql
  - supabase/tests/0002_rls_client_scoping_test.sql
  - supabase/tests/0003_rls_admin_unrestricted_test.sql
autonomous: true
requirements: [AUTH-06, AUTH-07, AUTH-08]

must_haves:
  truths:
    - "A pgTAP suite runs via `supabase test db` and proves, at the Postgres RLS layer, that a PM sees only clients they are assigned to (AUTH-06)"
    - "The suite proves a Client sees only their own client record and no other client's rows (AUTH-07)"
    - "The suite proves an admin sees every clients/profiles/pm_clients row regardless of assignment (AUTH-08)"
    - "The suite proves a non-admin (PM) CANNOT change their own profiles.role or profiles.status via UPDATE — the 05-01 prevent_profile_privilege_escalation() trigger rejects it (Blocker 1)"
    - "The suite exercises the ACTUAL is_admin()/pm_assigned_clients() helpers, RLS policies, and the column-immutability trigger shipped in 05-01's migrations — it does not redefine them"
    - "`supabase test db` exits 0 with all planned assertions passing"
  artifacts:
    - path: "supabase/tests/rls_helpers.sql"
      provides: "Shared fixture + role-simulation helpers (seed 2 clients, 1 PM assigned to only client A, 1 Client scoped to client A, 1 admin; a set_auth(uid, role) wrapper over set_config('request.jwt.claims'...))"
      contains: "request.jwt.claims"
    - path: "supabase/tests/0001_rls_pm_scoping_test.sql"
      provides: "pgTAP assertions that a PM assigned only to client A can SELECT client A but NOT client B (AUTH-06), AND that a PM cannot UPDATE their own role/status (Blocker 1 privilege-escalation guard)"
      contains: "plan("
    - path: "supabase/tests/0002_rls_client_scoping_test.sql"
      provides: "pgTAP assertions that a Client user scoped to client A sees only their own client row and no other client's data (AUTH-07)"
      contains: "plan("
    - path: "supabase/tests/0003_rls_admin_unrestricted_test.sql"
      provides: "pgTAP assertions that an admin sees all clients, all profiles, and all pm_clients rows (AUTH-08)"
      contains: "plan("
  key_links:
    - from: "supabase/tests/*_test.sql"
      to: "public.is_admin() / public.pm_assigned_clients()"
      via: "RLS policies from 05-01 migration 0004 evaluated under a simulated JWT"
      pattern: "set_config\\('request.jwt.claims'"
    - from: "supabase/tests/0001_rls_pm_scoping_test.sql"
      to: "public.prevent_profile_privilege_escalation()"
      via: "a PM self-UPDATE of role/status that the BEFORE UPDATE trigger must reject"
      pattern: "throws_ok|throws_like"
    - from: "role simulation"
      to: "auth.uid()"
      via: "request.jwt.claims sub + set role authenticated"
      pattern: "set (local )?role"
---

<objective>
Prove — at the Postgres RLS layer, not the UI — the three data-isolation guarantees that are the highest-severity requirements of this phase: a PM only sees clients they are assigned to (AUTH-06), a Client only sees their own client's data (AUTH-07), and an admin sees everything (AUTH-08) — AND prove that a non-admin cannot escalate their own role/status (the 05-01 column-immutability trigger, Blocker 1). Deliver an automated pgTAP suite that runs via `supabase test db`.

Purpose: These three requirements are explicitly non-negotiable "enforced at the data layer (RLS), not just hidden in the UI" per the phase success criteria, and RLS misconfiguration (CVE-2025-48757 class) is the single highest-severity risk in this phase. 05-VALIDATION.md classifies AUTH-06/07/08 as the only requirements in Phase 5 that get automated coverage (pgTAP) rather than manual click-through. This plan is that coverage — it turns the RLS policies (and the privilege-escalation guard) already shipped in 05-01 into a regression-proof, executable proof.

Output: Four SQL files under `supabase/tests/` — a shared fixture/role-simulation helper plus one pgTAP test file per requirement — that exercise the ACTUAL `is_admin()` / `pm_assigned_clients()` helpers, the `profiles`/`pm_clients`/`clients` RLS policies, and the `prevent_profile_privilege_escalation()` trigger from 05-01's migrations, and pass under `supabase test db`.

Delivers AUTH-06 (PM blocked from unassigned clients at RLS), AUTH-07 (Client blocked from other clients' data at RLS), AUTH-08 (admin unrestricted by RLS), plus the Blocker 1 privilege-escalation guard proof. No production code or schema changes — pure test deliverables against the existing schema.
</objective>

<phase_goal>
**As a** platform operator, **I want to** have an automated proof that Supabase RLS actually blocks a PM from clients they are not assigned to, blocks a Client from other clients' data, lets an admin see everything, and stops a PM from making themselves an admin, **so that** the phase's core multi-tenancy and privilege guarantees are regression-tested at the database layer and cannot silently break in a later phase.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-access-roles/05-CONTEXT.md
@.planning/phases/05-access-roles/05-RESEARCH.md
@.planning/phases/05-access-roles/05-VALIDATION.md
@.planning/phases/05-access-roles/05-01-PLAN.md

<interfaces>
<!-- Schema + helpers established by 05-01 (migrations 0001-0004) that these tests consume directly. Do NOT redefine any of these in test files — reference them. -->

From supabase/migrations/0001_profiles.sql (05-01):
- enum public.user_role: 'admin' | 'pm' | 'client'
- enum public.approval_status: 'pending' | 'approved' | 'rejected' | 'deactivated'
- table public.profiles (id uuid PK -> auth.users(id), email text, role user_role, status approval_status, must_change_password boolean, client_id uuid nullable -> clients(id), created_at timestamptz)
- trigger on_auth_user_created -> handle_new_user() (inserts a profiles row from auth.users metadata + copies email)
- trigger prevent_profile_privilege_escalation_trg (BEFORE UPDATE on profiles) -> raises an exception if role or status changes and not is_admin(). THIS is the system-under-test for the Blocker 1 assertion.

From supabase/migrations/0002_clients_stub.sql (05-01):
- table public.clients (id uuid PK default gen_random_uuid(), name text, created_at timestamptz), RLS enabled, seeded with at least one row.

From supabase/migrations/0003_pm_clients.sql (05-01):
- table public.pm_clients (pm_id uuid -> profiles(id), client_id uuid -> clients(id), PK (pm_id, client_id)), RLS enabled, index idx_pm_clients_pm_id.
- partial unique index uq_profiles_one_client_login_per_client on profiles(client_id) where role='client' (D-10; not exercised by this suite but present).

From supabase/migrations/0004_rls_policies.sql (05-01):
- function public.is_admin() returns boolean — language plpgsql, stable, security definer, set search_path = '' — true when caller's profile has role='admin' AND status='approved'.
- function public.pm_assigned_clients() returns setof uuid — language plpgsql, stable, security definer — returns client_ids from pm_clients where pm_id = auth.uid().
- RLS policies on profiles (self-select OR is_admin(); self-UPDATE allowed at the row level, with role/status column-immutability enforced by the 0001 trigger; admin full access), pm_clients (is_admin() OR pm_id = auth.uid()), clients (is_admin() OR id in pm_assigned_clients() OR id = own profile's client_id).

Role simulation in pgTAP against these policies (RESEARCH.md Pattern 4 / Validation Architecture, and Supabase pgTAP docs): a policy references `(select auth.uid())` — `auth.uid()` reads the `sub` claim from `current_setting('request.jwt.claims')`. To simulate a signed-in user in a test, set the GUC and the Postgres role before the assertion:
- `select set_config('request.jwt.claims', json_build_object('sub', <user_uuid>, 'role', 'authenticated')::text, true);`
- `set local role authenticated;`
- run the query, assert visible rows, then `reset role;` and clear the claim for the next actor.
Privilege-escalation assertion uses pgTAP `throws_ok`/`throws_like`: under the simulated PM, `update public.profiles set role = 'admin' where id = <pm_a>` (and a separate `set status = 'approved'`) MUST raise the trigger's exception.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pgTAP fixture + role-simulation helper (shared seed for all three RLS suites)</name>
  <files>supabase/tests/rls_helpers.sql</files>
  <read_first>
    - .planning/phases/05-access-roles/05-RESEARCH.md (Pattern 4 RLS helpers; Validation Architecture -> Phase Requirements Test Map AUTH-06/07/08 -> "supabase test db (pgTAP) ... set role authenticated; set request.jwt.claims = ..."; Pitfall 1 recursion; Pitfall 3 app-role-vs-auth-role)
    - .planning/phases/05-access-roles/05-VALIDATION.md (Test Infrastructure -> `supabase test db`; Per-Task Verification Map AUTH-06/07/08 rows)
    - .planning/phases/05-access-roles/05-01-PLAN.md (Task 3 migration definitions — the exact table/column/function names these fixtures must target, incl. the profiles.email column and the prevent_profile_privilege_escalation trigger)
    - supabase/migrations/0001_profiles.sql
    - supabase/migrations/0002_clients_stub.sql
    - supabase/migrations/0003_pm_clients.sql
    - supabase/migrations/0004_rls_policies.sql
  </read_first>
  <action>Create `supabase/tests/rls_helpers.sql` providing the shared fixture and role-simulation utilities every RLS test file reuses. It MUST NOT redefine `is_admin()`, `pm_assigned_clients()`, the privilege-escalation trigger, or any table/policy — those come from 05-01's migrations and are the system-under-test. The file MUST: (1) insert fixture rows into `auth.users` (with fixed literal uuids so tests are deterministic — e.g. via `auth.users(id, email, ...)` direct inserts, or `auth.admin`-equivalent seeding acceptable in the local test DB) for four actors — `pm_a` (a PM), `client_a_user` (a Client), `admin_user` (an admin), and rely on the `on_auth_user_created` trigger to create their `public.profiles` rows, OR insert profiles directly if the trigger is not desired in the fixture (document which approach is used); ensure the resulting profiles are: `pm_a` role='pm' status='approved', `admin_user` role='admin' status='approved', `client_a_user` role='client' status='approved' client_id=<client A id>; (2) insert two `public.clients` rows with fixed uuids `client_a` and `client_b`; (3) insert one `public.pm_clients` row assigning `pm_a` to `client_a` ONLY (never to `client_b`) — this is the crux of AUTH-06; (4) define a helper function `tests.set_auth(uid uuid)` (or a documented inline snippet if a helper function is disallowed in the test DB) that calls `set_config('request.jwt.claims', json_build_object('sub', uid, 'role', 'authenticated')::text, true)` and `set local role authenticated`, plus a `tests.clear_auth()` that runs `reset role` and clears the claim — matching the RESEARCH.md Validation Architecture pattern `set role authenticated; set request.jwt.claims = ...`. Use fixed literal uuids (not `gen_random_uuid()`) for all four actors and both clients so the assertion files can reference them by constant. Wrap the whole fixture so it can be included/run at the top of each test file (either as a re-runnable `insert ... on conflict do nothing` seed or a documented `\i`/include convention that `supabase test db` supports). Do NOT weaken any RLS policy, disable the privilege-escalation trigger, or run the fixture as a superuser bypass that would mask a real policy failure — fixture inserts run before role-switching; the actual assertions must run under the simulated `authenticated` role so RLS is genuinely enforced.</action>
  <verify>
    <automated>test -f supabase/tests/rls_helpers.sql && grep -vE '^\s*(--|$)' supabase/tests/rls_helpers.sql | grep -c "request.jwt.claims" | grep -qv '^0$' && grep -vE '^\s*(--|$)' supabase/tests/rls_helpers.sql | grep -qi "pm_clients" && echo "fixture helper present with jwt-claims simulation and pm_clients assignment"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/tests/rls_helpers.sql` exists and sets a simulated JWT via `request.jwt.claims` (grep on non-comment lines matches `request.jwt.claims` at least once).
    - The fixture seeds exactly two clients (`client_a`, `client_b`) with fixed literal uuids and assigns the PM to `client_a` ONLY via a single `pm_clients` insert (non-comment grep for `pm_clients` matches; a reviewer confirms no `client_b` assignment row exists).
    - The fixture does NOT redefine `is_admin()`, `pm_assigned_clients()`, the privilege-escalation trigger, `create table`, or any `create policy` (grep: non-comment `grep -c 'create function public.is_admin\|create .*policy\|create table\|prevent_profile_privilege_escalation' supabase/tests/rls_helpers.sql` == 0).
    - Actors have the intended roles: profiles for `pm_a` (pm), `admin_user` (admin), `client_a_user` (client, client_id=client_a) all status='approved' (reviewer confirms against fixture inserts).
  </acceptance_criteria>
  <done>A shared, deterministic fixture exists that seeds two clients, a PM assigned to only one, a Client scoped to one, and an admin, plus a JWT-claims-based role-simulation helper — all targeting 05-01's actual schema without redefining any helper, policy, or trigger.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] pgTAP assertions for AUTH-06/07/08 + privilege-escalation guard + green `supabase test db`</name>
  <files>supabase/tests/0001_rls_pm_scoping_test.sql, supabase/tests/0002_rls_client_scoping_test.sql, supabase/tests/0003_rls_admin_unrestricted_test.sql</files>
  <read_first>
    - .planning/phases/05-access-roles/05-RESEARCH.md (Pattern 4 example policy on clients — the exact USING clause shape these tests assert against; Validation Architecture -> Sampling Rate "per wave merge: supabase test db"; Anti-Patterns "RLS is the boundary, not a UI if-check")
    - .planning/phases/05-access-roles/05-VALIDATION.md (Per-Task Verification Map AUTH-06 T-05-01, AUTH-07 T-05-02, AUTH-08 T-05-03 — Automated Command `supabase test db`)
    - supabase/migrations/0001_profiles.sql (the prevent_profile_privilege_escalation trigger under test)
    - supabase/migrations/0004_rls_policies.sql
    - supabase/tests/rls_helpers.sql
  </read_first>
  <action>Create three pgTAP test files under `supabase/tests/`, each beginning with `begin;`, `select plan(<n>);`, including/loading the fixture from `rls_helpers.sql`, and ending with `select * from finish();` and `rollback;` (standard pgTAP structure that `supabase test db` runs). Each assertion queries a table AS a simulated actor via the `set_auth()` / `clear_auth()` helper and asserts the row set RLS returns. **0001_rls_pm_scoping_test.sql (AUTH-06 + Blocker 1):** authenticate as `pm_a`; assert `select count(*) from public.clients where id = <client_a>` returns 1 (PM sees assigned client) using pgTAP `results_eq`/`is`; assert `select count(*) from public.clients where id = <client_b>` returns 0 (PM CANNOT see unassigned client — the core AUTH-06 guarantee); assert `select count(*) from public.clients` for `pm_a` equals 1 (only the assigned client, not all clients). THEN add the Blocker 1 privilege-escalation assertions (still authenticated as `pm_a`): use pgTAP `throws_ok`/`throws_like` to assert that `update public.profiles set role = 'admin' where id = <pm_a>` RAISES the trigger's exception (the PM cannot promote themselves to admin), and that `update public.profiles set status = 'approved' where id = <pm_a>` — for a value distinct from the current one, or explicitly setting status when a change is attempted — also RAISES (the PM cannot alter their own approval status); include a positive control asserting a non-role/non-status self-update the PM IS allowed to make succeeds (e.g. no error changing must_change_password on their own row, if the RLS self-update policy permits it) so the test proves the trigger is column-scoped, not a blanket update ban. Use descriptive messages naming the requirement (e.g. `'Blocker 1: PM cannot self-escalate role to admin'`, `'Blocker 1: PM cannot self-approve status'`). **0002_rls_client_scoping_test.sql (AUTH-07):** authenticate as `client_a_user`; assert the Client sees exactly their own client row (`clients` where id=<client_a> returns 1) and does NOT see `client_b` (returns 0); assert the Client sees only their own `profiles` row (count of visible profiles = 1, and it is their own id) — proving no cross-client profile leakage. **0003_rls_admin_unrestricted_test.sql (AUTH-08):** authenticate as `admin_user`; assert `select count(*) from public.clients` returns 2 (both clients — admin unrestricted); assert admin sees all `pm_clients` rows and all `profiles` rows (counts equal the fixture totals, i.e. >= the 3 seeded actor profiles and the 1 assignment row). After each actor block, call `clear_auth()` before switching actors. Use pgTAP assertion functions (`is`, `results_eq`, `ok`, `throws_ok`/`throws_like`) with descriptive messages. Set the `select plan(n)` count to the exact number of assertions in each file (remember 0001 now includes the extra privilege-escalation assertions). Then RUN the suite with `supabase test db` (requires a local Supabase stack via `supabase start`, or the linked project's local shadow DB per 05-01's Supabase CLI setup) and iterate until it exits 0 with zero failing assertions. If `supabase start` / local stack cannot be brought up in this environment, this task is BLOCKING: stop and flag that the pgTAP suite must be run against a local Supabase stack before the phase can be verified — do NOT mark the task done on unrun tests.</action>
  <verify>
    <automated>supabase test db 2>&1 | tee /tmp/pgtap-out.txt; grep -Eiq "(^ok |Result: PASS|All tests successful|tests passed)" /tmp/pgtap-out.txt && ! grep -Eiq "(^not ok|Result: FAIL|# Failed)" /tmp/pgtap-out.txt && echo "pgTAP suite green"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase test db` exits 0 and reports all planned assertions passing (verify command prints "pgTAP suite green"; no `not ok` / `# Failed` lines).
    - `0001_rls_pm_scoping_test.sql` contains an assertion that the PM's visible-count for the UNASSIGNED client (`client_b`) is 0 (grep: file references `client_b` in an assertion with expected 0) — the literal AUTH-06 block guarantee.
    - `0001_rls_pm_scoping_test.sql` contains `throws_ok` or `throws_like` assertions proving a PM's self-UPDATE of `role` to 'admin' AND of `status` are rejected by the trigger (grep: `grep -Eci 'throws_ok|throws_like' supabase/tests/0001_rls_pm_scoping_test.sql` >= 2) — the Blocker 1 privilege-escalation guard.
    - `0002_rls_client_scoping_test.sql` asserts the Client sees exactly 1 client row (their own) and 0 rows for the other client (reviewer confirms both assertions present with the AUTH-07 message).
    - `0003_rls_admin_unrestricted_test.sql` asserts the admin's visible `clients` count is 2 (both fixture clients) — the AUTH-08 unrestricted guarantee.
    - Each test file calls `plan(<n>)` matching its assertion count and finishes with `finish()` inside a `begin;`/`rollback;` transaction (grep: each file contains `plan(` and `finish(`).
    - No test file redefines a helper, policy, or trigger (grep: `grep -c 'create policy\|create function public.is_admin\|create function public.pm_assigned\|prevent_profile_privilege_escalation' supabase/tests/*_test.sql` == 0 — the trigger name may appear only inside a throws_ok expected-message string, which the reviewer confirms is not a redefinition) — tests exercise the shipped 05-01 objects.
  </acceptance_criteria>
  <done>Three pgTAP files assert AUTH-06/07/08 at the RLS layer against 05-01's actual policies AND prove the prevent_profile_privilege_escalation trigger blocks a PM from self-escalating role/status; `supabase test db` runs green with every planned assertion passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated JWT -> Postgres RLS | The entire multi-tenancy guarantee rests on RLS correctly reading `auth.uid()` from the JWT and gating rows; this plan is the adversarial test of that boundary |
| Authenticated JWT -> profiles column immutability | A PM's own row is self-updatable at the row level; the column-immutability trigger must still block role/status changes — this plan adversarially tests that boundary |
| Test fixture -> policy under test | The fixture must seed data WITHOUT bypassing the policy/trigger it is meant to test (no superuser read that masks a real RLS hole) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Information Disclosure | A PM reading a client they are not assigned to (broken/absent RLS policy on clients) | mitigate | 0001 test asserts PM's visible count for unassigned `client_b` is exactly 0 under a simulated authenticated JWT (Task 2) |
| T-05-11 | Information Disclosure | A Client reading another client's rows (broken profiles/clients RLS) | mitigate | 0002 test asserts the Client sees exactly 1 client (own) and only their own profile row (Task 2) |
| T-05-12 | Elevation of Privilege (false-negative test) | A test that passes because the fixture ran as superuser and RLS was never actually enforced | mitigate | Fixture inserts run before role-switch; every assertion runs under `set local role authenticated` with a simulated JWT so RLS is genuinely evaluated (Task 1 + Task 2) |
| T-05-13 | Tampering | A test that redefines is_admin()/policies/trigger, testing a stub instead of the shipped object | mitigate | Grep gate forbids `create policy` / `create function public.is_admin` / `pm_assigned_clients` / trigger redefinition in any test file (Task 1 + Task 2 acceptance criteria) |
| T-05-19 | Elevation of Privilege | A PM promoting themselves to admin or self-approving via a direct profiles UPDATE | mitigate | 0001 test uses throws_ok/throws_like to prove the prevent_profile_privilege_escalation trigger rejects self role/status changes (Task 2) |
| T-05-SC | Tampering | npm installs | accept | No new packages — pgTAP ships with the Supabase local stack; `supabase` CLI already a dev dep from 05-01's Approved Package Legitimacy Audit; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `supabase test db` exits 0 with all planned assertions passing (no `not ok` / `# Failed`).
- 0001 proves a PM sees the assigned client and is blocked (count 0) from the unassigned one (AUTH-06), AND that a PM cannot self-escalate role/status (Blocker 1, throws_ok/throws_like).
- 0002 proves a Client sees only their own client and only their own profile (AUTH-07).
- 0003 proves an admin sees both clients and all profiles/pm_clients rows (AUTH-08).
- No test file redefines helpers, policies, or the trigger; assertions run under a simulated `authenticated` JWT so RLS + the trigger are genuinely enforced.
</verification>

<success_criteria>
- An automated pgTAP suite (`supabase test db`) proves at the Postgres RLS layer that a PM is blocked from clients they are not assigned to (AUTH-06).
- The suite proves a Client is blocked from other clients' data at the RLS layer (AUTH-07).
- The suite proves an admin is unrestricted by RLS and sees everything (AUTH-08).
- The suite proves a non-admin cannot change their own role/status (05-01 column-immutability trigger, Blocker 1).
- The suite exercises 05-01's actual `is_admin()` / `pm_assigned_clients()` helpers, RLS policies, and privilege-escalation trigger and exits 0.
</success_criteria>

<output>
Create `.planning/phases/05-access-roles/05-03-SUMMARY.md` when done.
</output>
